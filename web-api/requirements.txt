# ===========================================
# Formlabs 원격제어 시스템 - 의존성 목록
# ===========================================

# Web Framework
fastapi>=0.109.0
uvicorn[standard]>=0.27.0

# HTTP Client
httpx>=0.26.0

# Data Validation
pydantic>=2.5.0
pydantic-settings>=2.1.0

# Database (Phase 2: Local API)
sqlalchemy>=2.0.25
# asyncpg>=0.29.0  # PostgreSQL용 (나중에 사용)
# alembic>=1.13.0  # 마이그레이션 (나중에 사용)

# MQTT (Phase 4: Vision)
aiomqtt>=2.0.0

# Phase 3: 시퀀스 서비스 (한솔코에버 자동화)
pymysql>=1.1.0
pymodbus>=3.6.0

# 파일 업로드
python-multipart>=0.0.6

# WebSocket
websockets>=12.0

# 환경변수
python-dotenv>=1.0.0

# 인증 (JWT + bcrypt)
python-jose[cryptography]>=3.3.0
bcrypt>=4.0.0

# 로깅
python-json-logger>=2.0.7

# 테스트
pytest>=7.4.0
pytest-asyncio>=0.23.0
pytest-cov>=4.1.0

# 개발 도구
black>=24.1.0
isort>=5.13.0
mypy>=1.8.0

# 타입 힌트
types-python-dateutil>=2.8.19
