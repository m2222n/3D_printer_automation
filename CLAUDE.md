# 3D Printer Automation System

> **새 세션 시작 시**: CLAUDE.md, CLAUDE.local.md 읽은 후 `~/.claude/projects/-home-jtm/memory/MEMORY.md`도 반드시 읽을 것

---

## 🥇 프로젝트 Goal — 모든 목표의 기준 (2026-08-18 태민님 명시)

> **태민님**: *"모든 것의 목표의 기준은 내가 방금 올려준 **대표님 기획서**를 참고하면 돼.
> **그게 프로젝트의 Goal이야.**"*
> 📌 기준 문서 = `오리누_서식3_사업계획서_협약변경_260815_최종.pdf`(8/15자 수정 사업계획서)

**Goal** = 3D프린팅 자동화 라인(셀A)과 후가공 자동화 라인(셀B)을 **통합 제어 서버와 엣지AI 서버로
제어**하고 **클라우드 기반 Lite MES/ATS 대시보드를 구축**하여 **실시간 생산 관리 체계를 완성**한다.
(협약 ~**2026.10.31** · 총사업비 6,000만)

### 📦 산출물 6개 = "무엇을 만들어야 하는가"

| No | 완성물 | 일정 |
|---|---|---|
| ① | **통합 제어 서버 시스템**(셀A+셀B 통합 제어 · 리모트IO·Modbus TCP·MQTT·웹소켓 · **셀A→셀B 공정 연계 SW** · 셀별 재고) | 7~9월 |
| ② | **셀B 제어 소프트웨어**(리모트IO↔로봇암 Modbus TCP · **부품 인식(비전AI) 인터페이스 통합** · 스핀들·그리퍼 시퀀스) ⭐**빈피킹이 여기 들어간다** | **8~9월** |
| ③ | **비전AI 기반 상태감시 모듈**(세척기·경화기 화면·LED 인식 · **3조합 전부 구현·검증**) | 진행 중 |
| ④ | **셀A 출력물 치수검증 시스템**(외관 전반 치수 + 미성형 검출 · **홀은 유무 판정 수준**) | **9~10월** |
| ⑤ | **클라우드 Lite MES/ATS 대시보드**(실시간 모니터링·생산스케줄링·KPI) | **9~10월** |
| ⑥ | **특허 출원 1건**(클라우드 기반 이종 3D프린팅+후가공 로봇 공정 통합 제어) | 10월 |

### 📊 정량목표 5개 = "성공/실패"의 판정 기준

| # | 항목 | 전 | 목표 | 평가 |
|---|---|---|---|---|
| 1 | 제조 리드타임 단축 | 8H | **5.6H (30%↓)** | 전체 공정 시연 |
| 2 | 통합 모니터링 응답속도 | 없음 | **3초 이내** | ⭐**KTR SW 성능 시험성적서** |
| 3 | 실시간 모니터링 설비 수 | 0대 | **10대 이상** | 대시보드 시연 |
| 4 | 생산 데이터 디지털화율 | 0%(수기 엑셀) | **90% 이상** | 클라우드 DB 리포트 |
| 5 | 지식재산권 출원 | 0건 | **1건**(국내) | 출원증명서 |

🚨 **리드타임 30%의 정의를 지킬 것**(기획서 각주) = *"물리적 이송시간 단축이 아니라 **장비 상태
육안 확인 대기시간·수동 기록 및 공정 전환 판단 지연의 제거**에 의한 것(셀 간 이송은 작업자 수동 유지)"*
⇒ ⭐ **"로봇이 빨라져서"가 아니라 "사람이 기다리고 적는 시간을 없애서"** 달성하는 숫자이고,
이것이 8/5 *"로봇 제어 제외"*와 정합하는 이유다.

### ⭐⭐ 매 작업 전 자문 = 2단계

1. **이 작업이 산출물 6개·정량목표 5개 중 무엇에 기여하나?** → **못 대면 Goal 밖이다**
2. (빈피킹 작업이면) 이 작업이 **카메라 인식·로봇 파지**에 기여하나?

### ⚠️ Goal 밖 (사업 후 / 별개 트랙)

**hand-eye·로봇 파지·`rx`/`ry` 티칭**(8/5 범위 밖, 기획서에도 없음) ·
**SaaS 3축**(6/18 지시, 별개 트랙 — 단 기획서 5-1이 *"패키지 제품화해 타 기업 공급"*을 명시하므로
**사업 종료 후 본선**) · **KAIST 논문** · **완전 무인화**(기획서는 "셀 단위 무인화 기반"까지)

상세: `memory/project_project_goal_0815.md` · 기획서 분석 `memory/project_agreement_change_0815.md`

---

## 🔒 보안 원칙 — 모든 외부 유출 가능 출력물에 적용 (최우선 필독)

> **⚠️ 이 항목은 README 한정이 아님.** 외부로 나갈 수 있는 **모든 출력물**에 적용된다. 작업 전에 "이게 외부에 보일 수 있나?"를 먼저 물어볼 것.

### 적용 범위 (외부 유출 가능 출력물)
- **GitHub 리포 전체** — README, 소스코드 주석, 커밋 메시지, PR/이슈 본문, 릴리즈 노트, public/private 무관 (Private도 한솔 미러로 공유됨)
- **공유 문서** — 배포용 PDF, PPT, docx, Google Docs, Notion, 회의자료
- **외부 메시지** — 카톡, 이메일, Slack, 문자 (한솔·대표·파트너·지원사업 담당자 등)
- **스크린샷·동영상** — 데모 녹화, 버그 리포트 캡처, 발표 슬라이드 이미지
- **로그·리포트** — IRIS 보고, 사업보고서, 외부 제출 파일
- **채팅 히스토리** — 외부 Claude 세션(웹 Claude 포함), 공유된 대화

### 절대 포함 금지 항목

| 카테고리 | 금지 항목 |
|---------|----------|
| Credentials | Basic Auth 비번, OAuth Client ID/Secret, API 키, Service Token, DB 비번, SSH 키, 2FA Backup Codes |
| 네트워크 | 공인 IP, VPN IP, 내부 IP (192.168.*, 10.*), 도메인(`factory.flickdone.com` 등), SSH 포트/커맨드 |
| 인프라 식별자 | Cloudflare Tunnel ID, Windows 서비스명, NSSM 경로, 공장 PC 디렉토리 경로, AnyDesk ID, 프린터 시리얼 |
| 개인정보 | 개인 이메일, 담당자 실명(예승/파트장/기원 등), 회사 내부 호칭("대표님"), 전화번호 |
| 내부 운영 | 장비 입고일, 수리 이력, 교육 이력, 마일스톤 일자별, 회의 안건/발언, 머지 커밋 해시 |
| 사업정보 | 사업비, 계약 조건, 협업 구조 상세, 타 업체 가격 |
| 분쟁 가능 발언 | 특정 직원·업체에 대한 부정적 평가, 내부 갈등 관련 코멘트 |

### 예외 — 내부 문서는 상세히 OK
- `CLAUDE.md`, `CLAUDE.local.md`, `~/.claude/projects/-home-jtm/memory/*.md`
- 태민님 개인 로컬 메모 (Mac 노트 앱 등)

이 파일들은 git 추적되어도 **공개되지 않는 본인 개발 문서**이므로 상세할수록 좋다. 위 규칙은 **외부 유출 가능성이 조금이라도 있는 출력물**에만 적용.

### 작업 프로토콜 (반드시 준수)

1. **작업 시작 전 자문**: "이 결과물이 어디까지 보일 수 있나?"
   - 내부 메모리·CLAUDE.md → 상세 OK
   - 그 외 전부 → 민감 정보 제거 모드
2. **작성 중**: credentials·IP·도메인·실명·경로는 **플레이스홀더**로 (`your_password`, `<SERVER_IP>`, `<담당자>`)
3. **저장/커밋 전 self-check**:
   ```bash
   grep -niE "orinu2026|jtm@|61\.109|106\.244|10\.145|192\.168|b939f49b|이예승|김주엽|김기원|대표님|factory\.flickdone|D:\\\\3D_printer|admin.*password|Bearer " <대상파일>
   ```
   하나라도 걸리면 push/전송 중단하고 정리
4. **스크린샷 공유 전**: URL 바, 터미널 prompt, 파일 경로, `.env` 내용 마스킹 확인
5. **의심스러우면 멈추고 태민님 확인 요청** — push/전송 먼저 하지 말 것

### 2026-04-24 사고 (실제 있었던 일, 반복 금지)

README 전면 개편(`51fce05`) 시 위 7 카테고리 전부 박아서 origin + personal(한솔 미러) 양쪽 push. Basic Auth 비번 `orinu2026!`, Cloudflare Tunnel ID, 공인 IP 2개, 공장 PC 경로, 담당자 실명 3명, 개인 이메일까지. 태민님 직접 지적 후 `1272ddb`로 정리.

**원인**: "내부 문서처럼 상세할수록 좋다"는 기준을 **공개 범주 문서에 잘못 적용**. 외부 노출 가능성 판단을 생략한 것이 근본 실수.

**상세**: `memory/feedback_readme_public_security.md`

---

## 프로젝트 개요

### 기본 정보
| 항목 | 내용 |
|------|------|
| 프로젝트명 | 3D프린터-로봇 연동 자동화 시스템 |
| 회사 | 오리누 주식회사 (구 플릭던) |
| GitHub (회사) | https://github.com/orinu-ai/3D_printer_automation (Private) |
| GitHub (개인) | https://github.com/m2222n/3D_printer_automation (Private) — 한솔코에버 협업용 |
| 서버 경로 | `/home/jtm/3D_printer_automation/` |
| 사업 | 2025년 경기도 제조로봇 이니셔티브 (사업비 2억원) |
| 사업 기간 | 협약일 ~ 2025.12.31 |
| 담당 개발자 | 정태민 (1인 개발) |

### 프로젝트 목적
점자프린터 플라스틱 부품(약 20종) 생산 공정 자동화
- **1차 목표**: 웹/앱에서 프린터 완료 신호 수신 및 새로운 프린팅 요청 전송
- **궁극적 목표**: 서버가 3D프린터 현황 모니터링 + 로봇 작업 지시 + 전체 공정 자동화 제어

---

## ⭐ 대표님 6/18 신규 방향성 — 3D 프린팅 SaaS 플랫폼 + 후공정 모니터링 + ERP (제품화)

> **2026-06-18 대표님 녹음 지시**: 현 시스템(3D프린터 모니터링 + 작업지시)을 앞뒤로 확장해 **타 기업에 판매 가능한 제품**으로 키워라.

세 축으로 확장:
1. **① 앞단 — 고객 셀프 주문 홈페이지**: 고객이 모델 파일 업로드 → **자동 견적** → **PG 결제** → 결제 시 **자동으로 작업 큐 투입**(우리가 출력 시작 누르는 것까지 자동) → 완료 시 **담당자 알림**(카카오톡/SMS/우리 앱 푸시) → 픽업. ⭐ **이 시스템 자체를 타 기업에 판매**(멀티테넌트 제품화).
2. **② 뒷단 — 후공정 작업 카운팅 모니터링**(현재 전무): 공장 모니터에 "어떤 파츠 / 어떤 가공 / 총 몇 개 중 몇 개째" 실시간 표시. ⭐ **로봇과 통신**(동일 작업 몇 번째인지) — 기존 HCR Modbus 자산 연계.
3. **③ ERP**: 원재료(SLA 레진 / FDM 필라멘트) **입고·사용량·잔여·발주 알림** 관리. 6/1 ERP 참고영상 2개(`memory/project_ceo_terminology_study_0601.md`) 기반 학습.

**진행 방식 (대표님 명시)**: **개발 기획을 Claude와 정리 → 대표님께 먼저 보여드리고 검토** → 실개발은 그 후 별도 결정. **바이브 코딩**으로 개발. (코드 짜기 전 명세 → 대표님 align = 5/6 방식과 동일)

⚠️ 현 주업무(빈피킹 + KAIST, ~7/9)와 **별개 트랙**. 6/18 시점 = **기획만 먼저**(실개발 미착수). 6/1 제조용어 프레임(현 웹=MES / 로봇=ATS)과 정합: ①상거래 프론트 + ②MES 확장 + ③ERP 계층 추가.

⭐ **6/29(월) 착수**: 대표님이 6/26 "다음주 월요일에 이 건으로 얘기하자" → 출근 직후 논의 + 3축 기획 초안 작성 착수 약속.

✅ **6/29 기획 초안 작성 완료**: `docs/SaaS_플랫폼_기획초안_0629.md`(MD) + `docs/saas_plan_0629.html`(PDF용). 방향=비전+로드맵, 1순위=① 주문 홈페이지. ⭐**핵심 발견=기존 자산이 3축에 거의 다 매핑**(알림 sender 추상화·Formlabs 견적원천·레진잔량 보유, 결제만 신규). 외부연동 1순위=포트원/솔라피/trimesh. ⚠️행정심사(PG·알림톡)가 코딩보다 오래걸림→선신청 권장.

⭐⭐ **7/7 대표님 회의 = 기획 고도화 지시 (본업 P0, 9월말 지원사업 마감)**: 6/29 초안이 "너무 포괄적"→**정식 기획서**로. ①현 개발현황 정리 ②**타사/FDM 프린터 범용화**(API 있으면 API·없으면 오픈소스 슬라이서, FDM은 오픈소스 잘 나와 난이도 낮음) ③주문·관리 시스템 ④⭐**ERP/MES/ATS로 발전하려면 어떤 기능 필요한지="이거부터"**(1순위). +한솔 로봇교육 재요청. → ✅ **기능정의 문서** `docs/MES_ERP_ATS_기능정의_0710.md`(현황§1·3계층 필요기능§2·프린터 범용화 어댑터§3·주문관리§4·대표님 결정 7가지§5). ✅ **현 앱 정직 현황**(코드 전수조사)=SLA 전용 소형 MES+로봇 ATS(7탭), 3축(주문·ERP·카운팅)은 코드 0건. ✅ 앱 상태점검=3서버 200·PreFormServer connected·프린터 NOT READY(실물출력 시연은 탱크 필요). ✅ **대표님 보고용 HTML 문서**=`/data/jtm/synth_out/factory_system_report.html`(현 앱 설명+앞으로 방향 4파트, sim2real 스타일·PDF버튼, 내부용). 보고용 문서 표준형식→`memory/feedback_report_doc_format.md`. ✅ **코드 리팩터링 완료 = 프린터 어댑터 토대**(`memory/project_printer_adapter_refactor_0710.md`): Formlabs 전용→`PrinterAdapter` Protocol 격리(`web-api/app/adapters/`), 테스트 0→27개 신설(⚠️PrinterSummary 5필드=로봇 계약 동결), 죽은코드(local-api·basic_auth) 정리, `PRINTER_VENDOR` config 스위치. FDM 추가 시 어댑터+factory elif만. 런타임 0 변경(실구동 검증). main 머지(`2a6d69a`)·dual push 완료. **✅ 3개 서버 전부 배포·검증 완료**(6000·카카오VM·공장PC, web-api·JWT·CMD DB등록 정상). ⚠️공장PC 시뮬 CMD는 프린터 4대 OFFLINE(전원·탱크 없음)으로 미진행=리팩터링 무관 물리이슈. ✅ **7/10 대표님 보고 3개 문서(PDF) 제출**(내부용, 대표님만)=①빈피킹 비전 AI 성과 보고(KAIST 우수상 검증·위치0.91/종류0.85/종합0.684) ②sim2real 학습 리포트(11섹션 상세) ③3D 프린터 자동화 현황&확장 방향(SaaS 7/7 지시 답). 배경=대표님 "그동안 공장개발 못 하고 KAIST 교육 들었으니 성과 달라". ⏭️ **월요일(7/13) 대표님 회의에서 SaaS 방향 확정**(3축 우선순위·9월범위·1순위·판매모델·견적단가)→착수. 회의 전=회신 미정.

상세: `memory/project_saas_platform_directive_0618.md` + `memory/project_ceo_saas_directive_0707.md` + `memory/project_current_app_status_0710.md` + `memory/project_printer_adapter_refactor_0710.md`

---

## 🎉🎉 협약변경 문서 = 미결 3건이 한꺼번에 풀렸다 (2026-08-15 작성 / 8-18 공유)

> 대표님이 구글 드라이브에 **수정 사업계획서 + 협약 변경 신청서**를 올리고
> *"구매품에 L2 스위치 하나 추가했습니다. 치수검증 광학계에 300mm 조명 및 렌즈 변경했습니다."*
>
> ⭐⭐⭐ **8/5 회의 이후 두 주간 우리 작업을 막고 있던 미결 3건이 이 문서로 전부 해소됐다.**
> 총사업비 **6,000만 / 협약기간 ~2026.10.31 변동 없음**(비목 간 조정만).

### 🎉 ① 「셀B 제어 SW」충돌 소멸 — 위탁1이 통째로 교체됐다

| | 당초 | 변경 |
|---|---|---|
| **위탁1** | **셀B 제어SW + 통합 미들웨어 개발** (2,000만) | **디지털 제어 인프라 구축 설계·시공** (**1,600만**) |
| **위탁2** | 클라우드 MES/ATS 기획·설계 (400만) | **클라우드 대시보드 개발·구축** (**800만**) |

**변경사유서 다섯째** = *"소프트웨어 설계 및 백엔드 개발은 **자체 수행으로 전환**하고,
당사가 보유하지 않은 **기구 시공 역량과 프론트엔드 구현**을 위탁 범위로 확정"*

⇒ 🎯 **위탁1에 "제어 SW"라는 말 자체가 없어졌다** — 8/5 *"로봇 제어 전부 제외"*와의 정면 충돌이
**물어볼 필요 없이 사라졌고**, 8/7 범위맵의 "로봇 이송 노드 다수" 충돌도 함께 풀린다.

⚠️ **단 완성물 ②는 여전히 「셀B 제어 소프트웨어」로 남아 있다** = *"리모트 IO ↔ 로봇암
Modbus TCP 연동 SW, 부품 인식(비전AI) 인터페이스 통합, 스핀들·그리퍼 공정 시퀀스 제어"*.
⭐ **위탁에서 빠졌을 뿐 산출물은 살아 있고 이제 우리가 짠다.** 대표님 8/18 답변
(**트리거 방식** = *"이미 짜여져 있는 로직을 실행해라 라는 명령(신호)를 전달"*)으로 읽으면 8/5와 정합한다.

🚨 **위탁1 일정이 9~10월로 밀렸다**(당초 5~8월) ⇒ **배선·구조물이 9월에 오므로 그 전까지
SW는 현 배선 상태로 개발한다.** 그리고 과업 ⑤가 **"재배치 후 로봇 티칭 경로 재설정 및 간섭 검증"**
이라 ⚠️**빈피킹 `PICK_CAPTURE_POSE`도 9월 재배치 후 다시 잡아야 할 수 있다.**

### 🎉 ② 치수검사 측정 대상 확정 — 착수를 막던 것이 풀렸다

완성물 ④ 원문:
> *"3D프린팅 출력물의 **외관 전반 치수 확인 및 미성형 부위 검출**
> (**홀은 유무 판정 수준**, 장비 도입 + 기본 구축 범위: 광학계 설치, 캘리브레이션,
> 기준물 검증, 기본 촬영·판정 파이프라인)"*

🚨🚨 **이것이 8/3~8/4 광학 결론을 무효화한다:**

| | 8/3~8/4 우리 전제 | 8/15 확정 |
|---|---|---|
| 재는 것 | **홀 지름·위치**(ø2.7·3.0·5.0) | **외관 전반 치수**(수축·휨) |
| 홀 | ±0.1mm **측정** | 🚨**유무 판정만** |
| 필요 FOV | **60~70mm** | **100mm + 일반 80~300mm** |
| 부품 크기 | 홀 국부만 | **최대 외형 300mm 대응** |

⭐⭐ **전제가 바뀌면 결론도 바뀐다**(8/14 제외 6종과 같은 형태). 8/4에 *"FOV를 명시 안 하면
관행대로 넓은 쪽이 온다"*고 경고하며 60~70mm를 권고했는데, **재는 대상이 외관 치수면
넓은 FOV가 오히려 맞다**(300mm 부품을 담아야 한다).
🚨 **내 권고가 틀린 게 아니라 그 권고의 전제가 폐기된 것이다** — `dimension_inspection` 문서의
광학 검산은 **"홀 지름 측정" 시나리오에 한해 유효**로 격하한다.

**확정 광학계** = 12.3MP 글로벌 셔터 · **텔레센트릭 1(FOV 100mm)** + **일반 렌즈 3(FOV 80~300mm)** ·
**백라이트 300×300(주력)** + 링라이트(보조) + 스트로브 컨트롤러 · 지그 450×600 · 차광박스 · **핀 게이지**

⭐ **8/4 권고 2건이 그대로 채택됐다**: ①*"텔레센트릭 1개 + 일반 렌즈 2~3개"*(대상이 바뀔 수 있어서)
②*"측정값을 검증할 기준물이 빠져 있다"* → **핀 게이지 반영**.
⭐ **백라이트가 주력으로 올라간 것도 정합** — 8/3에 *"백라이트 실루엣은 관통홀+평면+불투명일 때만
유효"*라 했는데 **재는 것이 외관 윤곽이면 실루엣이 정확히 맞는 방식**이다.

🔴 **셀B는 검사 대상이 아니다**(구성도 명시) = *"이미 형성된 홀의 디버링과 표면 서포트 잔재
제거 공정이라 **치수 변동 요인이 없고**, 가공 실패 시 폐기하므로 별도 치수검사 대상이 아니다."*
⇒ ⭐ 8/5에 *"드릴 진동 격리 논리가 약해져 자리 근거를 재검토해야 한다"*고 적은 것이 정리됐다.

### 🎉 ③ "1차 개발 범위" = 월별 일정으로 확정

장비 발주 **8월**(변경 승인 후) / **셀B 제어SW 8~9월** / 통합서버·셀 연동 7~9월 /
**치수검증 구축 9~10월** / 대시보드 9~10월(위탁2) / 통합테스트 8~10월 / 특허·정산 10월
⇒ ⭐ 선구매 우선순위(**리모트IO 먼저·납기 2주**)와 정합한다.

### ⭐⭐ 추론 위치 = 시점이 다른 이야기였다 (내 "어긋남" 판정 철회)

> 🚨 **8/18에 내가 이것을 "우리 기록과 어긋나는 것"으로 올린 것은 과했다.**
> 태민님 지적 = *"젯슨 Thor를 구매하실 거고, 오는 데 시간이 걸리는 동안 IPC-510으로 돌리는 것이다.
> 결과적으로 비싼 Thor를 사는데 안 쓸 이유가 있나?"* ⇒ **맞다. 안 쓸 이유가 없다.**

| 시점 | 추론 위치 | 이유 |
|---|---|---|
| **지금 (Thor 도착 전)** | **IPC-510**(RTX 5060 보유 / 8/14 검증은 CPU) | Thor를 기다리면 **개발이 멈춘다** |
| **최종 배포** | **Thor**(엣지 AI 서버) | 사업계획서 구조 = 엣지가 추론 담당. **사는 이유가 이것** |

⇒ ⭐ **문서(젯슨)와 8/12 판단(IPC)은 애초에 모순이 아니었다** — 대표님 문서는 **최종 배포 형태**,
IPC는 **그때까지의 개발·검증 환경**이다. 📌**7/13 방침과 그대로 일치**
(**A100=개발 / Thor=배포 엣지 / IPC-510=공장 하드웨어**)이고, 8/12에 내가 *"Thor는 IPC 대체가 아니라
우분투인 채로 옆에 붙는다"*고 정정한 것도 이 구도다.

🚨 **내가 내 8/12 근거를 잘못 인용했다** — 당시 실제 근거는 *"추론을 IPC에서 (영구히) 돌린다"*가
아니라 **"개발이 Thor 도착을 기다리지 않는다"**였고, 그것이 **선결제에서 뺄 근거**였다.
**구매 자체는 유지**였고 사유까지 적어뒀다(*"사업비는 기간 끝나면 못 쓴다"*).
⇒ ⭐⭐ **"살 것인가"와 "선결제로 당길 것인가"는 다른 판단이라는 그때 내 원칙을 엿새 뒤 스스로 뭉갰다.**
8/14 *"결론이 같다"와 "근거가 유효하다"는 다른 문제*와 같은 계열인데, 이번엔 **과거 근거를 실제보다
강하게 인용해 없는 충돌을 만들었다.**

**🟡 그래서 남는 진짜 질문 = "언제 무엇을 이식하는가"** (Thor=**ARM64** / IPC·6000·A100=**x86_64**)

| 항목 | ARM64 난이도 |
|---|---|
| 우리 파이썬 코드(`depth_track`·6요소 E2E·게이트·`pick_socket_server`) | 🟢 **그대로 돈다**(순수 파이썬) |
| **torch** | 🟡 **JetPack용 휠을 NVIDIA가 별도 배포**(x86 휠 불가) |
| **pypylon**(Basler) | 🟡 ARM64 리눅스 빌드 확인 필요 |
| 모델 체크포인트(`T100/best.pt`) | 🟢 아키텍처 무관 |

⇒ ⭐ **"이식"이 아니라 "환경 재구축"**이다 — 8/14에 IPC에서 한 것(Python 3.12 · torch+cpu ·
numpy 1.26.4 · pillow · scipy)을 Thor에서 한 번 더 하는 일이고 **import 전수 추출** 방법이 재사용된다.
🚨 **새 변수 하나** = Thor는 **GPU 추론이 목적**이므로 CPU 조합이 아니라 **JetPack CUDA 조합**이 된다
(8/14 *"되는 것을 먼저 만들고 빨라지는 것은 다음"*의 **그 "다음"**).

**⭐ 지금 IPC 작업은 버려지지 않는다** = ①⭐⭐**정답 대조표**(6000과 소수점까지 일치한 9건·
`r_guide_a_r` 0.991이 **Thor 검증의 기준선**, 다르면 이식 오류 신호) ②**검증 기준**
(*"개수가 아니라 부품 이름·score를 대조한다"*) ③**IPC에 남는 것**=web-api·pylon·서비스 등록
(**카메라·로봇이 물리적으로 붙는 자리**라 Thor가 와도 남는다)

**🟡 젯슨이 두 개다 — 어느 쪽이 완성물 ②의 대상인가**

| 위치 | 기재 | 상태 |
|---|---|---|
| 디지털 솔루션 **보유현황** | **"젯슨 나노 비전AI 시스템(셀B)"** · 2026.02 · 활용도 "중" | 🚨**우리가 실물·스펙 확인한 기록 0건** |
| **이번 도입 장비** | **"Jetson AGX 개발키트"** | 🛒 신규 구매 |

🚨 완성물 ②의 *"젯슨(비전AI) ↔ 제어SW 통신 인터페이스 구축(자체 개발)"*이 **어느 쪽인지가 8~9월
작업 대상을 가른다**(기구축 나노면 **실물 확인이 선행조건**, 신규 Thor면 **도착 시점에 착수**).
💡 7/23 대표님 *"Orin NX·Nano 이미 보유"*가 보유현황 쪽일 가능성.

### ✅ 우리 기록과 정확히 맞는 것

- ⭐⭐ **셀 간 이송 = 작업자 수동 유지**를 여러 곳에 반복 명시. 리드타임 30%도
  *"물리적 이송시간 단축이 아니라 **장비 상태 육안 확인 대기시간·수동 기록 및 공정 전환 판단
  지연의 제거**"*로 각주 처리 ⇒ 8/5 이후 우리 판단과 동일. 별지3도 **"소프트웨어 공정 연계"**로 조정
- 정성적 목표가 **"셀 단위 무인화"**이고 완전 무인화가 아님을 명시
- **KTR = SW 성능 시험성적서 / 통합 모니터링 응답속도 3초 이내**
  💡 8/5 실측 추론 최악 **2.67s**가 참고선이나 ⚠️**대상이 다를 수 있다**(우리 측정은 빈피킹 추론) → 확인
- 비전AI 상태감시 = **3조합 모두 구현·검증**(NPU 온디바이스 / 웹캠+로컬 엣지 / 웹캠+클라우드)

**사업비** = 장비 2,400→**2,950만(+550)** · 위탁 2,400만(내부 재조정) · **지식재산 850→300만(PCT 550만 제외)**
⇒ ⭐ **장비 증액 재원이 PCT 포기**다. 증감 사유에 *"리모트 I/O 및 산업용 네트워크 장비 구성 조정"*이
있고 이것이 대표님 8/18 **"L2 스위치 추가"**(산업용 관리형 L2 스위치, PoE 스위치와 별도 항목)다.

**인력 4→5명**(성주희 연구원 신규). 태민님 = *"셀A Form API 서버 통합·데이터 수집·비전AI 상태감시
모듈 개발·클라우드 연동"*, 사업 후 = *"통합 제어 서버 및 엣지 계층 운영·유지보수 전담"*.
⚠️ **조직도상 "빈피킹·치수검증 추론 연동"이 신규 인력 담당**으로 되어 있다 — 우리 방침은
*"태민님 = 빈피킹 개발 집중"*(7/13)이므로 **분담 확인 필요**(문서엔 "공동수행"으로 명기).

상세: `memory/project_agreement_change_0815.md` · `memory/project_ceo_robot_trigger_0818.md`

---

## ⭐⭐⭐ 빈피킹 방향 재정립 — 병목은 종류 식별이고, 전제부터 검증해야 한다 (2026-08-19)

> 태민님 = *"여태 개발한 것 + KAIST에서 얻은 것 + 헤쳐나갈 과제를 통합적으로 생각해
> 최적 방향을 도출하자"*. 아래는 **오늘까지의 실측** 위에서 낸 결론이다.

### 병목의 위치 (8/19 90장)

| 지표 | 8/5 | **8/19** | 판정 |
|---|---|---|---|
| F1 (c1 실운영) | 0.4070 | **0.5445** | — |
| 위치 precision | 0.971 | **0.975** | 🟢 사실상 완성 |
| 위치 recall | — | **0.816** | 🟡 **18% 놓침** |
| 종류 정답률 | 48.5% | **61.3%** | 🔴 **병목** |

⇒ 찾은 것의 97.5%는 실제 부품 위인데 **그중 61%만 이름을 맞추고 18%는 아예 못 찾는다.**

### 🚨 재학습 효과가 +0.03뿐이다 (오늘의 최대 발견)

| 모델 | c1 | c2 | c3 헛검출 | 판정 |
|---|---|---|---|---|
| BASE(T100) | 0.4070 | 0.0814 | 96 | — |
| A lr1e-4 | 0.3537 | 0.0690 | 25 | 🔴 과적합 |
| B lr5e-5 | 0.4588 | **0.0659** | 55 | 🟡 c1만(우연 의심) |
| **C T100서 재fine-tune** | 0.4379 | **0.0864** | **23** | 🟢 **c1·c2 동반** |
| ep10 | **0.5057** | 0.0565 | 65 | 🟡 c1 최고나 c2 하락 |

⭐⭐ **c1만 보면 B가 최고인데 c2를 함께 보니 C가 유일한 진짜 개선**이었다
(c1은 10장·TP 35 전후라 4건 차이가 우연일 수 있다). **표본을 늘리자는 판단이 결론을 바꿨다.**

🚨 **데이터를 13배(7→90장) 늘려서 얻은 재학습 효과가 +0.03**
⇒ **"실측을 더 모으면 해결된다"는 경로가 약하다.**

### ⭐⭐ 태민님 질문이 옳았다 — 재촬영 판단 정정

8/14에 *"이렇게 될 것 같은데 굳이 사진을 또 찍는 이유가 있냐"*는 질문에
나는 *"7장(8%)뿐이라 실패한 것"*이라 답했다. **결과가 질문 쪽이 맞았다.**

🚨 **7/8에 KAIST가 이미 "lr·epoch·해상도·threshold·병합·loss·aug 전부 천장,
병목은 학습이 아니라 센서 물리"라고 결론냈는데, 알면서도 데이터 추가를 1순위로 밀었다.**

⭐ 정확히 나누면 **"재촬영은 유효했고 재학습이 헛다리였다"** —
🟢 F1 +0.1375(**촬영 조건 개선분**) · 배경재 판정법 · 라벨 630개 · 혼동 구조 규명
🔴 **재학습 효과 +0.03**
📌 답했어야 할 것 = *"재학습 기대치는 낮다. 다만 **촬영 조건이 학습 분포와 맞는지 검증**하는
목적으로는 필요하다."* — **목적을 잘못 잡았다.**

### 🚨 "F1을 얼마까지 올려야 하나"에 답이 없다

8/7 실측 = **종류 48.5%에도 파지 가능률 83.8%**, 안전여유 +10mm면 **92.6%**.
지금 종류가 61.3%니 더 높을 것이다.
⇒ ⭐⭐ **"인식 F1"과 "집을 수 있는가"는 다른 숫자**이고 성과 지표는 후자에 가깝다.
게다가 8/5로 파지가 범위 밖이라 **"결과를 웹에 전달하는 것까지" 닫는 게 산출물에 직결**된다.

### 🎯 ①번 실행 결과 — 파지 가능률 87.4% (전제는 반만 맞았다)

| 지표 | 값 |
|---|---|
| 종류 정답률 | 60.9% |
| ⭐**파지 가능률** | **87.4%** (무해 418 / 헐거움 31 / 🔴치명 65 / 514쌍) |

⇒ ⭐ **"종류를 틀린다"가 곧 "못 집는다"가 아니다**(+26.5%p). 📌**보고는 두 숫자 병기.**

🚨 **92% 판정선은 못 넘었다** — 안전여유 +10mm 90.1% / +15mm 91.6% /
**+20mm 96.3%지만 헐거움이 31→450건(514쌍의 88%)으로 폭증**해 미끄러짐 위험.
8/7에도 같은 폭증을 보고 +10mm를 최적점으로 잡았다 ⇒ **실용 구간 90~91.6%.**

⭐⭐ **그런데 치명 65건이 몇 쌍에 몰려 있다** — `13_variant→16_cam_f_bracket` 8 ·
`bracket_sen_1→01_sol_block_a` 8 · `brkt_switch→15_roller_bracket`/`11_sw_block` 5+5 ·
`03_sol_block_front→01_sol_block_a` 5 ⇒ **상위 5쌍이 31건 = 절반.**
(8/7에도 `bracket_sen_1` 하나가 치명 10건 중 5건이었다 — 벌림 63mm로 최대급)

⇒ 🎯 **전면적 인식 개선이 아니라 몇 쌍 겨냥으로 풀릴 수 있다.**
**다음 = 치명 65건을 "왜 부족한가"로 분해**(DB 값 문제 vs 진짜 형상 혼동)
→ 이것이 **RGB 융합(1~2주) 진입 여부를 가른다.**

⚠️ 전제 = DB `gripper_width_mm`이 *"STL bbox 추정치, 티칭 후 실측 교정 필요"*.
절대값이 바뀌면 치명 건수도 바뀐다(단 도구는 **방향만** 판정하므로 결론은 유지).

### 재학습 6판 — C만 유효, 그리고 c1↔c2 역상관

| 모델 | c1 | c2 | c3 헛검출 |
|---|---|---|---|
| BASE(T100) | 0.4070 | 0.0814 | 96 |
| A lr1e-4 | 0.3537 | 0.0690 | 25 |
| B lr5e-5 | 0.4588 | 0.0659 | 55 |
| **C T100서 재fine-tune** | 0.4379 | **0.0864** | **23** 🟢 |
| ep10 / ep20 / ep30 | **0.5057** / 0.4444 / 0.4824 | 0.0565 / 0.0442 / 0.0491 | 65 / 63 / 53 |

⭐⭐ **c1이 오를수록 c2가 떨어지는 역상관** ⇒ 🚨 **"c1에 맞추는 것"과 "일반화"가
다른 방향**일 수 있다. **배포 = 보류**(C가 유일 통과하나 +0.031, c1 10장 표본).
**기본값 = T100 유지.**

🚨🚨 **[2026-08-21 정정] 위 표의 c2 값은 낡은 평가기로 측정된 것이다.**
A100 평가기가 **7/6자**로 **8/19 동치 처리(`14_13`→`13_variant`)가 빠져 있었고**,
동기화 후 재측정하니 **c2 기준선이 0.0985 → 0.1281**로 올랐다
(`14_13`이 c1엔 2건뿐인데 c2엔 9건이라 **영향이 c2에 집중**).
⇒ **7모델 재측정 결과 c2를 넘는 모델이 하나도 없다** — *"C만 동반 상승"*은 성립하지 않는다
(C의 c2 0.1170 < 기준선 0.1281). 📌 **보류 결론은 유지되나 근거가 교체된다**
= *"표본이 부족해서"*가 아니라 **"c2가 기준선에 못 미쳐서"**(더 단단한 사유).
⭐⭐ **8/14 교훈 재적용** = *"결론이 같다"와 "근거가 유효하다"는 다른 문제다.*
상세 = `memory/project_evaluator_version_drift_0821.md`

⭐ **판정 기준 확립 = c1 하나로 판단하지 않는다.** 조건 3개를 **합치지 말고 따로**
(성격이 다르다). c3는 기준선이 0이라 F1 대신 **FP 감소**로 본다.
🐛 `analyze_confusion_impact.py`의 `--cond` 기본값이 `c1`이라 **8/18 데이터는 `--cond ""`**.

### 📋 ToDo 우선순위 (기한 없음 — 최대한 빨리)

| 순위 | 할 일 | 근거 |
|---|---|---|
| ✅**1** | ~~파지 가능률 재측정~~ **8/20 완료** | **95.5%**(올바른 필드 `grip_span_flat_mm` + 안전여유 +10mm) ⇒ **92% 돌파**. 8/19의 87.4%는 **틀린 필드**(`gripper_width_mm`, 방향 혼재)로 잰 값이었다 |
| ✅**1'** | ~~안전여유 반영 + 판정 필드 교체~~ **8/20 완료** | **DB `gripper_width_mm` = `grip_span_flat_mm`**(29종·옛 값은 `_legacy_0806`으로 보존) + **`GRASP_SAFETY_MARGIN_MM=10.0`을 `grasp_plan.py`에 신설**. 🚨**여유를 DB에 박으면 동어반복**(used·need가 같이 올라 84.4%) ⇒ **DB=물리량 / 여유=런타임**으로 분리. 검증 = 여유 0→84.4% · 여유 10→**95.5%** 두 지점 일치 · pickable 최대 벌림 **79.0mm ≤ 85mm** · **회귀 175/175** |
| ✅**2** | ~~recall 18% 임계값 스윕~~ **8/20 완료** | **score_thresh 0.45→0.20** = F1 0.5445→**0.5838**(+0.0393, 학습 불필요). 🚨판정 근거는 F1이 아니라 **집을 수 있는 부품 491→577개(+86)** = GT 대비 77.9%→**91.6%**(치명은 +9뿐, **9.6:1**). ⚠️파지 *비율*은 95.5→94.7로 내려가나 **분모가 커진 탓**. 📌recall은 0.10에서 포화 ⇒ **"더 낮추면 된다"는 경로는 닫힘** |
| ✅**3** | ~~재학습 배포 판정~~ **8/20 재판정 → 🚨8/21 근거 교체 · 보류 유지** | thr 0.20에서 7모델×4조건 재평가 — **C가 여전히 유일 🟢**이고 이득 **2.2배**(c1 +0.031→**+0.068** · c3 FP −98) ⇒ 보류 사유는 약해졌으나 **c1이 10장(TP 3건 차이)** 이라 **기본값 T100 유지**. ⭐⭐기준이 임계값에 안 흔들림(c1 최고 ep30은 c2 미달 탈락) = *"c1만 보면 안 된다"* 가 옳았다. 🚨**90장은 C의 학습 데이터**라 거기서 잰 "99.7%"는 **무효(train-on-test)** |
| ✅**3'** | ~~KAIST 자산 반영 점검~~ **8/21 완료** | 🚨**"안 썼다"는 틀림** = 조교 코드(`mentoring_new`)+`T100_best.pt`가 **몸통**. 미반영은 **TTA 하나**(그것도 조교 코드가 아니라 7/13 내 구현). 🔴**TTA는 같은 날 측정해 판정났다 → 채택 안 함**(아래 3''''' 행). 🚨*"6000에 torch가 없어 측정 불가"라 한 것이 틀렸다* — **A100 접속법이 memory에 있었다** ⇒ [[never-state-unverified]] |
| ✅**3''** | ~~score_thresh 0.20 운영 반영~~ **8/21 완료** | 🚨🚨**8/20에 평가 스크립트만 고쳤고 운영 추론 기본값은 0.25·IPC 명령은 0.45였다** ⇒ *"측정한 값"과 "도는 값"이 달랐다*. **8/20 교훈 재발**(주석·스크립트만 고치면 코드는 옛 값을 쓴다). ✅3파일 0.25→0.20 + IPC 명령 0.45→0.20(`76bf2bf`). ⭐**IPC 합격 기준도 "9건"→이름·score 대조로 정정**(임계값 내리면 검출이 늘어남) |
| ✅**3'''** | ~~공정 화이트리스트 신설~~ **8/21 완료** | 태민님 *"27종인데 지금 21종이니 모델을 수정해야 하지 않나"* → ⭐**모델 재학습이 아니라 후처리로 푼다**(🔴클래스가 ckpt `cad_codebook`에 박혀 재학습이고 **되돌릴 수 없다**·+0.03 전례·**8/14 A판정이라 손해 없음**). 실측=**GT 630에 제외종 0건**·예측 **22건(4.2%) 전부 FP**·**치명 C 23→17건(26%↓)**·**F1 0.5838→0.5998**(A100 정식 측정, thr 0.20). 🚨**recall은 안 오름**(TP는 라벨 일치 조건 ⇒ **precision 전용**) ⇒ *"정답으로 돌아온다"가 아니라 "오답이 사라진다"*. ⭐목록은 **DB `pickability`에서 읽음**(두 곳에 두면 갈라진다) ⇒ **yaml 한 줄로 되돌림**. ✅변이 3종 잡힘·회귀 **204/204**(`65625aa`). ✅**A100 정식 측정 완료**(8/21) — recall은 두 조건 모두 **소수점까지 불변** = precision 전용 설계가 그대로 동작 |
| ✅**3''''** | ~~A100 평가기 버전 어긋남 규명 + 7모델 재측정~~ **8/21 완료** | 🚨🚨**A100 평가기가 7/6자로 8/19 동치 처리가 빠져 있었다** ⇒ **8/20 재판정 전체가 그 값**. 동기화 후 **c2 기준선 0.0985→0.1281**로 올라 *"C만 c1·c2 동반 상승"* 근거가 무너짐 — **7모델 재측정 결과 c2를 넘는 모델이 하나도 없다**(C의 0.1170 < 0.1281). 📌**보류 결론 유지·근거 교체**(*"표본 부족"*→**"c2 기준선 미달"** = 더 단단함). ⭐발견=**md5(데이터·라벨 동일)→플래그(동일)→코드**에서 갈림 ⇒ **"같은 조건"에 코드 버전이 들어간다**. ✅방어=**평가기가 `evaluator_sha256`·동치표를 결과에 남김**(`28eb378`, 6000·A100 해시 일치 확인). 🚨**내가 확인 없이 두 번 단정해 틀림** → [[never-state-unverified]] |
| 🔴**3'''''** | ~~TTA 측정~~ **8/21 완료 · 채택 안 함** | **3scale F1 0.5838→0.5134(−0.070)·8배 느림** / 5scale −0.110·15배. ⭐⭐**같은 TTA가 KAIST test102엔 +0.007 도움**이었는데 🚨**FP 거동이 정반대**(KAIST는 TP도 함께 늘어 577→632 / 우리는 **TP가 줄고**(371→345) FP만 늘어 **recall까지 0.589→0.548**) ⇒ *"커버리지를 넓힌다"는 TTA 전제가 cross-session에선 성립 안 함*. 📌**KTR 3초는 볼 필요 없었다**(F1이 떨어져 이미 탈락). 🚨곁가지=**TTA 평가기에도 동치 처리 누락** 발견·수정 |
| ✅**4** | ~~web-api 기동~~ **8/21 완료** | **7/10부터 42일 돈 프로세스 교체 → 8/13 라우터가 처음 올라왔다.** ✅binpick 경로 5개 등록 · ⭐**실왕복 HTTP 201**(A100 E2E 산출물→6000) · 조회로 끝까지 확인(검출 7건·게이트 `in_distribution` 3.13%·`trusted true`) · ✅**운영 데이터 전부 보존**(⭐`notification_events` +8은 **기동 시 프린터 OFFLINE 기록**으로 7/31·7/16·7/10과 같은 정상 패턴). 🐛내 실수=게이트를 **최상위 키로 추측해 읽어** "설계원칙 깨짐"으로 오판(실제 `scene_gate` 중첩에 정상) → [[never-state-unverified]] |
| ✅**4'** | ~~E2E 러너 신설~~ **8/21 완료** | 🚨🚨**태민님 지적 = "공장에 적용 가능한지도, 어떻게 연결하는지도 하나도 없다"** ⇒ **코드로 확인해보니 맞았다**(조각은 각각 검증됐는데 **잇는 것이 0건**, `main_pipeline.py`는 4월 FPFH 시절이라 `depth_track` **grep 0**). ✅`run_binpick_e2e.py` 신설 = **추론→6요소→게이트→웹을 한 명령**. ⭐**검증된 플래그 6개를 코드에 못박음**(8/14에 `--real_uint16_max_depth_m` 누락으로 **검출이 늘면서 좌우가 뒤바뀌는데 무경고**였다). ✅A100 **2/2 성공**. 🚨**한계=장당 6.7~7.5초로 KTR 3초 초과** — ⭐**추론이 느린 게 아니라**(웜 2.45초) **매번 모델을 다시 로드**하는 탓 ⇒ **상주 프로세스가 해법** |
| 🥇**4''** | 🥇🥇 **RGB 융합 D단계** — 📅**8/26(화) 재택** | 🚨🚨**태민님 8/21 "가장 중요한 말"** = *"**Depth·RGB 무조건 연동** + 카메라를 **로봇암에** + 로봇좌표로 **그리퍼가 집게**"* ⇒ **이것이 궁극 목표**다. 🚨**내가 이걸 반복해서 "범위 밖"으로 밀었다**(8/5를 근거로 hand-eye·파지 후순위, 8/20엔 RGB까지 하향). ⭐⭐**재는 자를 잘못 골랐다** — 내 자=인식 F1 효율(0.8%) / 🥇태민님 자=**집을 수 있는가 ⇒ RGB는 효율이 아니라 전제**. 📌**"사업 범위"와 "궁극 목표"는 다른 축**. ✅**선행 A~C 완료**(렌즈·extrinsic·정합=비용 기지불) · 🔴**D는 `rgb` grep 0건**. ⭐**90장에 RGB 동봉**이라 새 촬영 불필요 · 🚨**train-on-test 금지**(90장은 C의 학습 데이터) → [[binpicking-ultimate-goal-0821]] |
| ✅**0** | ~~새 시험지 F1 측정~~ **8/25 완료** | 🎉**세션 B 30장 = F1 0.6404**(8/18 90장 0.5998 대비 **+0.0406**) · P 0.6633 · R 0.6190 · 화이트리스트 +0.0124(**recall 불변** = precision 전용 설계 그대로). ⭐**유효한 이유** = T100은 **7/7자**라 8/24 촬영보다 7주 앞서 학습 종료(물리적 학습 불가) + 세션 B는 **카메라 재설치·그룹 순서 반전**(진짜 cross-session). ✅`evaluator_sha256 ae7c3602e9ba` 6000과 동일 |
| 🔄**0'** | 🥇🥇 **학습 3종 — 8/25 착수** | 🚨🚨**내가 먼저 틀렸다** = *"학습은 닫힌 경로"* 라 했는데 **촬영을 2회차로 나눈 설계 자체가 재학습 전제**였다(태민님 지적). 🥇**특히 "Depth 재학습"을 놓친 것이 컸다** = RGB만 학습하면 **"RGB 덕인가 데이터 60장 덕인가"를 못 가른다** ⇒ **같은 데이터로 depth-only를 돌려야 RGB 몫이 분리**된다. **①세션B 측정 ✅ / ②RGB 융합 🔄 / ③Depth 재학습 🔄실행중**(`D0824_depth_lr1e4_ep80`·PID 1654753·8/19 C 레시피·세션A 60장만·**약 2시간**·nohup이라 세션 무관). ✅**D-2 정합 90장 매핑률 100%**(`src/acquisition/rgb_to_depth_align.py` 신설 — ⭐**depth 격자로 색을 끌어온다**, 반대로 하면 라벨을 다시 만들어야 한다). 🔄**D-3 = `zvrgb` 5채널 모드 추가**(기존 모드 불변 — T100 재현성). ✅**색 문제 진단 완료 = 학습을 막지 않는다** — ⭐⭐**배경은 중립(B−R −7.3)인데 부품만 파랗다(+27~33)** ⇒ 전역 화이트밸런스가 아니라 **부품 도료/주변광** 탓. 8/24에 고친 **R/B 뒤바뀜(틀린 색)과 다르다** = 지금은 **일관되게 찍힌 실제 색**이고 세션 A·B가 같은 설정이라 cross-session이 안 오염된다. ⚠️단 픽셀편차 15~19는 노이즈라 **약한 블러 고려**. 🚨**내가 두 번 잘못 판정**(*"부품이 청회색"*→반증 / *"정합이 틀어뜨렸다"*→원본 부품 표면이 원인) ⇒ ⭐**전체 평균은 밝은 배경과 어두운 부품이 상쇄돼 중립으로 보인다 — 영역을 나눠 재야 한다**. 🚨🚨**대조 설계 정정**(태민님 *"제일 좋은 결과로 RGB?"*) = **Depth N개 중 최고 vs RGB 1개는 불공정**(우연이 한쪽에만 섞임·8/19 전례) ⇒ ✅**같은 레시피 1개로 양쪽**(차이는 입력 채널뿐), HP 튜닝은 그 다음(7/8에 16조합 소진). 📋**ToDo 8건은 CLAUDE.local.md 하단** |
| 🥈5 | IPC 마무리(pylon·서비스) | ⚠️여기서만 OS 판단이 뒤집힐 수 있다. 🆕**8/25에 원격분 준비 완료**(아래 5') |
| ✅**5'** | ~~금요일 도구 일체 제작·검증~~ **8/25 완료** | 🎯**"명령 카드"를 "검증된 파이프라인"으로**. ⭐**대조표를 실제로 만들었다**(`/data/jtm/e2e_reference_0825/` — 이전 카드엔 *"대조하라"* 만 있고 대조 대상이 없었다). 도구 5개 = 좌표변환(**8/8**·거부 5건 포함) · 파지시퀀스(**24/24**) · E2E대조(**8/14 사고 재현해 잡음**) · 환경지문 · 라벨검사기(**변이 5종 잡음**). 🔬**실측 정정 4건** = ①IPC 의존성 3개→**5개**(러너가 `subprocess`로 추론기를 불러 **러너만 보면 안 나온다** ⇒ AST로 13개 파일 재귀추적) ②🚨**`T100_best.pt`가 git에 없었다**(md5 `afcf7351…`) ③`checkRunnableMotion`은 **startPose 필수**(`ko:38`) ④`depth_preprocess` 경로. ⚠️**`.gitignore`에 `.pt` 규칙이 없어 70MB 가중치가 한솔 미러로 나갈 상황**이었다(추가). 🚨**전부 합성·시뮬 검증 — 실물 0회**. 🔴**IPC 실작업은 내가 못 한다**(6000에 Tailscale 없음) ⇒ ⭐**내가 명령·판정기준 / 태민님이 RDP 실행**(8/14 방식) |
| ⏸️6 | **RGB 융합 D단계** — **응급 진입만 취소, 항목은 유지(8/20 정정)** | 조건이 *"1번이 92% 미만일 때만"*이었는데 **95.5%로 미성립** ⇒ **"파지 실패를 막으려고 지금 1~2주"라는 근거만 소멸**. 🚨**"RGB가 불필요"가 아니다** — Blaze-112에 **RGB 센서가 동봉**돼 있고 7/31 오버레이 정합까지 통과(extrinsic 확증)라 **하드웨어·캘리브 비용은 이미 지불됨**(🚨**[8/21 정정] 단 이것은 *하드웨어*가 동봉이라는 뜻이고 **8/18 90장에 RGB 파일이 있다는 뜻이 아니다** — 실측 RGB는 **0건**이라 **동시 촬영이 선행**이다). ⚠️단 KAIST 경고 유지=**표면패턴(유형B)에만 RGB가 결정적**이고 C 23건은 유형이 섞여 있다 ✅**선행 분석 8/20 완료** = C 23건 중 **19건이 "크기가 명백히 다른 쌍"**(부피비 0.06~0.76) ⇒ 크기 판단 실패지 표면패턴이 아니고 **RGB는 크기를 주지 않는다**. 유형B 후보는 **4건뿐**(514쌍의 0.8%) ⇒ 🔻**우선순위 추가 하향**(8/5 *"실측 > RGB"* 판단의 독립 재확인). ⚠️단 치수 기반 분석이라 *"우선순위가 낮다"*까지만 지지(8/19 bbox 실수 경계). 💡**살아있는 근거는 산출물 ③·④** — RGB가 본령이므로 **⑦치수검사와 묶어서** 볼 것. 🆕**8/25 태민님 = "B는 나중에 고려"로 명시 보류** ⇒ ⭐**재개 조건 3개 중 하나가 서면 올린다** = ①**9/5 판정 후 파지가 되는데 종류 오인이 실제로 걸릴 때** ②**치수검사(산출물④) 착수 시**(RGB가 본령이라 같이 하면 비용이 나뉜다) ③**남은 치명 17건의 유형 재분해에서 표면패턴 비중이 올라갈 때**. 📦**데이터는 이미 있다**(8/24 세션 A 60장 학습 / B 30장 시험지) ⇒ **착수 시 촬영 불필요, D-2 정합부터** |
| 🥉7 | **치수검사** | 8/15로 대상 확정돼 착수 가능. 🚨미착수 |
| 🥉8 | **빌드플레이트 랙 설계** | 대표님 8/18 지시. 🚨미착수 |
| 🥉9 | 건조 공정 설계 | 7/16 지시. 8번과 한 몸 |

🔴 **하지 말 것** = 추가 데이터 촬영(+0.03/90장) · 합성 증강(KAIST 천장 0.203) ·
하이퍼파라미터 추가 튜닝(7/8에 16조합 그리드서치 소진, threshold 무영향) ·
🆕**TTA**(8/21 실측 −0.070·8배 느림 ⇒ KAIST에선 +0.007이었으나 **cross-session에서 역전**)

✅ **KAIST 결론 전수 대조(8/21)** = 학습 레시피(csblur·lr1e-4·ep80·batch2·320×576·amp false)가
**배포 모델과 일치**(체크포인트 `args` 직접 확인·cad_ids 27) · `score_mode det` 🟢 ·
입력 320×576 🟢 · 실측 지렛대 🟢(90장) · 천장 항목 "안 할 것"에 명시 🟢 · 자산 4폴더 전부 존재 🟢 ·
⏸️RGB만 의도적 보류 ⇒ ⭐ **"거의 다 반영, 안 된 건 TTA(해로움)·RGB(보류)"**
⚠️ **"전부"라고는 말하지 않는다** — 위가 확인한 범위이고 6주 산출물 전체 항목표는 따로 없다.
🚨 대조하다 **A100 추론 기본값이 0.25로 남은 것**을 찾아 동기화했다(같은 사고를 하루에 두 번).

상세: `memory/project_binpicking_direction_0819.md`

---

## 🧹 문서 구조 — 2026-08-19 정리 (어디를 봐야 하나)

> 8/19에 md 문서를 정리했다. **지운 것보다 "무엇이 대체했고 무엇이 뒤집혔는지"를
> 적어둔 것이 핵심**이다. 되돌림 = `git tag pre-md-cleanup-0819`.

### 현행 문서 위치

| 용도 | 정본 |
|---|---|
| **판단·현황 전체** | `CLAUDE.md`(이 파일) + `CLAUDE.local.md`(주간) + `memory/` |
| 촬영 현장 카드 | `bin_picking/FIELD_CARD_0818.md` |
| 라벨링 | `bin_picking/LABEL_CARD_0819.md` |
| P0 완료 기록(7/28~30) | `bin_picking/docs/P0_0728_0730_완료기록.md` |
| Lite MES 기능정의 | `docs/MES_ERP_ATS_기능정의_0710.md` |
| 발주 리스트 | 🥇**노션** `🛒 7/27 최종 발주 리스트업` |

### ⛔ 아카이브 (이력 보존용 — 실행에 쓰지 말 것)

`docs/archive_track1_202605/`(트랙1·YOLO) · `docs/archive_meetings_202604_05/`(4~5월 회의) ·
`docs/archive_purchase_202607/`(7월 구매 조사) · `docs/archive_202604/`(일회성)
⭐ **각 폴더 README에 "무엇이 대체했나 / 무엇이 뒤집혔나" 표**가 있다.

### 🚨 회사 / KAIST 경계 (태민님 8/19 지시)

| | `kaist_backup/` | `bin_picking/depth_track/` |
|---|---|---|
| 성격 | **동결된 백업**(7/10 시점) | **살아있는 개발** |
| 수정 | ❌ 하지 않는다 | ✅ 계속 바뀐다 |

**회사 개발이 KAIST 산출물 위에서 이어지는 것은 맞지만 폴더는 나눈다.**
수정이 필요하면 `depth_track/`으로 복사해서 거기서 고친다(7/13 편입 방식).
🚨 **KAIST 팀 repo는 PUBLIC**이라 회사 정보가 가면 안 된다.
상세 = `kaist_backup/README_경계.md`

### ⭐⭐⭐ 이 정리에서 얻은 교훈 — 요약 통합은 숫자를 흘린다

KAIST 미팅 4개를 합쳤더니 **원본의 36%로 압축되면서 실험 결과표 3개의 수치가 사라졌다.**
🚨 태민님이 *"정말 삭제한 건 아니지?"*라고 물어 **검증하고서야 발견**했다(40개 토큰 누락 → 복원).
⇒ ⭐ **통합 후 "원본 토큰이 살아있나" 대조를 반드시 할 것.**
⇒ ⭐ 그래서 P0 문서는 **원본을 지우지 않고** 통합본을 인덱스로 뒀다
(**통합본=결론·교훈 / 원본=세부 수치·절차**).

📌 **폐기 표기 원칙**(7/31 교훈) = 오래된 문서를 지우는 대신 헤더에 `⛔ 폐기 + 현행 문서`를
적는다. *"빈피킹=Modbus"라는 폐기 설계를 3개월 뒤 현행으로 오독한 사고*가 실제로 있었다.

상세: `memory/project_md_cleanup_0819.md`

---

## 🎉🎉 재촬영 문턱을 넘었다 — 막고 있던 것은 "빈 바닥이 IR을 반사하는가"였다 (2026-08-18)

> 8/14에 차광용지를 깔아만 두고 못 넘긴 재촬영을 8/18에 통과시켰다.
> **배경재도 조명도 아니고, 빈 바닥의 850nm 반사가 진짜 변수였다.**

### 실측 — 상자를 바꾸니 한 번에 통과

| 조건 | all_valid | BAND(crop 400~600mm) | 판정 |
|---|---|---|---|
| c1 (학습·F1 0.4070) | 5.44% | 9.47% | 기준선 |
| 주황 플라스틱 공구상자 | 76~94% | **20~86%** | 🔴 0/5 |
| **택배 골판지 상자** | **3.71%** | **3.01%** | 🟢 **5/5 통과** |

⭐ 골판지 조건이 c1보다도 **학습 세션 원본(all_valid 3.1%)에 가깝다.** DIST 452mm로 c1 대역(442~476mm)과 일치.

### 왜 플라스틱 빈으로는 원리적으로 안 되나

주황 플라스틱은 850nm를 **반사**해서 빈 바닥이 부품과 함께 400~600mm 대역에 들어온다.
🚨 **부품 높이 범위와 바닥 거리가 서로 겹친다** — 8/18 저녁 실측: **부품 픽셀 스프레드 45mm**
(p5 441 → p50 460 → p95 486mm)인데 **바닥 최빈값 475mm가 그 안에 들어간다**(±15mm에 크롭의 65.9%).
⇒ ⭐⭐ **어떤 임계값을 잡아도 한쪽이 잘린다.**
(🚨 처음에 적었던 *"높이차 6~30mm"*는 **검증 없이 쓴 인상값**이었고, 실측은 그보다 나쁘다.)
카메라를 가까이 가져가면 바닥이 400mm 아래로 빠지지만 **부품도 같이 빠진다**
(실측: DIST 367~372mm에서 BAND 11~13%인데 화면의 부품은 0~2개뿐).
골판지는 **흡수**하므로 바닥만 무효가 되고 **거리와 무관하게** 성립한다.

### ❌ 오늘 수치로 부정된 가설 3개

| 가설 | 결과 |
|---|---|
| ① 차광용지가 850nm를 반사한다 | **무죄.** 용지는 빈 *바깥*에 있었고 원인이 아니었다 → 시트지 재구매 불필요 |
| ② 형광등 때문이다 | **무관.** 소등하니 지표가 악화(92.38→96.50%). 🚨**단 해석이 반대다** = 소등하니 **무효 픽셀이 줄어(3.3~4.4%) 센서가 바닥을 더 잘 본 것**이다(형광등 NIR 노이즈가 사라짐). ⭐**"지표가 나빠진 것 ≠ 센서가 나빠진 것"** ⇒ *"커튼 없이 간다"*는 **유지되나 근거가 다르다**(빛이 무해해서가 아니라, 빛을 없애도 진짜 원인인 바닥 반사가 그대로라서) |
| ③ 용지 밖 콘크리트가 화면을 채운다 | 근접 촬영으로도 94.26% ⇒ 배경 면적 문제가 아니었다 |

### ⭐⭐ 교훈 두 개

**1. "눈에 검은 것"과 "850nm를 흡수하는 것"은 다르다.**
차광용지는 눈에 완전히 검은데 **유효율 92.4%**(c3 = F1 0.0000 대역)였다.
🚨 **빈·배경재를 눈으로 고르면 안 된다** — 깔고 찍어서 판정 도구로 수치를 봐야 한다.

**2. BAND 숫자만 보면 안 된다.**
`BAND 5~15%`를 통과 기준으로 삼았더니 **부품이 하나도 없는 화면이 13%로 통과**했다
(대역을 채운 것이 빈 밖 바닥 덩어리였다). ⇒ ⭐ **판정은 "숫자 + 눈" 둘 다** =
**배경이 검고 부품 6~8개가 선명한가**를 먼저 보고, 그 상태에서 BAND 3~10%를 확인한다.
8/5 *"몇 개 찾았나 ≠ 맞게 찾았나"* 의 같은 계열이다.

### 🔬 "검정 배경에 회색 부품이라 인식이 안 되나?" = 색 대비 가설은 부정됐다 (8/18 저녁)

**부정 근거 2개:**
1. 화면은 컬러 영상이 아니라 **depth map**이다 — 픽셀 색 = 거리이고, 표면색은 depth에 관여하지 않는다
   (850nm 왕복 시간으로 잰다).
2. ⭐⭐ **결정적 반증 = 갈색 골판지 위의 회색 부품이 5/5 통과 최고 조건**이다.
   색 대비가 변수라면 여기서도 나빠야 하는데 정반대다.

**크롭 안 depth 히스토그램 — 가장 명확한 증거:**

| 조건 | 425~450 | 450~475 | 475~500 | 크롭 유효율 |
|---|---|---|---|---|
| 주황 공구상자(소등) | 5.7% | **31.1%** | **46.1%** | **98%** 🔴 |
| **택배 골판지** | 1.4% | **4.0%** | 1.2% | **7%** 🟢 |
| **c1 (학습 기준선)** | 2.6% | **6.0%** | — | **9%** 🟢 |

⇒ ⭐ **골판지와 c1의 히스토그램이 사실상 같은 모양**이다(우연이 아니라 재현).

⚠️ **절반만 맞았던 내 설명** — *"부품이 회색으로 묻혀 보이는 건 거리가 같아서"*는,
크롭의 65.9%가 바닥 ±15mm에 몰린 건 사실이나 **depth 컬러맵이 유효 범위 전체에 자동
스케일링되는 것**도 섞여 있다(바닥이 화면을 지배하면 부품 대비가 죽는다).

💡 **단 색이 진짜 변수가 되는 시점** = **RGB 융합을 켜면** 회색 부품 vs 검정 빈의 대비가
실제 변수가 된다 ⇒ ⭐ **운영 빈을 고를 때 IR 흡수와 RGB 대비를 같이 보면 두 번 안 고른다.**

📌 **재현 환경** = `/data/jtm/depth_venv/bin/python` (🚨 시스템 python3에는 numpy가 없다)

### 🚨 촬영 전 체크 — "만든 곳"과 "쓰는 곳"이 다르다 (2026-08-24 실사고)

**촬영기는 6000에서 만들고 실행은 맥에서** 한다(카메라가 맥에 붙는다). 그 사이가 끊기면
**공장에 가서야 스크립트가 없는 것을 안다** — 8/24에 실제로 그래서 두 시간이 밀렸다.

| 확인 | 방법 |
|---|---|
| 맥에 최신이 와 있나 | 🚨 **6000 커밋을 push하지 않으면 pull해도 안 온다** |
| 지금 어느 클론인가 | 🚨 맥 리포는 **중첩**돼 있다 — `git remote -v`에 **origin+personal 둘 다** 있어야 진짜 |
| 급할 때 | **rsync로 파일만 직접 투입** — 단 **자립형 스크립트만**(`grep -E "^\s*(import\|from) "` 로 내부 import 0 확인) |
| 카메라 대역 | `sudo $(which python) bin_picking/tests/setup_camera_net.py --apply` (🚨 `sudo python`은 시스템 파이썬을 잡는다) |
| 어댑터 | 맥북엔 랜포트가 없다 — **USB-C 이더넷 2개**를 꽂아야 포트가 생긴다. 없으면 **열거 0대가 정상** |

⭐ **열거 2대를 확인하고 촬영을 시작한다** — 1대면 ACE2가 안 열려 스크립트가 중단된다(의도된 동작).
📌 **교훈 = "만들었다"가 "쓸 수 있다"가 아니다** — 8/21 *"평가 스크립트만 고쳤고 운영 추론은
옛 값이었다"* 와 같은 계열(**한쪽만 갱신된 것을 전체로 읽었다**).
상세: `memory/reference_mac_repo_clone_0824.md`

### 🚨🚨🚨 Blaze+ACE2 동시 촬영 = 확립된 설정 (2026-08-24, 두 시간 헤매고 얻음)

> **다음 촬영 때 이 표를 먼저 본다.** 아래 4개를 몰라서 두 시간을 썼고 **넷 다 내가 만든 함정**이다.
> 전문·명령: `memory/reference_dual_camera_setup_0824.md`

| 항목 | ✅ 정답 | 🚨 틀리면 |
|---|---|---|
| **대역 분리** | Blaze `.20.10`←포트 **`.20.1`** / ACE2 `.30.20`←포트 **`.30.1`** | 같은 대역이면 **하나만 열린다**(macOS가 트래픽을 한 인터페이스로만 보냄) |
| **호스트 IP** | 🚨 **`.20.1`**(자동 할당값) | `.20.2`로 바꾸면 **`Failed to discover`** |
| **PixelFormat** | 🚨 **`BayerRG8`** | **BGR8은 0/5**(15MB/frame이 대역 초과) |
| **throughput** | 🚨 **30Mbps** | **낮추면 역효과** — 10으로 조이니 0/8, 30으로 올리니 8/8 |

⭐⭐ **ARP·ping·열거가 전부 성공해도 안 열릴 수 있다** — [[gige-camera-network-rule]]의
*"열거는 브로드캐스트, 열기는 유니캐스트"* 가 **인터페이스 레벨에서도 성립**한다.
⭐ **`DeviceLinkThroughputLimit`은 "제한"이지 "할당"이 아니다** — 낮추면 버퍼 언더런이 난다.
⭐ 7/28 캘리브 때 안 걸린 이유 = **그때는 흑백(intensity)** 이었고 **컬러 동시 촬영은 8/24가 처음**.

⚠️ **판정 임계값을 현장에서 완화했다** — `BAND_PX_MIN` 8000→**5000**(8/18 통과분 4%≈7k도
떨어뜨렸다) · `sharp` 50→**15**(실측 24). 🚨**느슨해진 만큼 눈 확인이 더 중요**하다.

### 🚨 5번째 함정 = Bayer 디모자이크 패턴 (색이 틀어진다)

**실물은 회색인데 영상이 주황빛**이었다. 원인은 화이트밸런스가 아니라 **패턴**이다
(`BalanceWhiteAuto`는 이미 `Continuous`였다).
🚨 **Basler `BayerRG8` ≠ OpenCV `COLOR_BayerRG2BGR`** — 기준점이 달라 **R/B가 뒤바뀐다.**

⚠️ **카메라는 `BayerBG8`을 지원 안 한다**(촬영기가 `BGR8`로 넘어가 RGB가 통째로 빠진다)
⇒ ✅ **카메라 `BayerRG8` + OpenCV `COLOR_BayerBG2BGR`**(`blaze_ace2_capture_dual.py:300`).

**판정법 = 무채색 물체로 B/G/R 편차를 잰다**(눈보다 숫자가 빠르다).
실측 = `RG` B219/G237/**R240**(붉은기) · `BG` **B240**/G237/R219(푸른기) · `GR`·`GB` 237/230/237 🟢

⭐⭐ **교훈 두 개**
1. **판정 기준은 목적에서 끌어온다** — 나는 *"일관되면 학습은 된다"* 며 재택으로 미루려 했는데,
   그건 **depth 단독 학습의 기준**이고 **색을 신호로 쓰는 이번 목적엔 안 맞는다.**
   태민님이 *"지금 조정해야 하는 것 아니냐"* 고 되물어 바로잡혔다(틀린 색으로 찍었으면 **재촬영**).
2. 🚨 **스크린샷으로 색을 판정하지 않는다** — 캡처를 거치면 색이 과장된다.
   내가 사진만 보고 *"명확한 파란색"* 이라 단정했으나 **실물을 본 판정은 "회색"** 이었다.
   ⭐ **실물을 보는 사람의 눈 > 캡처를 보는 내 판단.**

🚨 **세션 A와 B의 설정이 같아야 한다** — 다르면 cross-session 평가가 오염되어
**세션을 가른 의미가 사라진다.**

### ✅ 결과 — 90장 확보, 무결성 통과 (RGB 실측 0건이 해소됐다)

세션 A **60장**(c1=A그룹·c2=B·c3=C) / 세션 B **30장**(c4=C·c5=B·c6=A — **재설치 + 순서 반전**).
🚨 **세션 B는 학습에 넣지 않는다.** 백업 = `/data/jtm/dual_capture_0824/`(1.2GB).

**촬영 직후 무결성 검사 = 반드시 한다** — 8/21에 *"RGB 동봉"* 이라 믿었다가 **실측 0건**이었다.
⭐ **"찍었다"와 "쓸 수 있는 게 저장됐다"는 다르다.**

| 검사 | 8/24 결과 |
|---|---|
| depth `.npy` | **(480,848) uint16** = 8/18 학습셋과 **완전 일치** 🟢 |
| RGB `.png` | **(2048,2448,3)** 원본 해상도 |
| 가짜 컬러(회색 3채널 복제) | **0건** 🟢 |
| **색 균형**(부품 픽셀 R/G/B 편차) | **1.8~4.5** 🟢 (`RG` 패턴일 때는 **21**이었다) |

### 🚨🚨 이날 세운 가설 6개가 **전부 틀렸다**

GigE TL 계층 / 같은 서브넷 이중 인터페이스 / 카메라 서브넷 마스크 / **대역폭 부족(→낮춤)** /
패킷 지연 / 화이트밸런스 — **하나도 맞지 않았다.** 진짜 원인은 위 표 5개(호스트 IP·포맷·
throughput·대역 분리·Bayer 패턴)였고 **전부 7/28·8/18 성공 코드에 답이 있었다.**

⭐⭐ **공통 형태 = "확인 안 한 항목 중 그럴싸한 것을 골랐다"** — 8/12 IPC에서 하루 다섯 번
밟고 **교훈까지 적어둔 것을 그대로 반복**했다. 📌 **증상이 같아도 원인은 다르다**
(`Failed to download XML`이 두 번 떴는데 한 번은 호스트 IP, 한 번은 픽셀 포맷).

⭐ **제 몫을 한 안전장치 2개**(같은 날 오전에 만든 것)
① **`--allow-no-rgb`를 명시해야만 depth 단독 진행** ⇒ RGB 없이 60장 찍는 최악을 막았다
② **변이 실험으로 검증해 둔 판정기** ⇒ 임계값 문제를 "카메라 탓"으로 오해하지 않았다
⭐ **크래시 2회에도 데이터가 온전** — 스크립트가 **파일 번호를 이어받는 설계**라 그대로 이어 찍었다
⇒ 📌 **긴 수집 작업은 "중단되어도 이어진다"를 설계에 넣는다.**

🚨 **상위 교훈 = 성공한 전례가 있으면 추측보다 그 코드를 먼저 연다.**
8/24에 *"확인 안 한 항목 중 그럴싸한 것"* 을 **네 번** 골랐는데(TL 계층→서브넷→대역폭→카메라 설정),
정답은 매번 **7/28·8/18 성공 코드를 열면 바로 나오는 것**이었다 → [[never-state-unverified]]

### ✅ 촬영 스크립트는 택배상자용이 아니다 (확인함)

`KEEP_RANGE_MM=(400,600)`·`CENTER_CROP=(1/6,5/6)`의 출처는 **평가 파이프라인**
(`reproduce_f1_0684.sh:54 --depth_keep_range 0.40,0.60`)이다. 상자 규격과 무관한 **모델 쪽 제약**이고,
스크립트는 "상자에 맞춘 것"이 아니라 **"이 모델이 쓸 수 있는 화면인가"를 재는 자**다.

### 📌 운영 빈은 골판지가 아니다 (다음 주 과제)

골판지는 **오늘 촬영을 성사시킨 임시 해법**이다 — 로봇이 긁으면 찢어지고, 젖거나 눌리면 형상이 변해
촬영 조건이 흔들린다. ⭐ **제대로 된 답 = "IR 흡수하는 무광 검정 플라스틱 빈"**
(8/10에 취소했던 방향이 맞았고, 다만 **시트지가 아니라 빈 자체**여야 한다).
🚨 **구매 전 샘플로 `check_background_material.py`를 돌려 고를 것** — 눈으로 고르면 차광용지를 또 산다.

상세: `memory/project_bin_ir_reflectance_0818.md`

---

## ⭐⭐ 대표님 8/18 저녁 = 공정 개편 예고 + Lite MES 범위 확정 + 랙 착수 (2026-08-18)

> 오전에 협약변경 문서로 미결 3건이 풀린 **같은 날 저녁**, 대표님이 공정 레이아웃과
> Lite MES 범위를 함께 전달했다. 첨부 = ①모바일 매니퓰레이터 CAD ②예정 공정 배치도 ③현재 공정 사진

### ⭐⭐⭐ 대표님이 아키텍처 요구사항을 직접 줬다 — 이것이 가장 중요하다

> *"어떻게 공정이 마무리되든 **시스템적으로는 문제없도록 하는게 목적**입니다."*
> *"하나 하나의 공정을 **독립적인 모듈 단위로 개발**하려 합니다. 그렇게하면 **추후 공정 순서가
> 바뀌어도 쉽게 조정이 가능한 모듈형 lite MES**로 개발할 수 있습니다."*

⭐ 이것은 *"레이아웃이 안 정해졌으니 기다려라"*가 **아니라 그 반대다** —
**확정을 기다리지 말고, 확정 결과에 영향받지 않는 구조로 지금 짜라**는 지시다.
⇒ 🎯 내일(8/19) 공정 회의가 어느 쪽으로 나도 **개발이 멈추지 않는 상태**가 목표이고,
그것을 보장하는 것이 우리 설계 책임이다.

📌 **정합** — 8/15 협약변경으로 위탁1 일정이 9~10월로 밀리자 *"배선·구조물이 9월에 오므로
그 전까지 SW는 현 배선 상태로 개발한다"*고 판단한 것과 **같은 논리**다.
이번엔 대표님이 그것을 **아키텍처 원칙으로 명문화**했다.

### 🚨 공정 레이아웃 = 2안 (8/19 회의에서 정리)

> *"기존공정 사진을 **최소한의 수정**으로는 예정공정 사진처럼 바꾸거나, 로봇 사진 처럼
> **모든 장비는 벽으로 붙고** 이와 유사한 로봇(**우리 한화 외팔 로봇암을 장착**)이
> **좌우 이동하면서 작업**하도록 개선할 예정입니다."*

| | A안 = 최소 수정 | B안 = 벽면 정렬 + 이동 로봇 |
|---|---|---|
| 장비 | 예정 배치도대로 재배치 | **전부 벽면 부착**(일렬) |
| 로봇 | 현행 **고정 베이스 2대** | ⭐**한화 외팔 로봇암을 이동 대차에 장착**해 좌우 주행 |

#### 🚨🚨 B안이면 빈피킹 좌표의 전제가 무너진다

8/5 도달 판정의 근거가 이것이었다:
> *"재는 것은 '팔이 뻗는 거리'가 아니라 **고정된 두 점 사이의 거리**다 —
> 팔은 움직이지만 **베이스와 스테이션은 안 움직인다**."*

| 항목 | B안에서 |
|---|---|
| `PICK_CAPTURE_POSE` | 🚨 **베이스가 움직이면 관절각 고정으로 같은 화면이 안 나온다** ⇒ 대차 정지 위치 반복정밀도가 새 변수 |
| hand-eye(범위 밖이나) | 🚨 base frame이 이동 ⇒ **대차 기준 좌표계**가 필요 |
| 촬영 거리 452mm | ⭐ **살아남는다** — 카메라↔빈 상대거리는 정지 후 자세로 유지 가능 |
| **8/18 재촬영 데이터** | ✅ **버려지지 않는다** — 인식 모델은 상대 화면만 본다 |

📌 8/15 협약변경 **위탁1 과업 ⑤ = "재배치 후 로봇 티칭 경로 재설정 및 간섭 검증"**(9~10월)이
이미 이 개편을 예고했다 ⇒ ⭐ **B안은 갑작스러운 게 아니라 위탁1 과업과 한 몸이다.**
⚠️ **미확인** = 이동 대차가 **구매 리스트(2,231만)에 없다** → 사업비인지 사업 후인지 확인 필요.

### ⭐ Lite MES/ERP 범위 = 축소 확정 (7/10 기능정의 갱신 대상)

> *"재무 회계 관련 기능은 이번 개발에서는 **자체 개발 하지 않고 이카운트 같은 외부 ERP와 연동만**
> 할 예정이고 **HR은 개발하지 않습니다.** 이번 개발은 **공정에 집중**해서 진행"*

| 계층 | `docs/MES_ERP_ATS_기능정의_0710.md` | 8/18 확정 |
|---|---|---|
| 재무·회계 | ERP 계층에 포함 | 🔴 **자체개발 제외 → 외부 ERP 연동만** |
| HR | (언급) | 🔴 **개발 안 함** |
| 공정(MES·ATS) | 부분 구현 | 🥇 **집중 대상** |

⭐ **축소가 아니라 초점 확정이다** — 정량목표 5개(리드타임·응답 3초·설비 10대·디지털화·특허)는
**전부 공정 계층 지표**이고 재무·HR은 어느 목표에도 안 걸린다 ⇒ 🎯 **"매 작업 전 자문 ①"로
걸러도 정확히 같은 답이 나온다.** 대표님 지시와 우리 기준이 일치한다.
🚨 **단 "연동만"도 개발이다**(이카운트 API 스펙·인증·동기화 주기·실패 처리).
**회계 로직을 우리가 안 짜는 것**이 이득이고, 조사는 **공정 모듈 다음**이다.

### 🆕 빌드플레이트 적재 랙 설계 = 착수 지시

> *"**빌드플레이트 적재 랙 설계 시작합시다.**"*

**예정 배치도의 근거 위치 3곳** = 좌측 **"이동 바퀴 / 빌드 플레이트 대기랙"** ·
중앙 하단 **"빌드 플레이트 건조공간(⭐선입선출)"**(상단은 세척기 2대) ·
우측 **"이동 바퀴 / 최종 결과물 회수랙"**. 하단 별도 = *"별도 공간에서 세척 후 대기랙 위치로 이동"*

⭐ **설계에 반영할 것 5가지:**
1. **선입선출(FIFO)** — 배치도가 명시 ⇒ 🚨 **소프트웨어 재고 모델도 FIFO 큐**여야 한다
   (슬롯 = 물리 자리 + DB 행. 언제 들어왔는지를 MES가 알아야 FIFO가 성립)
2. **랙이 이동체다**(바퀴) — 고정물 전제로 설계하면 안 된다
3. ⭐⭐ **건조 공간과 겸용** ⇒ **7/16 건조 공정 설계(미착수)와 같은 물건**이고
   **랙 설계 = 건조 공정 설계의 선행 절반**이다. **PET-7015+RTD 발주 보류 사유
   (*"건조 공정 설계 미착수"*)가 여기서 풀릴 수 있다**
4. **로봇 도달** — 8/5 기준 `√(수평²+높이차²) ≤ 900mm`(B안이면 대차 정지 위치 기준)
5. 🚨 **플레이트 실측 선행** — 외형·두께·무게를 재야 슬롯 피치가 나온다.
   ⭐ **TCP payload 1.5kg 제약이 플레이트 무게로 다시 걸린다**(그리퍼+부품 2.19kg 미결과 같은 계열)

### ⭐ 남는 교훈 후보 — "미확정을 기다리는 것"과 "미확정에 안 걸리게 짜는 것"은 다르다

8/5~8/18 두 주간 우리는 **미결 3건을 기다리며 막혀 있었다**. 이번엔 대표님이 먼저
*"어떻게 마무리되든 시스템은 문제없게"*라고 요구했다 ⇒ ⭐ **같은 상황에서 "확인 요청"이 아니라
"불변 부분부터 짜기"가 가능했는지** 돌아볼 값이 있다(8/15에 이미 그렇게 판단한 선례가 있다).

상세: `memory/project_ceo_process_mes_scope_0818.md`

---

## 🎉🎉 본촬영 90장 완료 — 학습 조건을 재현했다 (2026-08-18 오후, 공장)

> 8/14에 차광용지를 깔아만 두고 못 넘긴 재촬영을, 오전 배경재 판정(골판지 5/5)에 이어
> **오후에 90장으로 마무리**했다. 6000 저장·검증 완료.

### 결과 = 학습 c1 범위를 거의 정확히 재현

| 지표 | 오늘 90장 | 학습 c1 기준선 | 판정 |
|---|---|---|---|
| **크롭 BAND** | **4.05~7.55%** | **4.27~7.50%** | 🎯 **거의 완벽히 겹침** |
| 무효율 | 96.0~97.7% | 94.5~95.7% | ✅ (더 깨끗) |
| 유효중앙 거리 | 444~458mm | 442~476mm | ✅ |

**그룹별 BAND 중앙값** = B 4.92% / C 7.55% / A 4.05%
⭐ **세 그룹이 갈리는 것이 좋은 신호다** — 부품 높이 차(C 평균 28.9mm vs A 15.9mm)가 그대로
반영된 것이고 **우연이 아니라 물리로 설명된다** ⇒ 자연스러운 다양성이 들어갔다.

### ⭐⭐ 8/7 실패와 결정적으로 다른 점

| | 8/7 (실패) | 오늘 |
|---|---|---|
| 추가 장수 | **7장**(기존 87장의 8%) | **90장** |
| 결과 | F1 0.4231 → **0.2917**(악화) | — |
| 원인 | 소수 분포 → 일반화 아닌 과적합 | **해소** |

⇒ ⭐ **처음으로 "실측 데이터 추가"라는 지렛대를 제대로 시험할 수 있는 조건**이다.
(8/5 저녁 가설이 8/7에 부정됐지만 그때는 7장이라 시험 자체가 성립하지 않았다)

### 🚨 그룹 ↔ 조건번호가 어긋났다 — 파일명을 믿지 말 것

촬영 중 **스페이스 오조작**으로 조건 번호가 밀렸다(c2가 비고 c3/c4로).
`capture_meta.json` 메모도 **c3가 "A그룹"으로 오기**돼 있었다.

| shot 범위 | 파일 접미사 | **실제 그룹** |
|---|---|---|
| **001~030** | `_c1` | **B** |
| **031~060** | `_c3` | **C** ← 오기 교정 |
| **061~090** | `_c4` | **A** |

✅ 교정 완료 = conditions 재작성 + 각 shot에 **`group`·`group_parts` 필드 신설** +
`group_mapping`에 *"파일명 접미사가 아니라 shot 범위가 정답"* 명기(원본은 `.BACKUP_0818`).
🆕 **`group_labels_0818.json`** = 라벨링 정답지(후보가 21종→7종으로 좁혀져 시간 단축 +
다른 그룹 부품 이름을 달면 잡히는 오류 방어).

⭐ **교훈** = **기계가 매기는 식별자와 사람이 의도한 의미가 어긋날 수 있다.**
어긋난 경위까지 적어야 나중에 *"왜 c2가 없지?"* 로 헤매지 않는다.

### ⭐⭐ `13_variant` ↔ `14_13` = 병합 확정 (태민님 A안)

> 태민님: *"14_13와 13_variant는 내가 봐도 정말 라벨링을 못할 거 같아"* → **실측으로 확증**

| | 13_variant | 14_13 | 차이 |
|---|---|---|---|
| 치수 | **48.87** × 42.24 × 6.0 | **45.87** × 42.24 × 6.0 | 긴변 **3.0mm**뿐 |
| 파지 span / 눕힌높이 | 42.24 / 6.0 | 42.24 / 6.0 | **완전 동일** |

🚨🚨 **결정적 계산** = 450mm에서 **1픽셀 = 1.45mm**(fx 309.3) ⇒ **3mm = 2.1픽셀**.
depth 노이즈가 픽셀 단위로 흔들리고 부품이 조금만 기울면 투영 길이가 3mm 이상 변한다
⇒ ⭐ **"라벨링이 어렵다"가 아니라 "물리적으로 구별 불가"다.**
📌 DB 설명에 이미 있었다: `14_13` = *"얇은 판형, **13_variant 유사**"*.

**병합이 안전한 근거** = 파지 파라미터가 **완전히 동일**하므로 어느 쪽으로 인식해도
**그리퍼 동작이 같다**. 8/7 원칙 *"`label`은 벌림만 결정 ⇒ 틀린다 ≠ 못 집는다"* 그대로이고
8/10 **벌림 묶기**(19가지→15점, 파지 손실 0)와 **같은 논리**다.

**라벨링 규칙** = B그룹 장(shot_001~030)에서 **둘 다 `13_variant`로 라벨**
⇒ 🚨 **그 장에 `13_variant`가 2개**이고 **`14_13` 라벨은 존재하지 않는다**
(안 적으면 *"왜 2개지?"* 로 헤매고 평가가 `14_13`을 FN으로 집계한다).

🚨 **리스크 = 7/9에 병합이 F1을 떨어뜨린 전례가 있다** ⇒ **F1 측정 후 재검토 대상**으로 남긴다.

| 같은 계열 다른 쌍 | 판정 | 근거 |
|---|---|---|
| `r_guide_a_l`·`r_guide_a_r`·`08_r_guide_a` | 🟢 **구별 가능** | 길이 **271/163/117.8mm** = 70~100픽셀 차 |
| `roll_cover_left`↔`right` | ⚠️ **경계선**(병합 안 함) | XY 동일 + 두께 6mm(약 4px) → 라벨링하며 확인 |

### 🔵 근거리 픽셀 18장 = 무해 (놀랐다가 확인함)

전체 프레임 유효중앙이 129~180mm로 나온 장이 18개 있었다. 실측하니 `shot_023`의
100~130mm 픽셀 16,872개가 **화면 우측 끝 100px 띠**에 몰려 있고 ⭐ **평가 크롭 안에는 0개**다
⇒ 학습·평가에 영향 없다(카메라 우측 시야의 테이블/케이블).
⭐ **교훈 = 판정 기준을 지표 정의와 맞춰야 한다** — 촬영 화면의 `DIST med`는 **크롭 기준**이라
정확했고, 내가 **전체 프레임으로 계산해서 놀란 것**이다.

### ⚠️ 남은 리스크 2개

1. **A그룹만 거리가 멀다** — 중앙 **458mm**(B 444 / C 452), **476 초과 10장**(최대 501).
   대역 안이고 BAND는 합격이라 쓰기로 했으나 ⭐ **F1에서 A그룹이 유독 나쁘면 1순위 후보**다.
2. **병합의 F1 영향** — 위 7/9 전례.

### 🐛 촬영 중 막힌 것 (다음에 안 헤매게)

| 증상 | 원인 | 해결 |
|---|---|---|
| `❌ Blaze를 찾지 못했습니다` | 🐛 **스크립트 버그** = `blaze_capture_crosssession.py:123`이 `tl.CreateTl("BaslerGigE")` **반환값을 버려서** GigE 계층이 아닌 TlFactory로 열거한다(진단 스크립트는 `gige.EnumerateAllDevices()`를 써서 성공) | ⭐ **`--ip 192.168.20.10` 명시**로 우회. 🔴 **코드 수정은 미완** |
| 메모 재입력 안 나옴 | 재실행 시 **기존 조건을 이어씀**(292~296행) | 스페이스로 조건 변경 |

🚨 **내 판단 착오** = `unset BASLER_BLAZE_IP`를 먼저 시켰는데 **환경변수가 있었으면 `--ip` 없이도
됐을 것**이다(7/29 메모리의 *"환경변수가 기본값을 덮어쓴다"* 함정을 **반대 방향으로** 적용).
결과적으로 `--ip` 명시가 더 확실하지만 **불필요한 단계를 하나 만들었다.**

### 📂 저장 위치 (6000)

```
/data/jtm/synth_out/blaze_capture_0818/     총 77MB
  ├ shot_001~090_c{1,3,4}.npy   90장   ← 학습·평가용 원본
  ├ shot_001~090_c{1,3,4}.png   90장   ← 라벨링용
  ├ capture_meta.json                  ← 그룹 교정 완료
  ├ capture_meta.json.BACKUP_0818      ← 교정 전 원본
  └ group_labels_0818.json             ← 정답지 + label_rules(병합 규칙)
```

상세: `memory/project_capture_done_0818.md`

---

## 📸 본촬영 최종안 — 그룹은 혼동쌍으로 짠다 (2026-08-18)

> 태민님 *"어떤 환경에서 어떻게 찍는 게 최적일지 꼼꼼하게"* + *"크기도 고려한 건가"*
> → **학습 데이터와 부품 치수를 직접 재서** 조건을 정했다. 추측이 아니다.

### 🚨 "21종을 한 빈에 쏟아붓기"는 하면 안 된다

**학습 100장 실측**(`real_capture100/labelme_json/`, n=100):

| 항목 | 값 |
|---|---|
| 장당 인스턴스 수 | 최소 6 / **중앙 9** / 최대 9 |
| 장당 **종류** 수 | 최소 6 / **중앙 9** / 최대 9 |
| ⇒ 둘이 같다 | **1종당 1개씩만 넣었다** |

⇒ 🚨 계획서의 *"21종 3라운드"*를 **"빈에 21종을 다 넣는다"**로 실행하면 **학습에 없는 화면**이 된다.
**21종 = 3그룹 × 7종**으로 쪼개고, 한 그룹에서는 **그 7종만·1종 1개·겹침 최소**로 찍는다.

⭐ **이것이 F1 0.4070의 유력 후보이기도 하다** — c1(7/31)은 빈에 부품을 여러 개 겹쳐 담았고
학습은 1종 1개 배치였다. **분포 차이가 배경만이 아닐 수 있다.**

### ⭐⭐ 그룹 설계 원칙 = 혼동하는 부품은 같은 화면에 있어야 한다

모델이 A와 B를 구별하려면 **둘을 동시에 보며 "이건 A, 저건 B"를 배워야** 한다.
다른 그룹으로 갈라놓으면 각각은 외우지만 **같이 나타났을 때 못 가리고**,
실제 빈에는 둘이 같이 들어온다. ⇒ **임의로 7개씩 자르면 종류 식별 48.5%를 개선할 기회를 잃는다.**

**혼동쌍 12개 전부 같은 그룹(분리 0) + span 합 균등**을 동시에 만족하는 조합:

| 그룹 | 7종 | span합 | 높이 | 특징 |
|---|---|---|---|---|
| **A** 🥇 | `bracket_sen_1` · `guide_paper_roll_cover_left` · `guide_paper_roll_cover_right` · `01_sol_block_a` · `02_sol_block_b` · `18_button_function_niro` · `15_roller_bracket` | 300.6 | 9.0~28.0 | ⭐**혼동쌍 5개 집중**. `roll_cover_l↔r`은 XY 동일(59×48)로 **2D 원리적 구별 불가**, `bracket_sen_1`은 3쌍의 허브 |
| **B** | `07_guide_paper_l` · `09_guide_paper_r` · `13_variant` · `14_13` · `13_x2_bcf8ccb4` · `16_cam_f_bracket` · `bracket_sensor1` | 289.7 | 6.0~25.5 | `13_variant↔14_13` 쌍둥이(42.2/6.0 동일) · `07_l↔09_r`(19mm 차인데 틀림) |
| **C** | `06_sol_block_back` · `03_sol_block_front` · `plate_e` · `r_guide_a_l` · `r_guide_a_r` · `08_r_guide_a` · `brkt_switch` | 293.3 | 20.0~34.5 | ⭐**삼둥이 3종** · 높이 편차 최소 |

**촬영 순서 = B → C → A** (A를 손에 익은 상태에서 / ⚠️시간 부족하면 **C를 줄이고 A를 사수**)

### 🚨 크기를 넣어 발견한 것 — `08_r_guide_a`는 삼둥이였다

| 부품 | span | 눕힌 높이 | 길이 |
|---|---|---|---|
| `08_r_guide_a` | 40.0 | 34.5 | **271mm** |
| `r_guide_a_l` | 40.0 | 34.5 | **163mm** |
| `r_guide_a_r` | 40.0 | 34.5 | **117.8mm** |

⇒ ⭐⭐ **span·높이가 완전히 동일하고 길이만 다르다** = 2종 쌍이 아니라 **3종이 서로 혼동하는 삼둥이**다.
🚨 **처음에 `08_r_guide_a`를 단독종으로 취급한 내 판단이 틀렸다** — 크기를 안 봤기 때문이다.

### ✅ 21종 재검증 (그리퍼 사양 전수 재판정)

**JEGB-4285P-3MA** · 스트로크 **85mm** · 개폐 **0~85mm** · 조우 진입 높이 하한 **5.0mm**

```
DB 29종 − DB전용 2종(10_guide_paper_roll_l, top_inner_sheet004) = 학습 27종
학습 27종 − 제외 6종 = 촬영 대상 21종  ✅
  ├ pickable      19종
  └ needs_review   2종 (13_variant, 14_13 — 촬영·학습 O / 좌표 전송 X)
그룹 A+B+C = 21종 · 중복 0 · 촬영 대상과 완전 일치 ✅
```

⭐ **전수 재판정에서 DB 태그와 계산이 5종 불일치했으나 전부 "문서화된 사람의 결정"이다:**

| 부품 | 계산 | 결정 | 사유 |
|---|---|---|---|
| `main_body` | 🟢 h 6.0(+1.0) | 🔴 제외 | 태민님 8/6 — 경계선이나 제외 확정 |
| `11_sw_block` | 🟢 span 7.1 | 🔴 제외 | 태민님 8/14 실물 — "너무 작다" |
| `17_mks_holder` | 🟢 span 82.5(**여유 2.5mm**) | 🔴 제외 | 태민님 8/14 — "크고 높이가 없다" |
| `13_variant`·`14_13` | 🟢 h 6.0(+1.0) | 🟡 유지 | "집을 만해 보인다" |

⇒ ⭐ **계산이 못 가르는 자리다**(조우 실물 두께가 제조사 비공개).
✅ **`MANUAL_OVERRIDES`에 사유까지 박혀 있어 스크립트를 다시 돌려도 조용히 안 뒤집힌다**
— 8/14에 세운 *"안 박으면 되돌아간다"* 원칙이 실제로 작동함을 확인했다.

### 📸 촬영 조건 + 장수

| 항목 | 값 | 근거 |
|---|---|---|
| **거리** | **450~460mm** | 학습 유효중앙 **455mm**(p25 443 / p75 465) |
| **빈** | **어제 5/5 통과한 그 골판지 상자** | 재질이 바뀌면 판정이 무효 |
| **조명** | **형광등 ON** | 🚨소등하면 무효 픽셀이 줄어 **바닥이 더 잘 보인다**(역효과) |
| **차광용지** | **안 쓴다** | 8/18 무죄 판정 |
| **장당** | **7종·1종 1개·안 겹치게** | 학습 중앙 9종 |
| **장수** | **90장**(그룹당 30) | |

**🔄 매 장 바꾸는 것** = ⭐**제자리에서 손으로 휘저어 섞기**(위치·회전·겹침이 같이 바뀜, 장당 10~15초)
**🚨 고정** = 카메라 자세·거리 / **상자를 들지 않는다**(들면 거리가 변한다) / 조명 / 그룹 내 7종
⚠️ **억지로 세우거나 뒤집지 말 것** — 무작위 낙하는 **가장 안정한 자세로 수렴**하고 그리퍼 판정도
그 전제다. 실제 빈에 없는 자세를 학습시키면 해롭다.

### ⚠️ 라벨링이 진짜 상한이다

**90장 = 630 인스턴스 = 라벨링 4.5~6시간** ⇒ **8/20(목) 하루가 라벨링으로 다 간다**
(그날 IPC 마무리·F1 측정도 계획에 있었다).
⇒ ⭐⭐ **판단 = 찍기는 90장, 라벨은 60장 먼저 → F1 측정 후 나머지 30장은 다음 주.**
근거 = **찍는 것은 공장에 있을 때만 되고, 라벨링은 언제든 된다.**

### 🚨 첫 장 판정 (여기서 멈출 수 있다)

합격선 = **DIST 450~460mm · BAND 3~10% · all_valid 3~9%** + 👁️**배경 검정 + 부품 7개 선명**
🚨 **여기서 통과 안 되면 90장을 찍지 않는다** — 조건 틀린 90장 = 0장이고,
8/14에 차광용지를 깔아만 두고 사진을 안 찍어 하루 밀린 것과 같은 계열의 손실이다.

📌 **현장 카드** = `bin_picking/FIELD_CARD_0818.md` · 상세 `memory/project_capture_plan_0818.md`

---

## 🔴 촬영·공정 제외 6종 확정 — 전제가 바뀌면 원칙도 바뀐다 (2026-08-14)

> 8/6에 세운 원칙은 **"학습에서 빼지 않는다"** 였다.
> 근거는 *"제외해도 카메라엔 보이므로 다른 부품으로 오인한다"* 였는데,
> 🚨 **그 근거는 "빈에 들어온다"는 전제 위에 있었다.**

**8/14 태민님 방침 = 사출 전환 예정** ⇒ 제외 부품이 **빈에 아예 안 들어온다.**
⇒ 안 보이는 것을 학습에 넣을 이유가 없다 ⇒ **촬영에서도 뺀다.**

⭐ **"제외"에는 두 가지가 있고 결과가 정반대다**:

| | A. **빈에서도 뺀다** | B. **빈엔 있는데 학습만 뺀다** |
|---|---|---|
| 실사 화면 | 안 보임 | **보임** |
| 모델 반응 | 정상 | 🔴 **다른 부품으로 오인** |

**8/6 원칙은 B를 막는 것**이었고, 사출 전환은 **A**다. **A에서는 그 반대 근거가 성립하지 않는다.**

### 📊 학습 27종 → 촬영·공정 대상 **21종** / 제외 6종

**계산 확정(8/6) 4종** — `top_inner_sheet`(높이 1.0mm) · `bracket_sensor2`(2.5) ·
`bracket_case`(4.8) · `main_body`(6.0, ⭐**계산은 경계선이나 사람이 제외 확정**)

**⭐ 실물 확인(8/14) 2종**:

| 부품 | 무는 변 | 높이 | 사유 |
|---|---|---|---|
| `11_sw_block` | **7.1mm** | 7.0mm | *"너무 작다"* — 무는 변 자체가 작다 |
| `17_mks_holder` | **82.5mm** | 11.0mm | *"크고 높이가 없다"* — **스트로크 85mm에 여유 2.5mm뿐** |

🚨 **`17_mks_holder`가 위험한 이유** = 벌림이 **모자라면 치명(물리적으로 불가)**이다.
8/7 실측에서 치명 10건 중 5건이 `bracket_sen_1`(53.1mm) 하나였고 사유가
*"벌림이 최대급이라 무엇으로 오인하든 부족"* 이었다. **82.5mm는 그보다 크다.**

🟡 **유지 2종** = `13_variant`·`14_13` — 무는 변 **42.2mm(스트로크의 절반)** 라 벌림 여유가
크고 **높이 6.0mm 하나만** 경계다(*"집을 만해 보인다"*).
⭐ **실패 방향이 다르다** — 11번은 무는 변이 작고, 17번은 벌림이 상한에 붙었다.

### 🔴 조우(finger) 두께는 끝내 못 구했다

웹 검색·[jrtfa.com](https://www.jrtfa.com/) 확인 결과 **JEGB-4285 조우 규격은 제조사 비공개**다.
⇒ **계산으로 못 가르는 자리**라 실물을 본 사람의 판단을 기준으로 삼고,
`MANUAL_OVERRIDES`에 **사유와 함께** 박았다(8/6 원칙 — 안 박으면 **재실행 때 조용히 되돌아간다**).

### 🚨 촬영 시 = 빈에 물리적으로 넣지 않는다

**넣어놓고 라벨만 빼는 것은 최악이다** — 화면엔 있는데 GT엔 없으면
**모델이 맞게 찾아도 FP로 집계**된다. 8/5에 `linestrip` 17개가 GT에서 빠져
정확히 그 문제를 겪었다. ⇒ **빈에 안 넣으면 촬영·라벨링·학습이 전부 일관된다.**

### ⚠️ 되돌릴 수 없는 쪽

- ✅ **되돌릴 수 있음** = 찍어둔 것을 나중에 라벨에서 빼기(공짜)
- 🔴 **되돌릴 수 없음** = **안 찍은 것** — 제외 6종이 살아나면 **재촬영**

⇒ 그 위험을 감수하고 **실사 안전**을 택했다(*"실사 때 그러면 안 되니까"*).

⚠️ **모수를 섞지 말 것** — DB는 **29종**(학습에 없는 DB 전용 2종 포함), 학습은 **27종**이다.
8/6에 29종 기준 숫자를 27종 이야기에 갖다 써서 "24종"이라는 틀린 값을 냈다.
📌 **보고 시 병기**: *"공정 대상 21종 / 학습 27종 중 6종 제외"*

✅ 전체 회귀 **187/187** · 상세: `bin_picking/docs/excluded_parts_0814.md`

---

## 🎉 IPC-510에서 빈피킹이 돈다 — 검출 "개수"는 성공의 증거가 아니다 (2026-08-14)

> 8/11 화면 복구 → 8/12 원격 접속 → **8/14 모듈 구동.**
> 8/5 산출물(모듈 3개) 중 **②빈피킹이 배포될 하드웨어에서 검증**됐다.

✅ **IPC 추론이 6000과 소수점까지 일치**:
```
9
  r_guide_a_r__82d6ea93        0.991
  03_sol_block_front__b991ec0d 0.883
  13_variant__105573ee         0.853
```

### 🚨🚨 플래그 하나를 빼면 조용히 틀린다 — 8/14 실측

가이드에 명령을 쓰기 전 **6000에서 직접 돌려봤는데**, 플래그를 뺀 실행이 그럴싸하게 나왔다.

| 실행 | 검출 수 | 1위 | 2위 |
|---|---|---|---|
| ✅ **플래그 있음** | **9건** | `r_guide_a_**r**` 0.991 | `03_sol_block_front` 0.883 |
| ❌ 플래그 없음 | 10건 | `r_guide_a_**l**` 0.911 | `bracket_case` 0.910 |

⭐⭐ **개수가 아니라 "어느 부품인가"가 다르다.** 좌우(`_l`/`_r`)가 뒤바뀌고 부품 종류 자체가
달라지는데 **에러도 경고도 없다.**

**원인** = `--real_uint16_max_depth_m` 기본값이 `None`이라 **raw uint16을 mm로 취급**
(실제는 `raw×10/65535 = m`). z가 6~7배 뻥튀기돼 `--depth_keep_range`에서 엉뚱하게 걸러진다.

📌 **7/30에 이미 겪은 버그다** — 같은 플래그 누락으로 검출이 **9건→2건**이 됐고,
그때 *"못 잡았으면 cross-session 폭락으로 오진했을 것"*이라 적었다. **depth 단위 계열 여섯 번째.**

⇒ ⭐ **그래서 IPC 검증 기준을 "9건 나옴"이 아니라 "부품 이름·score까지 대조"로 못 박았다.**
이게 없었으면 **오늘 성공을 가짜로 선언**했을 수 있다.

### 🚨 내 실수 — `requirements.txt`를 믿었다

`ModuleNotFoundError: No module named 'PIL'`로 막혔다. `mentoring_new/requirements.txt`엔
**numpy·torch·tqdm만** 있는데 **실제 import는 PIL·scipy가 더 있었다**
(`depth_vq_detector/depth_preprocess.py:10`).

⭐ **"파일을 읽은 것"과 "그게 진짜 의존성인지 확인한 것"은 다른 문제다** — 시그니처 추측 금지와
같은 계열이다. 방어 = **import 전수 추출**:
```bash
grep -rhoE "^\s*(import|from)\s+[a-zA-Z_][a-zA-Z0-9_]*" depth_vq_detector/*.py infer_depth_vq_detector.py | sort -u
```
⭐ 다만 이건 `ModuleNotFoundError`로 **크게 실패**해서 즉시 잡혔다(좋은 실패).

### 🎯 막고 있던 것 3개 — 전부 "설정은 됐는데 다른 게 가로챈다"

| # | 증상 | 원인 | 해결 |
|---|---|---|---|
| ① | `python`이 Store 안내 메시지 | **앱 실행 별칭**이 가로챔 | 설정 → 앱 실행 별칭 끄기 |
| ② | venv 활성화가 조용히 안 됨 | **실행 정책** | `Set-ExecutionPolicy -Scope CurrentUser RemoteSigned`(영구) |
| ③ | `ModuleNotFoundError: PIL` | requirements 불완전 | `pip install pillow scipy` |

⭐ **①②는 8/12 TermService `Manual`과 같은 형태**다 — 설정은 돼 있는데 다른 계층이 막는다.
⚠️ ②에서 *"업데이트했지만 재정의되었습니다"* 경고는 **실패가 아니다**(CurrentUser엔 저장됐고
현재 창만 Process가 우선). 🚨`PermissionDenied`라는 단어 때문에 권한 문제로 보이지만 아니다.

### ✅ 확정 구성

**Python 3.12.8** · torch **+cpu** · numpy **1.26.4** · tqdm · **pillow** · **scipy** ·
모델 **T100**(`T100_csblur_lr1e4_ep80/best.pt`) · 경로 `C:\ipc_bundle\`

🚨 **c1plus(8/7 재학습분)를 쓰지 말 것** — holdout F1이 떨어져 배포하지 않았다.
⭐ **CPU부터 한 이유** = 6000에서 **검증된 조합**이 CPU이고, GPU는 CUDA 빌드·드라이버 정합이라는
**새 변수**를 들인다. **되는 것을 먼저 만들고 빨라지는 것은 그 다음.**

📌 **전송 = IPC에서 직접 scp**(RDP 드래그&드롭은 막혀 있다). ⭐**md5 검증 필수** —
깨진 체크포인트는 **로드는 되면서 엉뚱한 값**을 낸다.

⏭️ **남은 것** = web-api를 IPC에서 기동(🔴8/13 라우터는 **서버 재시작 필요**) · pylon · 서비스 등록

상세: `memory/project_ipc_binpick_running_0814.md` · 가이드 `bin_picking/docs/ipc_setup_0814.md`

---

## 🖥️ IPC-510 OS = Windows 유지 — "결론이 같다"와 "근거가 유효하다"는 다른 문제 (2026-08-14)

> 질문 = *"공장 산업용 PC가 윈도우인데, 빈피킹용으로 쓰려면 리눅스로 바꿔야 하나?"*
> **답 = 바꾸지 않는다.** 다만 그 답에 이르는 **근거가 세 번 바뀌었다.**

### 🚨 내 근거가 세 번 뒤집혔다 (결론은 매번 같았다)

| # | 내가 댄 근거 | 무엇이 무효로 만들었나 |
|---|---|---|
| ① | *"SDK는 로봇 제어용, 로봇 제어는 8/5로 범위 밖 → 무관"* | 🚨 **틀림.** SDK는 **협력사가 우리를 위해 한화에 요청해 제작 중**이고 *"답장 후 개발 2주"* 로 **이미 진행 중인 건**이었다 |
| ② | *"소켓 구조라 PC가 분리돼도 됨"* | 🟡 방향은 맞으나 **PC가 두 대라는 전제를 확인 안 했다.** IPC는 한 대고 그게 빈피킹 PC다 |
| ③ | *"C++ 갈래가 닫혔으니 우분투가 어딘가엔 필요"* | ✅ **3경로 답변으로 무효** — 1번이 OS 무관이라 **당장은 아예 불필요** |

⭐⭐ **교훈 = 매번 "Windows 유지"로 답했지만 ①②는 틀린 근거로 맞는 답을 낸 것이다.**
🚨 ①은 *"범위 밖"*이라는 라벨 하나로 **진행 중인 개발 건을 안 봤고**, ②는 **확인 안 한 전제** 위에 답을 세웠다
— 8/11~8/12에 다섯 번 밟은 *"확인 안 한 항목 중 그럴싸한 것을 고르는"* 그 형태.

### ⭐⭐⭐ 결정적 = 협력사가 알려준 로봇 제어 **3경로**

> **1. OS 무관** : Rodi 펜던트 내 **script 기능** / **2. 윈도우** : 기본 API인 **C++** / **3. 리눅스** : 요청 제작한 **python API**

🎯 **1번이 우리가 이미 검증해 둔 그 경로다** — `socketCreate` 통과(8/1) · 소켓 예시 확증(8/4) ·
**우리 소켓 서버 신설**(`pick_socket_server.py`) · **실측 801건 전수 왕복**(8/4) · `createPose`→`moveLinear` 6DoF 직접 전달(7/31).

⭐ **1번과 3번은 대체재다.** 7/31 *"Modbus 말고 API나 펜던트 스크립트"* 회신 때 **"API"의 실체를 몰라 두 갈래를 동시에 팠고**,
스크립트는 우리가 뚫었고 API는 협력사가 요청한 게 SDK다.
🚨 **1번의 제약은 실재** = 좌표는 소켓으로 받지만 **동작 흐름은 펜던트에서 사람이 작성**(외부 편집 불가).
⇒ 📌 **SDK를 버리는 게 아니라, "SDK 때문에 지금 OS를 갈아야 한다"는 압박이 사라진 것이다.**

### 🔴 2번(윈도우+C++)이 탈락한 이유

협력사: *"한화 기본 방식이 C++인데 저희가 따로 요청해 제작한 거라 **변경은 어려울 것 같다**"* ⇒ **윈도우용 파이썬 래퍼는 못 받는다.**
그런데 C++ 비용이 크다 — 우리 자산이 **전부 파이썬**이다(`depth_track` PyTorch · `pick_socket_server.py` · `grasp_plan.py` · 6요소 E2E · web-api).
🔴 **"로봇 제어만 C++"이 아니라 파이썬↔C++ 다리를 새로 놓는 일** = 1인 개발에 가장 비싸다.

💡 **원리** = 로봇 SDK는 **C++ 알맹이 + OS별 래퍼** 구조이고 래퍼는 **OS마다 따로 컴파일**해야 한다.
`pip install`이 편한 건 **OS별 완성품을 미리 만들어 올려두기** 때문(`pypylon`이 그 경우)이고,
🚨 **"전용 pip가 없다" = 한화가 그 배포 체계를 안 만들고 우분투용 하나만 빌드해 줬다**는 뜻.
⇒ 📌 **"윈도우라서 파이썬을 못 쓴다"가 아니라 "윈도우용 파이썬 껍데기를 아무도 안 만들었다"** 이다.

### ⭐ OS를 갈 때 실제로 잃는 것

🔴 **Tailscale+RDP 원격**(8/12 하루 = **재택에서 IPC를 만지는 유일한 경로**) · 🔴 **8/14 구동 검증** ·
🔴 web-api 운영 자산(NSSM·`deploy.bat`) / 🟡 pylon+Blaze는 미검증이나 **Basler는 Windows가 1급 지원**.
⭐ **반대로 OS와 무관한 것** = 촬영 데이터 · 재학습(A100) · **인식 성능(F1)** ⇒ 🎯 **제일 값어치 있는 것이 OS를 안 타므로 결정을 미뤄도 잃는 게 없다.**

### ⚠️ Thor 오해 정정

**Thor = Jetson AGX Thor**, JetPack(**Ubuntu 기반**) 전용이라 윈도우 불가 ⇒ *"토르는 무조건 리눅스냐"* → ✅ **맞다.**
🚨 **그러나 "우분투니까 SDK가 돌겠네"는 성립하지 않는다** — IPC/6000은 **x86_64**인데 Thor는 🚨**ARM64**다.
**우분투라도 x86 빌드는 ARM에서 안 돈다** → ✅ **8/24 확답+실측으로 확증**(아래 SDK 절):
예승님 *"SDK는 x86 전용"* + `.so` 전부 `ELF 64-bit x86-64` ⇒ 🚨**Thor에서 SDK는 못 쓴다.**
🚨 **더 중요한 정정 = Thor는 IPC를 대체하는 PC가 아니다** — 7/13 방침은 **A100=개발 / Thor=배포 엣지 / IPC-510=공장 하드웨어**(카메라·로봇이 붙는 자리)이고,
8/12 IPC 사양 확정으로 *"추론을 IPC에서 바로 돌릴 수 있어 개발이 Thor를 안 기다린다"* 로 정리했다.
⇒ ⭐ **실제 그림은 "IPC를 우분투로 이적"이 아니라 "Thor가 우분투인 채로 옆에 붙는다"** 쪽이 자연스럽다.

📌 **협력사에 *"결과적으로 빈피킹 PC를 우분투로 하는 건 맞다"* 고 전달**했는데 방향은 맞으나 위 구도와 어긋난다
→ 지금 또 정정 메시지를 보내는 것은 과하고 **SDK를 실제로 붙일 때 자연스럽게 정정**한다.

⇒ 🎯 **이번 주 최우선은 안 바뀐다** — 🥇배경재 판정→본촬영(21종) · 🥈pylon+Blaze 실취득(**여기서만 OS 판단이 뒤집힐 수 있다**) · 🥉web-api 기동.
**셋 다 SDK를 안 쓰고 셋 다 Windows에서 한다.**

상세: `memory/project_ipc_os_decision_0814.md`

---

## 🤖 한화 제어 SDK 수령·분석 — 받은 요약과 파일이 달랐다 (2026-08-24)

> 예승님이 8/11 역질문의 회신으로 클라우드 링크 전달(🚨유효기간 8/31 18:00, 대응 완료).
> ✅ 보관 = **`/data/jtm/hanwha_sdk_0824/`**(6000, 2.83GB).
> 🚨 **git 리포에 넣지 않았다** — 개인 repo가 한솔 미러로 나가 **벤더 SDK 재배포**가 된다.

### 🚨 파일을 열어보고 확인한 5가지

**① *"Python SDK 리눅스 운영체제"* = Client만 리눅스다**

```
User-PC                    E-PC (로봇 컨트롤러)
┌──────────────┐  TCP/IP  ┌─────────────────────┐
│ User app     │◄────────►│ RPC Server(Windows) │
│  RPC Client  │          │  Clink API          │
└──────────────┘          └─────────────────────┘
                                    ↕
                          Control SW (INtime RTOS)
```

**RPC Server는 로봇 안에서 Windows+INtime RTOS로 돌고 EPC 부팅 시 자동 실행**된다(가이드 4·5쪽).
`TARGET_IPADDR` = **로봇 컨트롤러 1번 이더넷 포트**(기본 `192.168.100.200`).
⇒ ⭐ **"리눅스 PC가 필요하다"가 맞고 "로봇을 리눅스로 바꾼다"가 아니다.**

**② 🚨🚨 990MB `v3.009.000.004.zip` = 로봇 OS(tgos) 펌웨어 — 올리지 말 것**

예승님 표현은 *"Python SDK를 위한 Rodi 업데이트 파일"* 인데 실제로는
`config.json`에 `robotEnv: INTIME` · `D:\hanwha\tgos\let-it-bot`,
`services/`에 **`rodi-broker/control/data/opcua/web/x-svc` 6개** = **로봇 제어 SW 전체**.
🚨 **현재 12A/10L은 펜던트 script 소켓으로 801건 왕복이 검증된 상태**이고 이것이 빈피킹의
유일하게 뚫린 경로다 ⇒ **적용 전 롤백 가능 여부부터 예승님/한화 CS에 확인.**

**③ 🚨 3세대 전용 — HCR-12A(2세대)는 불가로 보인다 [확인필요]**

`.so` 문자열 전수 = CBOX에 **`3GEN`·`3GEN_AMR`·`3GEN_SIM`·`EMPTY`·`NONE`뿐, 2GEN이 없다**
(ROBOT 모델엔 `HCR12`가 있다). 7/27 확정(**10L=3세대 / 12A=2세대**)과 맞추면
**10L 🟢 / 12A 🔴**. ⚠️ **바이너리 근거이고 벤더 확답이 아니다** — 12A를 쓸 계획이 서면 질의.

**④ x86 전용 확증** — `.so` 전부 `ELF 64-bit x86-64` ⇒ 🚨**Thor(ARM64) 불가**(위 Thor 절 갱신).

**⑤ 🚨 제어권은 배타적** — 가이드 9쪽 *"하나의 프로세스만이 제어권을 가진다… 획득하면
자동으로 다른 프로세스들은 제어권을 상실한다"* ⇒ **SDK를 쓰면 펜던트가 제어권을 잃는다**
= 검증된 경로(펜던트 script + 소켓)와 **동시 사용 불가**. SDK 채택 판단의 핵심 제약.

### ⭐ Python 패키지가 상위집합 — C++ 패키지는 사실상 불필요

우리 로봇 **10L config가 Python 쪽에만** 있고(C++는 `HCR14_3GEN` 하나), 가이드도 최신
(2026-02-03 vs 2025-03-07)이며 **cpp 예제까지 포함**한다.

### ⭐ 지금 판단은 안 바뀐다 = 사업 후 트랙 자산

로봇 제어는 **8/5로 범위 밖**, 대표님 8/18도 **트리거 방식**, 8/14 **IPC Windows 유지**
(3경로 중 **1번 펜던트 script**가 검증된 경로)도 그대로. **SDK는 보관하고 위 5개가 그때 쓸 근거다.**

📌 **교훈 = 받은 한 줄 요약을 전제로 삼지 않는다.** *"Python SDK 리눅스"* 는 *"로봇을 리눅스로"* 로,
*"Rodi 업데이트 파일"* 은 **로봇 OS 전체 교체**로 읽힐 수 있었다 — **같은 날 RGB 건과 같은 형태**
(같은 질문의 **양쪽 끝**을 봐야 전제가 선다).

상세·연락처: `memory/project_hanwha_sdk_0824.md`

### 📖 한화 매뉴얼 = 근거를 대고 답한다 (태민님 8/24 지시)

> *"다음에 같은 내용을 물으면 **정확한 근거로 어디 파일에 이렇게 명시되어 있었다**라고 답해라.
> 앞으로 한화 매뉴얼은 전부 이런 식으로 관리하자."*
> ⇒ 🚨 **한화 관련 답변에는 반드시 `파일명:쪽수`를 붙인다. 기억으로 답하지 않는다.**
> 색인 = `memory/reference_hanwha_manual_index_0824.md`(**압축 금지**)

**정독 결과 = 우리 경로는 Clink SDK가 아니라 `Rodi-Script`다**(로봇 안에서 도는 JS).
`rodi_script_api_manual_ko.pdf` **209쪽**에서 결정적인 것 4개:

| 발견 | 근거 |
|---|---|
| 🥇🥇 **`poseTrans`로 비전 좌표 → 로봇 좌표** | `ko:112` *"**비전이 작업 좌표계에서 알려준 위치를 로봇 좌표로 변환**"* |
| 🥇 **`getCoordinate` 3점법으로 워크 좌표계 등록** | `ko:114` *"3점 좌표계 정의는 워크 좌표계 설정의 **표준**" · "비전 보정에서 캘리브레이션 좌표 결정"* |
| 🥇 **소켓이 비전 연동 표준 용법** | `ko:171~178` — 매뉴얼 **예제의 소켓 이름이 `'vision'`** · `socketReadAsciiFloat`로 float 30개 · `socketAddListener` 콜백 |
| 🥇 **MQTT가 로봇에 내장** | `ko:171` *"11.5 MQTT (8) — pub/sub"* ⇒ **로봇이 브로커에 직접 붙는다**(산출물 ①의 구조 선택지) |

⭐⭐ **그래서 "hand-eye는 우리가 다 만들어야 한다"는 전제가 흔들린다** —
**빈 바닥 3점을 로봇으로 티칭**(`getCoordinate`)하고 **우리 6요소를 상대 포즈로 던지면**
(`poseTrans`) 로봇이 자기 좌표로 바꿔 움직인다. ⚠️ 단 **평면 가정**이라 높이는 우리 depth가 준다.

🚨 **[확인필요 · 김현욱 대리]** `visionPixelToWorld`·`visionMeasureTrigger`가
**HTML 예제에만 있고 PDF 209쪽엔 0건**이다(전수 검색). **외부 인식 결과를 등록할 수 있는지**가
갈림길 — 되면 로봇이 알아서 변환, 안 되면 위 `poseTrans` 경로로 우리가 한다(**어느 쪽이든 된다**).

**🥇 파지에 직결되는 것**(2차 정독에서 추가 — 1차엔 5·9·11장만 봤다)

| 발견 | 근거 |
|---|---|
| **그리퍼 개폐 = 도구 디지털 출력** | `ko:59` 예제 원문 *"그리퍼 닫기/열기"* `setToolDigitalOutput(0,1)` |
| ⭐ **잡았는지 확인 가능** | `ko:59` *"그리퍼가 부품을 잡았는지 확인"* `getToolDigitalInput(1)===1` ⇒ **P5 성공률을 로봇이 스스로 센다** |
| 🚨 **페이로드를 매번 갱신** | `ko:129` *"정확히 설정해야 **충돌 감지**·직접 교시·모션 추정이 안정적"* · 잡을 때 `setPayload(3.2,{z:50})` / 놓을 때 복원 |
| **충돌 경계 형상 등록** | `ko:131~132` `setToolBoundingBox(8점)` — 예제가 *"폭80·깊이60·높이100"* 으로 우리 그리퍼와 같은 크기대 |
| **TCP 정의** | `ko:127~128` `setToolCenterPoint([0,0,150,0,0,0])` — 우리 `a_0115` Z=200mm가 이 값 |
| **Modbus는 양방향** | `ko:181` *"**Slave 로 동작**하며 …또한 **Master 로서** 외부 슬레이브를 읽고 쓸 수도"* |
| 🟡 힘 제어 | `ko:145` *"조립·폴리싱·**디버링**·표면 추종"* ⇒ **셀B 디버링 공정에 직결 가능** |
| 🟡 컨베이어 추종 | `ko:138` *"흐르는 물건을 정확히 잡기"* ⇒ 공정 개편으로 컨베이어가 오면 |

**🚨 Rodi-X = 내 첫 판정("무관")이 틀렸다** — 앞 1~2쪽만 보고 넘겼다.
전수 정독하니 **Daemon 서비스**가 *"Rodi SW와 **별도 프로세스로 실행되는 프로그램을 제어**"*
(`Quick_Guide:11`)이고, 샘플이 *"**Python 으로 프로그램된 Daemon**"*(`Quick_Guide:18`),
예제 경로가 **`exec/vision/`**(`Tutorial_kr:22`)이다.
⇒ ⭐ **우리 파이썬 인식 모듈을 로봇 UI에 붙이는 공식 경로**이나 **지금은 불필요**
(소켓이 이미 검증됐고 8/5 범위는 "모듈+웹"까지) — **사업 후 제품화 경로로 기록.**
⚠️ 단 문서가 **Node.js v6·Rodi 2.1·2020년**이라 **현행 v3에서 유효한지 [확인필요]**.

**🥉 3차(전 함수 시그니처 209쪽 전수 추출)에서 추가**

| 발견 | 근거 | 의미 |
|---|---|---|
| ⭐ **그리퍼 경로가 셋** | 디지털I/O(`ko:58`) / **RS485+Modbus RTU**(`ko:123~124`, *"CRC를 내부에서 자동 처리"*) / 시리얼(`ko:181`) | 🚨**금요일에 실물 확인** — Modbus RTU면 **위치·힘 지정**이 되어 벌림 15점을 살릴 수 있다 |
| **Modbus Master** | `ko:183~185` `modbusAddDevice('plc','192.168.1.50',502,1)` | ⭐**로봇이 리모트IO를 직접 읽는다** ⇒ 산출물 ①에서 PC 중계가 선택 사항 |
| **MQTT에 인증** | `ko:186` `mqttInitialize(name,host,port,**user,password**)` · *"**클라우드 모니터링**·이벤트 알림"* | 🥇**산출물 ⑤에 로봇이 직접 붙는다** |
| 🚨 **`checkRunnableMotion`** | `ko:38` · `ko:20` *"특이점 근처 직선 경로는 실패할 수 있다"* | ⭐**파지 전 도달 가능 검증 필수** |
| `isSteady()` | `ko:205` *"정지면 true"* | 촬영 전 정지 확인 |
| 관절 온도·전류·전압 | `ko:204~205` | 💡**정량목표 3(설비 10대 모니터링)** 에 쓸 수 있다 |
| `controlTake/Release` | `ko:206` | SDK와 같은 개념인데 훨씬 간단 |
| 🔴 4장 용접(75~103쪽) | — | 무관(28쪽) |

**🧩 Rodi-X 완독 — `generateScript`가 핵심**
> `Tutorial_kr:20` — *"**프로그램 수행에 필요한 로직을 스크립트로 출력**하는 함수"*

⇒ ⭐⭐ **펜던트 프로그램 트리에 "빈피킹" 명령 아이콘을 만들고, 누르면 우리 Rodi-Script가
생성되게** 할 수 있다 — **대표님 8/18 "트리거 방식"의 가장 자연스러운 구현 형태**(사업 후).
서비스 4종 구조 = `Activator.js` + `*NodeService.js`(제목·HTML) + `*NodeContribution.js`(로직).
RodiAPI = `getRobotModel` · **`getIOModel`** · `getFunctionModel` · `getVariableModel`(`Tutorial_kr:31`).

📌 **`_en.pdf`는 ko판과 같은 내용이나 6장(RS485)이 빠져 있다** ⇒ **ko판이 정본**.
📌 **SDK 가이드 구판 vs 신판 차이는 1쪽**(빌드 유의사항 추가) ⇒ **신판만 본다**.
📌 **정독 범위를 색인 파일에 정직하게 적어뒀다** — 어디를 읽고 어디를 왜 건너뛰었는지.

---

## ⭐⭐ 빈피킹 웹 라우터 — "통과하는 테스트"와 "실패할 수 있는 테스트"는 다르다 (2026-08-13)

> 8/5 산출물 = **모듈 3개 + "결과를 우리 웹에 던져주는 것까지"**. 8/7에 **송신부만**
> 만들고(`web_reporter.py`) **받는 쪽이 없어** 기본 transport가 파일 출력이었다.
> 🔍전수 확인 결과 `web-api/app/`에 빈피킹 라우터가 **정말 0건** ⇒ 이 라우터가 그 수신구다.

**신설** = `web-api/app/binpick/`(models·schemas·routes) + `tests/test_binpick_ingest.py`

| 엔드포인트 | 용도 |
|---|---|
| **`POST /api/v1/binpick/reports`** | 수신 (모듈 → 서버) |
| `GET /binpick/scenes` (`gate_verdict`·`trusted_only`·`label`) | 장면 목록 |
| `GET /binpick/scenes/latest` · `/{pk}` | 운영 기본 뷰 / 상세(검출 `idx` 순) |
| `GET /binpick/health` · `WS /binpick/ws` | 상태(⭐`scenes_untrusted`) / 실시간 |

### ⭐ 설계 판단 3건

- ⭐⭐ **인증을 새로 만들지 않았다** — `app/core/jwt_middleware.py:109-113`이 **loopback을
  인증 면제**하고, 빈피킹 모듈은 IPC-510에서 **같은 호스트**의 web-api로 POST하므로 그대로
  해당한다. **5/29에 sequence_service가 같은 이유로 401을 받아 CMD 픽업이 실패한 회귀가
  있었고, 그때 넣은 면제가 여기서도 맞는 답이다.** 🚨원격 호스트에서 넣게 되는 날엔 JWT를
  붙인다(`http_transport(token=...)`가 이미 Bearer 지원) — ⭐**지금 없는 인증을 미리 짜두지
  않는다**(7/30 레지스터를 확정 전에 짰다가 무효가 된 전례).
- ⭐ **게이트 판정을 1급 컬럼으로 저장하고 조회 필터를 준다** — JSON 안에 묻어두면 조회·필터가
  안 되고, 그러면 결국 아무도 안 본다. 🚨**판정 없이 검출 수만 보이면 c3(배경을 부품으로 잡은
  장면)가 정상처럼 보인다.** ⚠️`not_checked`가 기본값 = **판정이 없는 것과 통과인 것은 다르다.**
- ⭐ **불일치는 거부가 아니라 경고** — `summary.n_detections`와 실제 `len(detections)`가
  어긋나면 **실제 길이를 저장하고 `warnings`로 돌려준다.** 🚨**거부하면 데이터를 잃고, 조용히
  넘기면 8/7 분모 버그처럼 그럴싸한 값이 굳는다.** 모르는 필드도 거부하지 않는다
  (`extra="ignore"`) — 웹이 까다로워서 인식이 멈추면 **보고 경로가 제어 경로처럼 구는 것**이다.

### ⭐⭐⭐ 테스트를 짜는 방식을 바꿨다 — 손으로 만든 dict를 쓰지 않는다

**진짜 `build_bin_picking_payload()`를 import해서 돌린 출력을 그대로 POST한다.**
🚨8/7에 게이트 키를 `valid_pct`로 추측해 틀렸는데 **테스트가 통과했고**, 그 이유가
*"손으로 만든 가짜 dict가 코드와 같은 오타를 공유"* 였기 때문이다.

⭐⭐ **그물이 실제로 찢어지는지 확인했다** — 스키마 키를 일부러 `valid_pct`로 어긋내니
**8건이 실패**하며 `송신부가 보내는데 수신 스키마에 없는 게이트 키: {'valid_ratio_pct'}`를
정확히 지목했다. ⇒ ⭐**"통과하는 테스트"와 "실패할 수 있는 테스트"는 다른 문제다.**
📌 같은 방식으로 **로봇 전용값 누출 회귀 검사**도 넣었다(`edge`·`camera_3d`가 입력엔 있고
payload엔 없어야 한다 = 8/7 설계원칙 2).

### 🐛🐛 곁가지로 잡은 버그 — pytest가 운영 DB를 쓰고 있었다

`LOCAL_DATABASE_URL` 기본값 `sqlite:///./presets.db`가 **상대 경로**라,
`web-api/`에서 pytest를 돌리면 **6000 운영 DB 파일을 그대로 쓴다.**
검증 중 **테스트 행 24건이 실제로 운영 DB에 들어갔다.**

- ✅ **처리** = `scene_id` 전수 확인으로 **전량이 내 fixture**임을 확인 후 삭제.
  **운영 테이블 전부 보존**(presets 5·print_jobs 7·notifications 282·vision 4/5).
  🚨live 8085는 **재시작하지 않았다**(uptime 34일 유지·HTTP 200).
- ✅ **근본 수정** = `tests/conftest.py`에서 **임시 DB로 격리**. ⭐내 라우터만의 문제가 아니라
  **기존 모든 web-api 테스트에 있던 위험**이다.
- ⭐⭐ **교훈 = "테스트가 통과했다"와 "테스트가 격리돼 있다"는 다른 문제다.**

✅ **검증** = **web-api 39/39**(기존 27 + 신규 12) · **bin_picking 166/166 회귀 없음** ·
⭐**실왕복 = 송신부 `http_transport` → 라우터 `HTTP 201`**(게이트 5.55%·지연 2670.4ms·
검출 순서 보존·로봇 전용값 누출 0) · **서버가 없으면 예외 없이 `ok=False`**(설계원칙 1).
🐛**환경 함정** = `python`이 없다(**`web-api/venv/bin/python`**이 정답이고 그것이 **live 6000
프로세스의 인터프리터**) / `bin_picking/tests/*.py`는 **pytest 파일이 아니라 자체 실행
스크립트**(`sys.exit`로 pytest 수집이 INTERNALERROR) → **직접 실행해야 한다.**

⏭️ **남은 것** = 🔴**운영 반영은 서버 재시작이 필요**(테이블은 생겼으나 돌고 있는 프로세스는
`/binpick/*`를 아직 서빙하지 않는다) / ⏸️**치수검사는 미착수** — ✅**측정 대상은 8/15 문서로 확정됐다**
(외관 전반 치수 + 미성형 검출 / 홀은 유무 판정) ⇒ 스키마 착수 가능, 구축 일정 9~10월.

상세: `memory/project_binpick_web_router_0813.md`

---

## 💰 구매 판단 — "살 것인가"와 "선결제로 당길 것인가"는 다른 문제 (2026-08-12)

> 8/11 주희님 연락 = 대표님과 **1차 개발 범위 확정** → 사업변경 후 구매지만 🚨**변경에 1주일**
> ⇒ **1차분만 먼저 자체 주문하고 추후 사업비로 재지불.** ⭐조건 = **세금계산서 발행 가능한 곳만**
> 8/12 = **재결제 가능 품목 리스트** 수령(합계 **2,296만~2,376만**) + *"선결제 안 하면 2~4주
> 걸리는데 그사이 가장 급한 부품은?"* 질문

✅ **회신 = 리모트 IO 계열만 급하다** (🥇**PET-2255U ×2 65.4만** + 🥇**PoE 스위치 34.1만** +
LAN 0.6만 ≈ **100만 = 전체의 4%**)
- ⭐ 근거 ① **납기 2주가 명시**돼 있어 지금 안 넣으면 그만큼 밀린다
- 🚨 근거 ② **PET-2255U가 PoE 급전**이라 스위치가 없으면 IO가 안 돈다(⚠️총판 미취급=별도 구매)
  ⇒ **셋 중 하나만 빠지면 나머지가 도착해도 못 쓴다** = 묶어서 사야 한다
- ⏸️ **PET-7015 + RTD는 예외** — **건조 공정 설계가 미착수**라 온오프 vs 연속이 안 정해졌고,
  그에 따라 PET-7015로 충분한지 아날로그 출력이 필요한지가 갈린다

⭐⭐ **오늘의 IPC 사양 확정이 구매 판단을 바꿨다** — **i7-14700 + RTX 5060 8GB + 32GB**이므로
**추론을 IPC에서 바로 돌릴 수 있고, 개발이 Thor 도착을 기다리지 않는다.**
⇒ 🎯 **Thor(875만 = 리스트의 38%)는 선결제 대상에서 뺀다. 단 구매 자체는 유지**한다
(사업비는 기간이 끝나면 못 쓴다 · 8/4 *"애매하면 사는 쪽"*).
⇒ ⭐ **"살 것인가"와 "지금 선결제로 당길 것인가"는 분리해서 판단해야 하는 두 질문이다.**

⚠️ **남는 확인거리** = ①**치수검사 8번대(약 1,000만)는 "가능 금액"이 공란** ⇒ 세금계산서 가능
업체 미확보로 읽히고, **재결제 불가면 사업변경 승인 경로로만** 살 수 있다 ②tPET-DA2는 건조 방식 확정 후
③🚨**"1차 개발 범위" 자체가 여전히 미확인**(리스트는 **품목 기준**이라 범위 목록이 아니다)
⇒ 위 우선순위는 *"현재 막혀 있는 작업"* 기준이므로 **1차 개발 정의가 다르면 바뀐다.**
⭐ **8/5 "로봇 제어 제외"·8/7 범위맵 충돌과 함께 물으면 미결 3건이 같이 풀린다.**

상세: `memory/project_phase1_purchase_advance_0811.md`

---

## 🎉 IPC 원격 접속 완성 — "토글을 켠 것"과 "서버가 리스닝하는 것"은 다르다 (2026-08-12)

> 8/11에 화면을 살렸고, 8/12에 **재택에서 들어갈 길**을 열었다. 목표는 *"AnyDesk를 고치는 것"*이
> 아니라 **재택에서 IPC에 들어가는 것**이었고, 그 기준으로 도구를 바꿨다.

✅ **최종 = Tailscale + RDP** — IPC **`100.115.122.38`** / 맥 `100.119.119.14` /
tailnet **`jtm.flickdone@gmail.com`** / 계정 `admin`(비번 8/12 신설) / `DESKTOP-TCO5269`
⭐ **P2P 직결 2.3ms**로 로컬 핑(2.7ms)과 동급이라 **항목 하나로 공장·재택 양쪽을 쓴다.**

### 🎯 막고 있던 것 3개 — 전부 "설정은 됐는데 프로세스가 옛 상태"

| # | 증상 | 원인 | 해결 |
|---|---|---|---|
| ① | 맥→IPC **핑 100% 손실** | 윈도우 방화벽 **ICMP 차단**(기본값) | `Set-NetFirewallRule -Name FPS-ICMP4-ERQ-In -Enabled True` |
| ② | **3389이 Listen조차 안 함** | 레지스트리는 0인데 **TermService가 옛 값을 물고 있었다**(재시작은 시스템 보호로 거부) | ⭐**재부팅으로만** 반영 |
| ③ | 🎯**잠금화면에선 불가, 로그인하면 붙음** | ⭐⭐**TermService `StartType = Manual`** → 부팅 시 안 뜨고 **로그인이 트리거** | `Set-Service TermService -StartupType Automatic` |

🚨🚨 **8/11 기록이 틀렸다** — *"RDP 켜뒀으니 8/14엔 Tailscale만 붙이면 됨"*이라 적었으나
**3389가 리스닝조차 하지 않았다.** ⇒ ⭐⭐ **설정 토글을 켠 것 ≠ 서버가 실제로 리스닝하는 것.**
오늘 확인 안 하고 재택에 갔으면 8/14에 발견했다. 📌 8/6 *"단일 출처를 만드는 것과 모든
호출자가 그걸 쓰는 것은 다르다"*와 같은 계열.

✅ **재택 신뢰성 검증** = 🚨**재부팅 → 실물 앞에서 로그인하지 않고 잠금화면 그대로 두고 → 맥에서 접속**
(핑 0% 손실 · `nc -vz 3389 succeeded` · Windows App 접속 성공).
⭐ **로그인하면 시험이 무효다** — 로그인 자체가 ③을 감춘다. 첫 시도의 *"혹시 몰라 로그인하니
들어가진다"*가 **오히려 원인을 특정해준 관측**이었다(잠금에서만 실패 = 로그인이 무언가를 올린다).
설정 3건 = **Run unattended** · Tailscale Automatic · **절전 3항목 전부 "안 함"**.

### 🚫 AnyDesk 폐기 — 고칠 수 있어도 구조가 안 맞는다

✅ **실제 원인은 좀비 프로세스였다**(8/11의 *"범인 = RTX 출력"*은 과한 추론. 로그에 GPU 오류 0건):
```
error win.imp     - Couldn't query the user token (0x00000005)   ← ACCESS_DENIED
info  app.session - Session closed: desk_rt_no_capture_permissions
```
서비스가 `Using existing control (16016)`로 **8/11에 실패한 프로세스를 계속 재사용**하고 있었다.
서비스 재시작(PID 4420→452)+16016 종료 후 AnyDesk가 스스로
`Zombie process detected (16016). Fixing the state.` → ⭐**`Screen recording manager registered
with os_session_id 1`** = **캡처 권한 등록 성공**. ⚠️그런데 다음 단계(`result_closed`)에서 또 끊겼다.

🎯 **폐기 근거는 "못 고쳤다"가 아니다** — ⭐⭐**비번을 걸면 재부팅 후 잠금화면에 멈추는데,
AnyDesk는 로그인된 화면 세션을 캡처하므로 그 상태엔 붙을 화면이 없다. RDP는 독립 세션을
새로 만들어 이를 구조적으로 피한다.** 🚨재택 중 재부팅은 실제로 온다(업데이트·정전)
⇒ AnyDesk만 있으면 그때 **공장에 가야 한다.**
📌 비번을 안 걸면 AnyDesk는 편해지지만 **RDP가 아예 불가**(빈 암호는 원격 로그인 정책상 거부)이고
공장 장비를 무인증으로 여는 것이라 더 나쁘다 ⇒ **비번 설정이 옳았다.**

### 🚨⭐⭐ 내 추측이 하루에 다섯 번 틀렸다 — 전부 같은 형태

| 내가 지목한 범인 | 실제 |
|---|---|
| NVIDIA RTX 출력 | 로그에 GPU 오류 **0건** |
| **5060=Blackwell이라 버전 부적합** | 9.7.14, 버전 실패 **0건** |
| 서비스 미기동(사용자 모드만) | Running + **세션 0·1 둘 다 정상** |
| **무인 액세스 미설정** | *"이미 이 비밀번호를 사용하는 프로필이 있습니다"* = **이미 설정돼 있었다** |
| 직접 연결 불가 | `arp -n`에 MAC 정상 = **L2 붙어 있음** |

⭐⭐ **공통 형태 = "확인하지 않은 항목 중 그럴싸한 것을 골랐다."** 매번 실제 상태는 정상이었고
**출력 한 줄이 나올 때마다 내 가설이 부정됐다.** 🎯결정적 정보는 둘 다 **읽으면 바로 나오는 것**:
`desk_rt_no_capture_permissions`+`Using existing control` / `Get-Service` → **`Manual`**.
⇒ 8/11에 *"관측하지 않은 것에서 연장 추론했다"*고 적어놓고 **오늘 다섯 번 같은 걸 했다.**
📌 태민님이 *"너 너무 그냥 안된다고 단언하는데, 되야하는게 정상일거라 생각해"*로 제동을 걸었고
**그 지적이 맞았다** — 구조는 다 갖춰져 있고 마지막 한 단계만 어긋나 있었다.

🐛 **부수 실수 2건**
- ⭐ **"관리자 창인지" 확인을 생략해 ACCESS_DENIED를 3번 오독** — `Restart-Service` 거부를
  *"AnyDesk 보호 ACL"*, 반복되는 거부를 *"Win11 IoT Enterprise의 조여진 정책"*으로 결론냈으나
  **그냥 일반 권한 창이었다.** 🚨프롬프트 `C:\Users\admin`을 두 번 보고도 넘어갔다
  (**관리자 창은 `C:\WINDOWS\system32`**). ✅방어=`-Verb RunAs` 후 `IsInRole('Administrators')` **True 확인 후 시작**
- ⭐ **PowerShell에서 `sc query`는 `Set-Content` 별칭**이라 조용히 실패(무출력) → **`sc.exe query`**.
  🚨나는 이 무출력을 **두 번이나 "서비스가 이상하다"의 근거로 삼았다.**

📌 **현장 유지 필수** = 🚨**PC 켜둔 채 퇴근**(꺼지면 원격 불가) · ⭐**HDMI는 GPU 포트 그대로**
🔵 **파워 W = 라벨이 보이지 않아 보류.** ⭐대신 **5060+14700이 셧다운 없이 동작**한다는 실측이
근거가 된다 ⇒ 업체가 교체했을 가능성이 높다(매뉴얼 250/300W는 **섀시 기본 사양일 뿐**).

상세: `memory/project_ipc510_remote_access_0812.md`

---

## 🎉🎉 IPC-510 해결 — "안 보인다"를 "없다"로 읽은 것이 원인이었다 (2026-08-11)

> **원인 = 모니터를 메인보드 백패널 HDMI에 꽂은 것.** 뚜껑을 열자 **MSI GeForce RTX가 이미
> 장착돼 있었고**, GPU가 있으면 **보드 내장 출력이 자동으로 꺼진다.**
> ⇒ **GPU 브래킷 포트로 옮기니 즉시 화면이 나왔다.** ✅ **Windows 11이 정상 부팅해 있었다.**

🚨🚨 **8/7 조사가 정면으로 뒤집혔다.** 나는 뒷면 사진을 보고 *"PCIe 6칸 전부 비어 있다"*고 적고,
그 근거로 내 가설 *"PCIe GPU가 내장 출력을 가로챌 것"* 을 **스스로 폐기했다.**
⭐⭐ **그 폐기한 가설이 정답이었다.**

⭐⭐⭐ **교훈 = 사진으로 내부를 판정한 것이 잘못이었다.** 뒷면의 슬롯 브래킷이 비어 보인 것을
**"슬롯이 비었다"로 읽었다.** 8/7에 *"실물 사진 한 장이 두 가설을 동시에 정리했다"*고 적었는데,
**이번엔 그 사진이 나를 틀리게 했다.**
⇒ ⭐ **"안 보인다"를 "없다"로 읽으면 안 된다.** 같은 날 오전의
*"화면이 안 나온다 → OS가 죽었다"*와 **완전히 같은 형태의 비약이고, 하루에 두 번 밟았다.**
🚨 두 번 다 **관측하지 않은 것을 관측한 것에서 연장 추론**했다(8/6 *"방향이 결과를 가른다"* ·
8/10 *"어느 쪽에 더하는가"*와 같은 계열).

⇒ 🚨 **AS는 필요 없다.** 8/10 대표님 AS 지시의 **전제 자체가 사라졌다** → 📮**재보고 필수**
(한솔에 납품업체를 물을 필요도 없어졌다).

✅ **실물 스펙** — 보드 **ELSKY GK1200-6L12C** / **Intel LGA1200** / ⭐**MSI GeForce RTX**
(🔴모델명 미확보) / 삼성 DDR4 ×2 / **FORESEE 512GB NVMe** / **Windows 11** / **IP 192.168.219.45**
⭐ 랜 MAC 앞 3옥텟 `8C:C5:8C`가 **아침 스캔의 `.45`와 일치** ⇒ 추정이 `ipconfig`로 확정됐다.

🚨 **8/4 결론 하나가 무효가 됐다** = *"PCIe 6칸 빈 슬롯 = GPU 증설 자리 확보"* →
**이미 GPU가 있다.** ⇒ ⭐⭐ **RTX 성능이 충분하면 Thor를 안 사도 될 수 있다**
(사업비는 기간이 끝나면 못 쓴다 ⇒ 판단이 빠를수록 좋다). ⚠️단 파워가 **250/300W**인데
RTX가 달려 있어 **전력 여유가 빠듯하다**(보조전원 연결 여부 미확인).

🔴 **원격은 미완 — AnyDesk를 배제했다.** ⭐**교차 검증이 원인을 하나로 확정했다**:
**맥→공장PC ✅ / 맥→IPC ❌**(*"이미지를 기다리는 중"* 후 즉시 종료) **/ IPC→맥은 정상 오류**
(macOS 화면기록 권한). ⇒ **맥·네트워크·AnyDesk 무료판·상업용 감지가 전부 무죄**이고
🎯 **범인은 IPC만 가진 조건 = NVIDIA RTX 출력**(인증은 통과하고 **화면 전송에서만** 끊긴다).
⭐ **이 교차 테스트가 "상업용 감지" 가설을 기각했다** — 모니터 2대 교차로 모니터 무죄를 밝힌 것과
같은 구조다. 🚫무효 시도 = 재부팅 · OpenGL 렌더러 · 하드웨어 가속 끄기.
✅ **RDP를 켜뒀다**(3389 · `DESKTOP-TCO5269`) — ⭐**RDP는 독립 세션을 만들어 GPU 캡처 문제를
구조적으로 피한다.** ⏭️ 8/14 = **Tailscale + RDP + 테더링 검증**(🚨`admin` 비번이 기록에 없다).

상세: `memory/project_ipc510_diagnosis_0811.md`

---

## 🚨🚨 (같은 날 오전) "화면이 안 나온다"에서 "죽었다"를 추론한 것이 비약 (2026-08-11)

> 8/7에 *"전원은 들어오나 화면 무신호"*를 보고 **하드웨어 사망**으로 추정했고,
> 8/10에 그 전제로 **AS 신청** 방향까지 잡았다. **오늘 관측으로 전제가 뒤집혔다.**

✅ **OS까지 정상 부팅한다. 죽은 것은 영상 출력 하나뿐이다.** 증거 3건이 일관된다:

| 증거 | 관측 | 뜻 |
|---|---|---|
| ① | 전원 스위치 5초 → **"삐" 비프음** | **POST 통과**(RAM 사망이면 패턴 비프나 무음) |
| ② | 빨간 LED **고정 → 깜빡임**(1분 후에도 유지) | **디스크를 계속 읽음** |
| ③ | ⭐⭐ **NumLock 눌러 LED 반응** | **키보드 입력 처리 중 = 살아 있음** |

⭐ **모니터·케이블 무죄 = 모니터 2대 교차로 확정** — 새 모니터+IPC ❌ / **새 모니터+공장PC ✅** /
**공장PC 모니터+IPC ❌**. 8/7과 결론은 같으나 **2대 교차라 훨씬 단단하다**.

🚨 **원인 후보 3에서 AS 여부가 갈린다** — ①BIOS 출력 포트 고정 ②⭐**GPU 드라이버**(OS 안에서
출력 꺼짐) ③보드 출력 회로 고장. 🚨🚨 **②면 AS 대상이 아니라 우리가 고친다**
⇒ **접수 전에 ①②를 갈라야 하고, 갈라야 증상을 정확히 말할 수 있다.**

⭐⭐ **내가 틀렸고 대표님 8/10 지목이 맞았다** — 대표님이 *"부팅했을 때 VGA 드라이버가
깔려있는건지부터 확인해봅시다"*라고 했을 때 나는 *"화면이 안 나와 OS를 못 보니 드라이버는
확인 불가, 현 증상은 하드웨어 단계"*로 반박했다. **OS 생존이 확정돼 그 반박이 무너졌다.**
⭐ **교훈 = "화면이 안 나온다"에서 "OS가 죽었다"로 간 것이 비약이었다.**
화면 없이 생존을 재는 수단(비프·LED 거동·NumLock)이 **처음부터 있었는데 8/7엔 재지 않았다.**
🚨 이건 8/6 *"방향이 결과를 가른다"* · 8/10 *"어느 쪽에 더하는가"*와 같은 계열 —
**관측하지 않은 것을 관측한 것에서 연장 추론했다.**

🐛 **부수 실수 2건**
- **"빨간 LED는 HDD 등이고 에러가 아니다"라고 단정** → 매뉴얼은 *"When the power LED is RED,
  it indicates a redundant power supply module failure"*로 **반대**를 적고 있었다.
  ⚠️단 그 문구는 **리던던트 파워 기준**이고 우리 것은 **단일 PS/2 파워**라 성립하지 않으며,
  ⭐**전면 LED·스위치 배선은 조립 시 사람이 보드에 꽂는 것**(매뉴얼 2.2 §7~8)이라
  **색과 핀 배정이 매뉴얼과 다를 수 있다** ⇒ **색이 아니라 거동(깜빡임)으로 판정해야 한다.**
- **"검은 스위치가 눌린 채 고정됐는지 확인"을 제안했다가 철회** — 매뉴얼상 전원·리셋
  **둘 다 모멘터리**라 눌려 고정될 구조가 아니다. ⚠️**색 배정은 문서에 없다.**

📌 **매뉴얼로 확정된 것**(IPC-510-B/C User Manual) — 뚜껑 = **뒷면 나사 2개** ·
파워 = **250W/300W 단일** · **IPC-510은 섀시 모델명**이라 MB/BP 버전으로 갈린다 ·
시리얼은 **정면(CTOS)+우측면(P/N&S/N)**
🚫 **~~"PCIe 6칸이 비어도 GPU 증설 불가"~~ = 폐기** — **GPU가 이미 꽂혀 있었다**(위 절 참조).
파워 여유가 빠듯하다는 우려는 **여전히 유효**하나, 전제가 "증설"에서 "이미 달려 있다"로 바뀌었다.

🌐 **공장 네트워크 구조가 오늘 처음 확인됐다** — **GAPM-7500R**(LG유플러스 공유기, DHCP 있음)에서
길게 배선되어 **ipTIME H6008**(8포트 스위칭 허브, DHCP 없음)에 물려 있다.
⭐**허브는 DHCP를 만들지 못하지만 통과시킨다** ⇒ H6008에 꽂아도 219 대역을 받을 수 있다
(🚨 내가 처음 *"H6008이면 IP를 못 받는다"*고 한 것은 **독립 전용망일 때만** 맞다).
⭐⭐ **공장 PC는 인터넷을 와이파이(219.41)로 쓰고 유선은 `192.168.100.1`에 게이트웨이가 없다**
= **DHCP가 아니라 고정 IP로 박은 "장비 전용망"**(그 망의 주인). ⚠️로봇 Modbus 대역대와 흔히 겹친다.
⭐ **원격 접속이 성공하면 CPU·RAM·OS를 화면 없이 확보**하고 **한화 SDK 회신의 "OS 미확정"도
확정으로 바뀐다** = 두 트랙이 한 작업으로 풀린다. 🚨**파워 W는 원격으로 알 수 없다**(라벨만이 답).

상세: `memory/project_ipc510_diagnosis_0811.md`

---

## 🔴🔴 cross-session 재학습 실패 — "실측 데이터 추가"가 데이터로 부정됨 (2026-08-07)

> 8/5 저녁에 **"실측 데이터 추가 > RGB 융합"** 으로 표적을 정정하고 8/6에 실행했으나,
> **c1 7장 추가가 오히려 성능을 떨어뜨렸다.** 배포하지 않고 T100을 유지한다.

| 평가 | 기준선 T100 | 신규 c1plus | 판정 |
|---|---|---|---|
| **holdout F1**(학습에 없던 3장) | 0.4231 | **0.2917** | 🔴 **−0.1314** |
| holdout 위치 R | 0.6774 | 0.5484 | 🔴 −0.1290 |
| holdout 종류 정답률 | 47.6% | 41.2% | 🔴 −6.4%p |
| **test102 F1**(회귀) | 0.6836 | 0.6756 | 🟡 −0.008 |
| **test102 matched_IoU** | 0.843 | **0.7668** | 🔴 −0.076 |

⭐ **원인 추정 = 7장이 87장 중 8%뿐인데 분포가 달라 일반화가 아니라 과적합.**
**근거 = 위치 recall과 matched_IoU가 *동시에* 떨어졌다**(종류만 나빠진 게 아니라
**마스크 품질 자체가 저하**) → "종류만 보강됐다"는 그림이 아니다.

✅✅ **숫자를 믿기 전 검증 4건**(이게 없으면 "재학습이 나쁘다"를 잘못 결론낼 수 있었다):
누수 0(train c1 7장 ∩ holdout 3장 = 공집합) · 체크포인트 md5 일치·두 모델 상이 ·
평가 조건 8/5와 동일 · ⭐⭐**기준선이 기록된 38/43/67%를 정확히 재현**(37.5/42.9/66.7).
**이 재현이 없었으면 하락이 모델 탓인지 평가 설정 탓인지 못 가른다.**

🚨 **표본 한계** = holdout 3장 / spatial TP 17~21건. **방향(양쪽 다 하락)은 분명하나
−0.13이라는 폭은 흔들릴 수 있다.**

⏭️ **다음 수** = 🥇**빈+무광시트 확보 후 학습 조건에 맞춰 수십 장 재촬영**(소량 추가는 과적합) /
🥈오버샘플링 재시도 / 🥉**혼동쌍을 벌림 기준으로 묶기**(→ 위 8/7 절에서 실행함).
상세: `memory/project_crosssession_retrain_0806.md`

---

## ⭐⭐⭐ 인식 성과는 두 숫자로 말한다 — 종류 48.5% / 파지 가능 83.8% (2026-08-07)

> **"종류를 틀린다"와 "못 집는다"는 다른 일이다.** 8/5 회의로 **인식 결과가 곧 사업
> 성과 지표**가 됐으므로, 무엇을 성과로 부를지가 그대로 사업 평가가 된다.

`label`은 **좌표에 쓰이지 않는다.** 좌표(x,y,z)는 **위치**에서 나오고 위치 precision은
**0.971**이다. `label`이 결정하는 것은 **그리퍼 벌림 하나뿐**(`grasp_plan.py`).
⇒ 혼동의 진짜 비용은 **"틀린 라벨의 벌림으로 집었을 때 잡히는가"** 다.

| 지표 | c1(실운영) | c2 |
|---|---|---|
| 종류 정답률 | 48.5% | 19.4% |
| ⭐**파지 가능률** | **83.8%** | **61.3%** |
| 차이 | **+35.3%p** | +41.9%p |

🚨 **판정은 방향이 가른다** — 벌림이 **작으면 물리적으로 불가(치명)**,
**크면 헐겁게라도 잡힌다(경미)**. 8/5에 방향을 안 보고 "다르면 실패"로 세어
**과대평가**했던 것을 코드로 고정했다. c1 분해 = 🟢무해 44 / 🟠헐거움 13 / 🔴치명 10 / ⬜불가 1.
✅ 임계를 0~5mm로 흔들어도 파지 가능률 **82.3~86.8%** = 결론이 임계 하나에 매달려 있지 않다.

🎯 **치명 10건 중 5건이 `bracket_sen_1` 하나** — 벌림 **63mm로 29종 중 사실상 최대급**이라
**무엇으로 오인하든 벌림이 부족**해진다. 나머지는 5종에 1건씩 분산(개별 대응 대상 아님).

⭐⭐ **안전 여유 +10mm가 최적점** — 치명 **10→4건**(파지 가능 92.6%)이면서 헐거움은
13→14로 거의 안 는다. **+20mm부터 헐거움이 59건으로 폭증**(전부 헐렁하게 잡아 미끄러짐).
⏸️ **DB에 박는 것은 보류** — 8/6에 확인했듯 `gripper_width_mm`은 **방향이 섞여 작성돼 있어**
절대값 자체가 티칭 때 교정 대상이다. **지금은 근거만 남긴다.**

📌 **보고 원칙 = 두 숫자를 병기한다.** "인식 정확도 48.5%"만 내면 실제보다 나쁘게 읽힌다.
구현: `bin_picking/tests/analyze_confusion_impact.py` · `tests/test_confusion_impact.py`(27/27)
상세: `memory/project_confusion_impact_0807.md`

---

## ⭐⭐ 벌림 묶기 — 등록 점수는 공짜로 풀리고, 치명은 안 풀린다 (2026-08-10)

> 그리퍼는 **최대 15점 등록**인데 DB 고유 벌림값은 **19가지**다. 그대로는 티칭을 못 한다.
> 구현: `bin_picking/tests/plan_gripper_groups.py` · 테스트 30/30

✅ **결론 = 19가지 → 15점, 파지 성능 손실 0**(치명 10→10 · 헐거움 13→13 · 무해 44→44).
**티칭 전 반드시 해야 하는 작업이 대가 없이 해결된다.**

🚨 **대표값은 반드시 그룹 최대값이어야 한다** — 벌림이 **모자라면 치명 / 남으면 경미**이므로
평균·중앙값으로 묶으면 **그룹 최대 부품이 새로 치명이 된다**(묶기가 새 실패를 만든다).

🚨🚨 **내 가설이 데이터로 부정됐다** — *"위로 묶으면 점수도 줄고 벌림도 커지니 치명도
함께 줄 것"* 이라 예상했으나 **치명이 10→10으로 불변**이었다.
⭐ **원인 = 두 조작이 다른 값을 건드린다.** 묶기는 **간격이 좁은 값만** 올리고(최대 **+5mm**)
대부분은 단독 그룹이라 그대로인데, **치명의 부족분은 3~43mm**(8건이 8mm 이상)다.
⇒ ⭐ **묶기는 "등록 점수 문제"만 푼다. 치명은 안전여유가 푼다.** 두 문제를 하나의 조작으로
설명하려던 것이 잘못이었다.

🐛 **실수 3건 — 전부 "돌려보고서야" 드러났다**
- **`analyze()` 반환 키를 최상위로 추측**(실제 `verdicts` 중첩) → KeyError로 **즉시 죽어** 잡혔다.
  시그니처 추측 금지의 일곱 번째 사례.
- 🚨 **안전여유를 DB 전체에 더했다** → **used와 need가 같이 올라 차이가 그대로**라 판정이
  하나도 안 바뀌었다(동어반복). 여유의 뜻은 *"예측한 벌림보다 N mm 더 벌린다"* 이므로
  **used에만** 더해야 한다. ⭐⭐ **"무엇을 더하는가"보다 "어느 쪽에 더하는가"가 결과를 가른다** —
  8/6에 *"방향이 결과를 가른다"*를 배우고도 같은 함정을 다시 밟았다.
- 🚨 **라벨이 맞은 건을 여유 적용에서 뺐다** → 실제 로봇은 **예측이 맞았는지 모르는 채로
  벌리므로 맞은 건도 헐거워진다**. 내 값 26 vs 8/7 기록 **59**(기록이 맞다).
  ⭐ **+10mm에서 결과가 같았던 것은 우연**이라, **한 점만 맞춰보고 넘어갔으면 못 잡았다.**

⭐⭐ **뒤 두 개를 잡은 것은 "8/7 기록값 재현" 검사다**(여유 0/10/20/43mm × 치명·헐거움·파지).
**기준선 재현이 없었으면 내 틀린 값을 그대로 믿었을 것이다** — 8/7 재학습 검증에서
*"기준선 38/43/67% 재현"*이 결정적이었던 것과 같은 구조라, 이 재현을 테스트에 박았다.

⏸️ **DB에 반영하지 않는다** — `gripper_width_mm`은 방향이 섞여 작성돼 있어 절대값이
티칭 때 실물 교정 대상이다(8/6). 근거만 남긴다.
📮 **협력사 확인 필요** = *"15점 등록"이 벌림 위치 15개인지 파지 프로그램 15개인지*
(지금은 **벌림값 15가지**로 해석).
상세: `memory/project_gripper_groups_0810.md`

---

## ⭐⭐ 촬영 환경 자재 선정 — 색은 "레진과 겹치지 않는가"로 정한다 (2026-08-10)

> 8/7 재학습 실패로 **재촬영이 유일한 경로**가 됐고, 재촬영의 선행조건이 **상자와 배경**이다.
> 고르는 과정에서 **선정 근거가 세 번 뒤집혔다.**

**① 회색 → 황색 : 주력 레진이 Grey다.**
8/5에 *"검정·흰 부품 양쪽과 대비되니 중간 회색"*으로 정했는데 **회색 부품을 고려하지 않았다.**
🎯 답은 **코드에 있었다** — `preform_client.py:139,661`의 출력 요청 기본값이 `FLGPGR05`(Grey V5)이고,
5/11 문서에 **"레진 = Grey 통일"(사용자 확정)**이 적혀 있다.
⇒ **회색 상자는 부품과 색이 겹쳐 부적합.** 보유 4종 = Grey(주력·ToF 유리) / White /
🔴**Black(850nm 흡수)** / 🔴**Clear(ToF 최악)**.
⚠️ **파생 = 검정·투명 레진 부품은 빈피킹에 못 넣을 수 있다**(지금은 주력이 Grey라 무해).

**② 황색 → 브라운 : c1이 택배상자였다.**
⭐ **데이터에 근거한 장점이, 원리에 근거한 장점을 이긴다.** 브라운의 이점은
**F1 0.4070이 나온 실측 조건(골판지 갈색)의 재현**이고, 더 깊은 상자(150mm)의 이점은
*"깊을수록 테두리가 잘 걸러질 것"*이라는 **추론**이다(120 vs 150 유의차는 데이터 없음).
🚨 8/6·8/7에 **원리로 세운 판단이 실측에 두 번 뒤집힌 전례**가 있어 데이터 쪽을 택했다.

**③ 부품상자 → 공구상자 : 전면 개방형이었다.**
부품상자는 앞이 낮게 파여 **부품이 굴러 나오고**, ⭐**깊이가 불균일해 촬영 높이를 하나로
고정한다는 전제가 흔들린다.** 공구상자는 사방이 막힌 통이다.

🚨🚨 **내가 틀린 것 2건 — 둘 다 "검증 없이 옮기거나, 내려둔 판단을 뒤집은" 유형**
- **시트 830×470을 그대로 옮겼다** — 그 값은 **인식 영역 823×464와 사실상 같아 여유가 3mm**다.
  ⭐ 게다가 **인식 영역은 촬영 높이에 비례해 커지는데**(450mm→823×464 / 600mm→1098×619)
  **높이는 "상자 도착 후 티칭으로 정한다"**가 방침이다 ⇒ **배경재는 인식 영역이 아니라
  테이블 자리(665×680)를 통째로 덮어야** 높이가 어떻게 정해지든 안전하다.
- **IR 전용 플록 시트를 권했다가 철회했다** — 플록은 털이라 **푹신하고**, 상자를 올리면
  **눌려서 높이가 변해** `PICK_CAPTURE_POSE` 고정이 깨진다. 8/5에 이미
  *"스티커가 매트보다 유리(두께 0·평평·안 밀림)"*로 정리해둔 것을 **"IR 전용"이라는 말에
  끌려 스스로 뒤집었다.** ⭐ **c1의 배경은 평범한 골판지였다** — 필요한 것은 완벽한 흡수가
  아니라 **"학습 때와 비슷하게 배경이 안 보이는 것"**이다.

⚠️ **배경재는 눈으로 고를 수 없다** — 가시광 차광 ≠ 850nm 흡수.
📏 **빈피킹 자리 = 긴 테이블 1530×680 중 좌측 665×680mm**(= 66.5×68cm).
상세: `memory/project_purchase_and_ipc_as_0810.md`

**⇒ 그래서 판정을 도구로 만들었다** = `bin_picking/tests/check_background_material.py`

⭐⭐ **이 도구를 믿을 수 있는 이유 = 판정이 실제 F1과 일치한다**(실데이터 30장 교차 검증):

| 조건 | 유효율(실측) | 판정 | 실제 F1 |
|---|---|---|---|
| **c1**(택배상자 = 학습 조건) | **4.27~7.50%** | 🟢 **10/10 통과** | **0.4070** |
| c2(상자 테두리) | 24.90~38.25% | 🔴 0/10 | 0.0814 |
| c3(흰 테이블) | 80.04~94.74% | 🔴 0/10 | 0.0000 |

⭐ **8/5 문서값을 소수점까지 재현했다.** 이것이 없으면 나중에 판정이 이상할 때
**도구 탓인지 데이터 탓인지 못 가른다**(8/7 재학습에서 *"기준선 재현"*이 결정적이었던 것과 같다).

⭐ **유효율만 보면 안 된다** — 배경이 완벽히 사라져도 **부품이 안 잡히면 촬영 실패**다.
그래서 **부품 대역(400~600mm, 평가의 `--depth_keep_range`와 같은 값)** 을 함께 재고
**0.5% 미만이면 유효율이 통과여도 🔴**로 판정한다.

📌 **현장 기준은 "배경 유효율 1.3%"가 아니라 "전체 유효율 4.3~7.5%"** 다(1.3%는 배경만
따로 잰 값). ⭐**전체 프레임으로 재는 이유 = 학습이 본 화면이 전체 프레임**이기 때문이고,
🚨**ROI로 부품만 꽉 채우면 c1도 31.5%로 올라간다** ⇒ 유효율은 "높으면 나쁘다"가 아니라
**"학습 때와 같은 화면 구성인가"의 대리 지표**다.

🐛 **만들면서 `raw_to_m`이라는 없는 함수명을 추측해 썼다**(실제 `raw_to_mm`).
⭐ **시그니처 추측 금지의 여섯 번째 사례**이고 8/7 `valid_pct`와 **완전히 같은 유형**이다.
✅ 방어 = 테스트에 **단위 물리 검산**(raw 65535 → 10000mm, 450mm 왕복)을 넣었다.

⭐ **본촬영 규모 = 최소 30장, 가능하면 50장 이상.** 8/7 실패 원인이 **7장이 87장 중 8%뿐이라
분포를 못 움직인 것**이므로 유의한 비중이 되어야 한다.
체크리스트: `bin_picking/docs/recapture_checklist_0810.md` · 테스트 23/23

---

## ⭐⭐ 모듈 → 웹 결과 전달 = 사업 범위의 경계선 (2026-08-07)

> 8/5 회의 산출물 = **모듈 3개 + ⭐"이것들 결과를 우리 웹에 던져줄 수 있는 것까지"**(원문).
> **여기까지가 범위 안이고, 로봇을 움직이는 것은 밖이다.**

🔍 **전수 확인으로 현황이 정정됐다** — "코드 0건"이 아니라 **절반은 이미 있었다**:

| 모듈 | 경로 |
|---|---|
| ① 상태 판단 | 🟢 **있음** — `web-api/app/vision/`: MQTT → `camera_manager` → DB 이벤트 → **WebSocket 푸시** + 조회 API 7개. ⚠️`camera_manager.py:30-33`에 **wash/cure 4대 하드코딩**이라 다른 모듈은 못 들어온다 |
| ② 빈피킹 | ✅ **8/7 신설**(`src/communication/web_reporter.py`) |
| ③ 치수검사 | ✅ **8/15 문서로 측정 대상 확정**(외관 전반 치수 + 미성형 검출 / 홀은 유무 판정) ⇒ 스키마를 정할 수 있게 됐다. 구축 일정 **9~10월** |

🚨 **설계 원칙 3가지**
- **①웹 전송 실패가 인식·파지를 멈추지 않는다** — 웹은 **보고 경로**이지 제어 경로가 아니다.
  예외를 던지지 않고 `ReportResult(ok=False)`로 성패를 돌려준다. ⭐**단 조용히 삼키지도 않는다**
  (원인을 `error`에 보존 + 로그). ⚠️**이 파일만 "크게 실패하라" 원칙을 예외적으로 뒤집는다.**
- **②로봇 전용 값은 웹에 안 보낸다** — 웹은 사람이 보는 화면이라 `edge`·`camera_3d`는
  쓸모없고 payload만 키운다. 실측 **6635 → 1237 bytes(5.4배 축소)**.
- **③게이트 판정을 반드시 함께 보낸다** — 없으면 **c3처럼 배경을 부품으로 잡은 결과가
  정상처럼 보인다**. ⭐**미실행은 `not_checked`로 명시**(판정 없음 ≠ 판정 통과).

⏸️ **엔드포인트 미확정 → transport 분리.** `web-api`에 빈피킹 수신 라우터가 **아직 없다**.
기본은 파일 출력(서버 없이 검증 가능), 라우터가 생기면 `http_transport`로 교체하면
호출부는 그대로다. ⭐**7/30 레지스터 설계를 확정 전에 짰다가 무효가 된 전례**를 피한 것.

✅ 실데이터 왕복 = c1 `in_distribution`(유효율 5.55%) / c3 `out_of_distribution`(81.21%).
구현: `src/communication/web_reporter.py` · `tests/test_web_reporter.py`(36/36)
상세: `memory/project_module_web_delivery_0807.md`

---

## ⭐⭐ 8/7 버그 2건 — "조용히 틀리는" 종류를 두 번 잡았다

둘 다 **에러도 경고도 없이 그럴싸한 값**이 나오던 종류다. 패턴이 같아 함께 남긴다.

**① 정규식이 빈 라벨 줄을 건너뜀** — `pred=main_body|gt=|iou=0.880|ok=0` 처럼 `gt=`가
**빈 문자열**인 쌍이 있는데(`__ignore__` 유래) `(.+?)`가 1글자 이상을 요구해 **그 줄을 누락** →
분모가 **68→67**로 줄어 정답률이 48.5%가 아닌 **49.3%** 로 나왔다.
⭐ **비율을 내는 코드는 분자보다 분모가 위험하다** — 분자가 틀리면 값이 크게 흔들려 눈에 띄지만,
**분모가 1 줄어드는 것은 그럴싸한 값**으로 나온다.
✅ 방어 = 빈 라벨도 잡아 `unknown`으로 명시 + **파싱 건수를 집계 필드와 장면 단위로 대조해
어긋나면 중단**.

**② 게이트 키 이름을 추측** — `valid_pct`로 지어 썼으나 실제는 **`valid_ratio_pct`** →
**유효율이 조용히 `None`으로 나갔다**(🚨`verdict`·`note`는 정상이라 안 보였다).
🚨🚨 **테스트가 못 잡은 이유 = 손으로 만든 가짜 dict가 코드와 같은 오타를 공유**했다.
**테스트와 코드가 같이 틀리면 통과한다.**
✅ 방어 = **진짜 `input_gate`를 돌려 교차 검증**하는 블록 신설(게이트 키 4개 실재 전수 대조).

⭐ **공통 교훈 = 시그니처는 추측하지 말고 확인할 것.** 8/5에 세 번, 8/7에 두 번 값을 했다.
⭐ **테스트는 "내가 만든 입력"만으로는 부족하다** — 상류 모듈의 **실제 출력**과 대조해야
키 이름·형식이 어긋난 것을 잡는다.

---

## ⭐⭐ 입력·출력 게이트 — 학습 분포를 벗어난 입력을 걸러낸다 (2026-08-06)

> cross-session 실측에서 **"폭락"이 세 가지 다른 일**로 갈렸다. c1(빈 안·실운영)은 **종류만
> 붕괴**했고 검출은 정상인데, c2(박스 테두리)는 **박스를 부품으로 오인**하고 c3(땅바닥)는
> **부품 자리를 아예 보지 않는다**. ⭐**c2·c3는 인식 성능이 아니라 입력 조건 문제**이므로
> **재학습이 아니라 코드로 대응**한다. 그것이 이 게이트다.

**두 층** = ①**출력 게이트(크기)** "부품보다 큰 예측은 부품이 아니다" → 개별 검출 제거
②**입력 게이트(유효율)** "학습 때와 화면 구성이 다르다" → 장면 신뢰 여부 판정

🚨🚨 **임계값을 GT 분포로 정했다가 틀렸다 — 반드시 기억할 실수.**
처음 200px로 잡았고(학습 GT 최대 166px·cross-session GT 최대 148px 근거) 그 결과
**c1 F1이 0.3882 → 0.3494로 떨어졌다.** 버려진 4건이 **전부 진짜 TP**였고 주범은
`08_r_guide_a`(**STL 271mm = 최대 부품**, 211~223px로 검출)였다.
⭐⭐ **원인 = 예측 bbox는 마스크 기반이라 GT보다 크게 나온다**(정답 예측 최대 **223px**).
**게이트가 판정하는 대상은 예측이므로 예측 분포로 임계를 정해야 한다.**
⭐ **교훈 두 개**:
- **게이트·필터의 임계는 "실제로 판정하는 값"의 분포로 정하고 검증할 것.**
- **테스트에 성능 회귀 검사를 넣을 것** — 내 첫 테스트는 GT만 봐서 이 실수를 통과시켰고,
  **F1 회귀 검사를 추가하고서야 잡혔다.** "코드가 도는가"와 "성과가 있는가"는 다른 질문이다.

**확정 임계값** (전부 실측 근거, 추측값 없음)
- **크기 230px** — 진짜 TP 예측 최대 223px + 여유. 임계별로 200px→**TP 4손실**·FP 81제거 /
  **230px→TP 0손실·FP 77(40%)제거** / 280px→FP 58 ⇒ **TP 무손실로 FP를 가장 많이 잡는 자리**
- **유효율 2~25%**(경고 10%) — 학습세션 실측 2.0~23.8%(p99 8.8%) · c1 4.3~7.5%(분포 안) ·
  c3 80~94.7%(이탈). 상한 25%가 **c1을 통과시키고 c2·c3를 잡는** 자리

**효과** = c1(실운영) **완전 무해**(F1·위치P 변화 0) · c2 위치 precision **0.338 → 0.512** ·
전체 FP **194 → 117(-40%)**. 🚨**c3는 recall이 0 그대로** = "잘못 잡은 것을 걸러내는" 문제가
아니라 **"애초에 못 찾는" 문제**여서 게이트로 해결되지 않는다(재학습 대상).

⭐ **호출자까지 같이 고쳤다** — 8/5에 `depth_units.py`를 단일 출처로 만들었으나 `convert()`가
그것을 쓰지 않아 **좌표가 전건 무효**였던 전례가 있다. 그래서 게이트는 **CLI에서 기본 적용**
(`--no-gate`로만 끈다)이고 소켓 서버의 vision provider에도 넣었다.
⭐ **거부 메시지에 원인을 싣는다** — "detections 비어 있다"만 보면 엉뚱한 데를 뒤진다.
`장면 게이트: 유효율 81.2% > 25%` + `크기 게이트가 이미 4건 제거`를 함께 출력한다.

**설계 원칙** = 게이트는 값을 **조용히 고치지 않는다**(버린 것을 이유와 함께 남긴다) ·
**판정 근거가 없는 검출은 버리지 않고 통과**시킨다(근거 없이 버리면 정상 검출을 잃는다) ·
장면이 분포를 벗어나도 **기본은 표시만** 한다(멈출지는 운영 정책이므로 상위가 정한다).
⏸️ **ROI 크롭은 빈 확정 후** — 빈이 화면에서 차지하는 영역이 정해져야 좌표를 박을 수 있다.

구현: `bin_picking/src/pipeline/input_gate.py` · `tests/test_input_gate.py`(27/27)
상세: `memory/project_input_gate_0806.md`

---

## ⭐⭐⭐ 빈피킹 그리퍼 확정 + 파지 가능성 판정 (2026-08-06)

> **그리퍼 = 주강로보테크 JEGB-4285P-3MA** (협력사 회신). 그전까지 **프로젝트 문서 어디에도
> 모델명이 없었다** — 전수 검색 결과 있는 것은 `D_GEN_OUT_0~3` 4채널이라는 제어 방식뿐이었다.
> 대표님 8/5 지시 *"그리퍼로 못 잡는 제품은 다 빼기"* 를 실행하기 위한 판정 근거가 이것이다.

**사양** (모델명 = `JEGB`(전동 평행) - `42`(본체) **`85`(스트로크 85mm)** `P` - `3MA`):
개폐 **0~85mm(완전히 닫힘)** · 파지력 **15~150N 프로그래머블** · **최대 15점 등록** ·
RS-485 half-duplex + 디지털 I/O(입력5/출력3) · Self-lock · 자중 1.25kg(140형) · 권장 페이로드 3kg

🚨🚨 **이 사양이 판정 기준을 뒤집었다.** 그리퍼를 모르는 상태에서는 *"최소 벌림보다 얇으면
못 집는다"* 는 평행 그리퍼 일반론으로 판정할 수밖에 없었는데, **JEGB는 0mm까지 닫히므로
그 전제가 아예 성립하지 않는다**(얇아서 못 집는 경우가 없다).
⇒ ⭐**실질 제약은 두 개뿐** = ①**스트로크 85mm** ②⭐**조우(finger) 진입 높이**.
**후자가 유일하게 남은 실제 병목이다.**

⭐⭐ **판정 전제 = 부품은 빈에 눕혀 있다.** 무작위 적재는 **안정 자세로 수렴**하므로 가장 얇은
면이 바닥에 닿는다(19×19×1mm 판이 1mm 면으로 서 있을 수는 없다). ⇒ **무는 변 = 두 번째로
작은 변 / 조우 진입 높이 = 가장 작은 변**이고, "세워서 두께를 문다"는 경로는 **존재하지 않는다.**
🚨**이 전제를 빼먹고 "세워 잡기"를 후보에 넣었다가 두께 1.0mm 부품을 "파지 가능"으로 판정하는
물리적으로 불가능한 결과를 냈다.** ⭐**교훈 = "치수가 범위 안"은 필요조건이고, "그 자세로
놓일 수 있는가"를 함께 봐야 한다.**

**판정 결과 = 🚨모수를 구분해야 한다** (8/7 검산으로 정정):

| 모수 | 제외 | 보류 | 공정 대상 |
|---|---|---|---|
| **⭐학습 27종**(= 실제로 카메라에 잡히는 것) | **4종** | 4종 | **🟢19종**(보류 포함 최대 **23종**) |
| DB 29종(= 27 + DB 전용 2종) | 5종 | 4종 | 20종 |

🚨🚨 **8/6에 "제외 5종 / 공정 대상 24종"이라 적은 것은 틀렸다.** 제외 5종 중
`top_inner_sheet004`는 **애초에 학습 27종에 없는 DB 전용 부품**이라 공정에서 제외할 대상이
아니다. ⭐**29종 모수로 센 숫자를 27종 이야기에 그대로 갖다 쓴 것**이 원인.
⭐ **DB 전용 2종** = `10_guide_paper_roll_l` · `top_inner_sheet004` (학습 라벨엔 없음.
반대로 학습에만 있고 DB에 없는 부품은 **0건** = 검출 27종은 전부 DB에 등재)
⭐ **보고 시에는 "19종 확정 / 실물 확인 후 최대 23종" 두 숫자를 같이 낸다** — 지원사업
정량목표에 종수 요건이 걸리면 27→19는 폭이 크고, **보류 4종의 생사는 조우 실물 두께가 가른다.**

| 판정 | 부품 | 무는변 | 높이 | 사유 |
|---|---|---|---|---|
| 🔴 | `top_inner_sheet` | 19.0 | **1.0** | 조우 진입 불가 |
| 🔴 | `top_inner_sheet004` | **89.0** | **1.0** | 진입 불가 **+ 벌림이 스트로크 85 초과** |
| 🔴 | `bracket_sensor2` | 4.8 | **2.5** | 조우 진입 불가 |
| 🔴 | `bracket_case` | 5.5 | **4.8** | 조우 진입 불가(경계) |
| 🔴 | `main_body` | 25.0 | **6.0** | ⭐**수동 확정** — 계산은 경계선이었으나 제외로 결정 |
| 🟡 | `11_sw_block`·`13_variant`·`14_13` | — | 6~7 | 보류(높이 여유 부족) |
| 🟡 | `17_mks_holder` | **82.5** | 11.0 | 보류(벌림이 상한 85에 근접) |

- ✅ **결론이 안정적인 근거** — 진입 높이 하한을 바꿔도 **5~6mm 구간에서 제외 종수가 동일**하고,
  실측 높이 분포에 **4.8mm와 6.0mm 사이 간극**이 있어 5mm 경계가 타당하다.
- ⭐⭐ **사람이 내린 결정은 코드에 박는다**(`MANUAL_OVERRIDES`) — 계산은 경계선을 "실물 확인
  필요"로만 낼 수 있고 **빼느냐 마느냐는 공정 결정**이다. 🚨**박아두지 않으면 스크립트를 다시
  돌릴 때 조용히 되돌아간다.** 덮어쓴 사실은 결과에 함께 출력해 숨기지 않는다.
- ⭐ **학습은 전 종수 유지, 로봇 동작만 태그로 가른다** — 제외해도 카메라엔 보이므로 모델이
  **다른 부품으로 오인**하고 잘못된 벌림으로 집으려다 손상된다(7/9 병합이 F1 0.684→0.669로
  떨어진 전례). 대표님 지시(**공정 대상 제외**)와 이 원칙(**학습 데이터 제외 금지**)은 층위가
  달라 양립한다.
- 🔬 **DB 대조로 드러난 사실** — 기존 `gripper_width_mm`는 **방향이 섞여 작성돼 있다**(20종은
  중간변+여유 약 10mm, **7종은 최소변+여유**). 눕힘 전제와 어긋나는 7종은 **티칭 때 실물로
  교정**할 것이고, 눕힘 기준 값은 새로 넣은 `grip_span_flat_mm`다.
- 🐛 **일부만 처리되는 버그 1건** — `"14_13"`이 숫자로 시작해 YAML이 따옴표를 요구하는데
  정규식이 인용부호 키를 놓쳐 **28/29만 태그**됐다. ⭐**입력(survey) 쪽만 보는 검사로는 안
  잡혔다** → **양방향 검증**(DB에 있는데 손대지 못한 부품이 있으면 중단)을 추가했다.
  ⭐**"일부만 태그"는 태그 없는 부품이 기본 동작으로 흘러가는 종류라 조용히 틀리면 위험하다.**
- 🚨 **확인 필요** = ①⭐**조우 실물 두께**(제외 종수를 직접 가른다) ②🚨**로봇 TCP payload가
  1.5kg인데 그리퍼 1.25kg + 부품 최대 0.94kg = 2.19kg로 초과**(로봇 한계 10kg는 여유)
  ③⭐⭐**"최대 15점 등록"이 맞으면 DB의 부품별 벌림 19가지를 그대로 쓸 수 없어 15개 이하로
  묶어야 한다**(4채널=16조합과 맞아떨어진다. RS-485를 쓰면 임의 지정이 가능할 수도 있다)
  ④**공식 카탈로그 PDF 미확보** — 85형 자중·반복정밀도·조우 규격은 제조사 문의가 필요하다.

구현: `bin_picking/tests/survey_graspability.py`(CAD 전수 판정) ·
`apply_pickability_tags.py`(주석 보존 줄단위 삽입) · `config/grasp_database.yaml`(29종 태그)
상세: `memory/reference_gripper_jegb4285.md`

---

## 🚨🚨 대표님 8/5 회의 — 사업 범위 근본 재정의 (로봇 제어 전부 제외)

> **2026-08-05 18:00 회의.** 노션 AI 회의록이 중간에 끊겨 태민님 메모로 보완했다
> (누락분은 그 페이지 하단 「회의록 누락분 보완」에 추가). 이 회의가 8월 계획을 바꾼다.

🚨🚨 **"로봇 제어는 이번 사업 전부 제외"** → 산출물은 **모듈 3개**(①상태 판단 비전 ②빈피킹 ③치수검사)와 ⭐**"이것들 결과를 우리 웹에 던져줄 수 있는 것까지"**(대표님 원문)다.

⇒ **8/5 아침에 세운 우선순위가 대부분 범위 밖이 된다**: hand-eye 캘리브·소켓 실기 검증·`rx`/`ry` 티칭·로봇 파지는 🔴**사업 범위 밖**이고, 10L 드라이버 오류도 급하지 않게 됐다. 브래킷은 🟡**카메라 고정용으로만** 의미가 남는다. **IPC-510은 유지**된다(모듈 3개가 여기서 돌아야 한다). 그리고 ⭐**cross-session 인식 정확도가 최우선으로 올라간다** — 산출물이 "인식 결과"라면 **F1 0.4070이 곧 성과 지표**다. ⭐⭐**같은 날 라벨링해서 F1을 측정한 것이 결과적으로 가장 값어치 있는 작업이 됐다.**

✅ **[해소됨 — 8/15 협약변경 문서 + 8/18 대표님 답변]** 아래 충돌은 **위탁1 과업이 "디지털 제어 인프라 구축 설계·시공"으로 교체**되고 대표님이 **트리거 방식**으로 확정해 풀렸다(위 8/15 절 참조). 이하는 발생 시점 기록.

🚨 **~~미해결~~ 충돌** = 사업계획서 완성물 ②가 **「셀B 제어 SW」**(위탁1 2,000만원 = 셀B 제어SW+통합 미들웨어+대시보드)인데 **"로봇 제어 제외"와 정면 충돌한다.** "제어 SW"가 **모듈 결과를 웹에 전달하는 미들웨어**면 충돌이 없고 **로봇 동작 제어**면 완성물을 못 채운다 → **지원사업 산출물이라 임의 해석은 위험하고 대표님 확인이 필요하다.**

🚨 **공정 순서가 바뀌었다** = *"셀A가 끝나고 셀B 시작 전에 치수검사 먼저, 그 이후 바텀비전부터 작업 시작"* → ⭐**치수검사가 "드릴 전"이 되어 재는 것이 드릴 홀이 아니라 출력물 자체 치수가 된다.** 8/5에 적어둔 "드릴 전(출력물 치수) 갈래"가 이 케이스다. ⚠️**"드릴 진동에서 격리" 논리가 약해져** 치수검사 자리 근거를 재검토해야 한다. ⭐**다만 이 순서가 더 합리적이다** — 불량 출력물에 드릴을 뚫는 낭비를 막는다. ✅**8/15 문서로 확정 = "외관 전반 치수 확인 + 미성형 검출 / 홀은 유무 판정 수준"** ⇒ 8/3~8/4 광학 검산은 **ø3mm 홀 지름 측정 전제**였으므로 **그 전제가 폐기됐다**(위 8/15 절 참조).

⭐ **부품 범위 축소 확정** = *"그리퍼로 못 잡는 제품은 다 빼기"* + *"바텀비전에서 넘어가는 부품도 빼거나 특정 부분만 검사"*. 이는 내가 같은 날 낸 "학습에서 제외 금지"와 **층위가 다르고 양립한다** — 대표님은 **공정 대상 제외**, 나는 **학습 데이터 제외 금지**(빼면 다른 부품으로 오인해 잘못된 벌림으로 손상)였다. ⭐**학습은 27종 유지하고 공정 대상만 축소**하며, 앞서 제안한 **3분류 태그**가 정확히 이 구조다. ⏭️**제외 후 남는 종수**가 정량목표에 걸릴 수 있다.

⭐ **ERP 연계** = *"빈피킹에서 버리면 재고관리도 되게끔"* → 6/18 3축의 ③ERP와 연결되고 **불량 판정 → 재고 차감**이 요구사항이다. **서비스 플로우는 유저 입장·우리 입장을 각각** 정리하고, 고객 **견적 조회~수령 완료** 전 과정을 ERP로, 제조 공정은 **간이 MES**로 나눈다(플로우 정리 후 개발 착수, 기획 한 달+·구현 2주).

🏭 **공장 레이아웃** = 공간 여유가 있어 **모든 장비를 벽면 배치**하는 안을 검토 중이고, ⭐**로봇 레일 도입 전에 사람이 로봇 역할로 공정을 시뮬레이션**한다("사람이 할 수 있으면 로봇도 할 수 있다"). ⚠️**연말/내년 공장 이전 시 레이아웃 변경 가능성이 크므로 8/5에 확정한 셀B 배치도 잠정으로 봐야 한다.**

상세: `memory/project_ceo_meeting_0805.md`

---

## ⭐⭐ KAIST 자산 → 회사 depth_track 편입 (2026-07-13) + 7월 빈피킹 이력

> 🚨 **일자별 경위는 이 파일에서 걷어냈다**(7/13~8/1). 상세 = `memory/project_depth_track_integration_0713.md` ·
> `CLAUDE.local.md` W28~W30 주간 요약 · `bin_picking/docs/P0_0728_0730_완료기록.md`.
> **여기에는 "지금도 유효한 방침·전제·수치"만 남긴다.**

### 🟢 지금도 유효한 것 (판단 근거)

| 항목 | 내용 |
|---|---|
| **위치·성격** | `bin_picking/depth_track/` = **Blaze ToF depth 단독 + CAD codebook 매칭**(색·재질 무관·CAD만 있으면 데이터 수집 없이 학습). `yolo_track/`(RGB YOLO)과 **대등한 두 번째 인식 트랙** |
| **자산 귀속** | 조교 모델 포함 **전부 회사 자산**(태민님 7/13). 원본 `~/kaist_project` **불변** + 복사 편입 |
| ⭐**GPU 전략** | **개발 = A100** / **배포 = Thor**(ARM64) / **IPC-510 = 공장 하드웨어**(카메라·로봇이 붙는 자리) |
| **편입 규칙** | 코드만 git(dual push) · 대용량 데이터·체크포인트는 **6000 상주**·`data/` 심볼릭·git 제외 |
| 🚨**경계** | `kaist_backup/` = **동결된 백업**(수정 금지) / `depth_track/` = 살아있는 개발. **KAIST 팀 repo는 PUBLIC**이라 회사 정보 금지 |

### 📊 성능 이력 (기준선 — 어느 시험지인지가 중요하다)

| 지표 | 값 | 시험지 |
|---|---|---|
| KAIST 최종 | **F1 0.684**(위치 0.88/종류 0.85) | 27종 test100 = **같은 세션**(재현 능력) |
| 학습세션 재평가 | 0.8184 | 🚨**train 섞임 = 일반화 아님**(7/29) |
| **cross-session 기준선** | **0.4070** | 8/5 c1 = **처음 보는 세션**(진짜 일반화) |
| **현행** | **0.5998** | 8/18 90장 + thr 0.20 + 공정 화이트리스트 |

🚨 **0.684와 cross-session 값을 나란히 놓지 말 것** — 시험지가 다르다. cross-session이 낮은 것은 정상이고 그게 진실이다.

### ⭐ 7월에 확립돼 지금도 쓰는 것

- **로봇 제어 3경로** = ①**펜던트 script + 소켓**(OS 무관·**우리가 뚫어놨다**, 801건 왕복 검증) ②윈도우+C++ ③리눅스+python SDK
  ⇒ ⭐**1번이 살아있는 경로**이고 `createPose`→`moveLinear`로 **실수 6DoF를 직접 전달**한다(INT16 인코딩 불필요).
  🚨**Modbus는 빈피킹 좌표에 쓰지 않는다**(16bit 정수라 부적합 / 130~255는 이송 전용) — 단 **이송 8단계 핸드셰이크는 유효**
- **6요소 좌표 E2E** = `src/pipeline/depth_track_to_6elements.py`(실측 801건·z 유효 100%) + **회전각 98.8%**(마스크서 기하 복구, 재학습 불필요)
- **카메라 캘리브 확정값** = Blaze intrinsic **RMS 0.213px**(`fx=309.3/fy=310.1`) · ACE2 **RMS 0.546px** · extrinsic 산포 **5.29mm**(`[+44.7,-0.5,-2.1]`mm·1.78°)
  🚨**폐기 추정치 `fx=553/fy=188`을 쓰지 말 것 — fy가 65% 틀렸고 그것이 7/20~21 정렬 실패의 진짜 원인이었다**
- **depth 단위 규약** = `depth_units.py` **단일 출처**(uint16이면 raw, float이면 통과). 🚨**`depth_m = raw×10/65535`** — 이 단위 버그를 **닷새에 다섯 번** 밟았다
- ⏱️**지연 예산 실측**(`tests/measure_inference_latency.py`, 8/5 6000 CPU) = **추론 웜 2.45s**(콜드 2.54·최대 2.67) + 6요소 0.002s + 검증 0.000s ⇒ **최악 2.67s** · 모델 로드 0.62s는 **접속 전 완료 가능이라 예산 밖**.
  ⭐**콜드/웜 차가 거의 없어 워밍업 우려는 기각**됐고 **병목은 추론 단독**이다.
  🚨**그래서 8/21 E2E 러너의 6.7~7.5초는 추론 탓이 아니라 `subprocess`가 매번 모델을 재로드한 탓** ⇒ **상주 프로세스가 해법**(KTR 3초)
- 🌐**GigE 규칙** = **카메라 IP 대역 = 꽂힌 랜포트 대역**(어긋나면 열거는 되고 열기만 timeout) → `setup_camera_net.py --apply`

### 🔴 닫힌 경로 (다시 시도하지 말 것)

**TTA**(8/21 실측 −0.070·8배 느림 — ⭐KAIST엔 +0.007이었는데 **FP 거동이 정반대**) ·
**합성 aug 강화**(천장 F1 0.203) · **HP 튜닝**(16조합 소진) · **추가 촬영**(+0.03/90장) ·
**모델을 21종으로 재학습**(클래스가 체크포인트에 박혀 되돌릴 수 없다)
⇒ ⭐⭐**성능 열쇠는 실측 소량 fine-tune이었고**(0.203→0.45→0.684), 그마저 8/19에 천장에 닿았다.
**남은 병목 = 센서 물리**(대칭·표면패턴) ⇒ 🥇**RGB 융합이 그 답의 후보**(→ § 궁극 목표)

## ⭐ 지원사업 구매물품 조사 (7/23 조사) → ✅ 7/24 미팅서 방향 대폭 확정 (아래 7/23 조사 전제 다수 뒤집힘)

> 🚨🚨 **2026-07-24 구매물품 확정 미팅(대표님·주희님)으로 아래 7/23 조사의 전제 다수가 무효화됨** — 반드시 최신 결정을 우선할 것. 회의록=`3a7a7d640dab804bbeaae18a262aa1e3`, 상세=`memory/project_hwaseong_support_program_0716.md` §7/24 미팅.
> - 🚨 **중국산 칩 전면 배제**(허용=퀄컴·브로드컴·NXP·마이크로칩·STM 최우선) → **reCamera 2002(SG2002)·오렌지파이·락파이 탈락**, 어제 "MaixCAM 동일칩 재사용성" 근거 무효. **N6는 ST칩이라 채택 유지**. 보유 MaixCAM=테스트/자비 장비로만.
> - ✅ **Thor 구매 확정**(과스펙시 NX/슈퍼 하향, AGX Orin 신규구매 제외). OCR=고화질 1대 크롭커버+파인튜닝. 외주 2천만 취소→내부개발(6천만 전액 장비 가능). 일정 10/30 종료·**빈피킹 8월말 데드라인 아님**.
> - 🔥 **ToDo #1=장비 리서치 7종 "오늘 마감"**(MPU카메라·웹캠·PoE카메라·SBC보드ST·PoE스위치·UPS·이더넷 리모트IO). 어제 카메라 조사는 중국칩 배제로 재작업 필요.
> - ✅✅ **7/24 오후 장비 리서치 실행**(노션 `🛒 7/24 장비 리서치` `3a7a7d640dab805294e0db6c5eb9889a`, 주희님 공동): ⭐**리모트IO 신호 전수조사**=우리 신호 거의 다 디지털(아날로그 0), **로봇은 Modbus 직결이라 리모트IO서 뺌**, 리모트IO=세척·건조·드릴·타워램프·안전 등 전선신호 통합허브(대표님 "전·후공정 포괄" 실체). 채택 **PET-7255**(8DI/8DO·PoE·453,000원·총판 굿모닝계측기 최동호), 개수 입출력 각~10→2식 필요, 건조 온도 시 PET-7015 아날로그 추가. Thor 미국가 $5,499 실인상·국내 962만 확인. 상세 `memory/project_hwaseong_support_program_0716.md` §7/24 장비 리서치.
> - ✅✅ **최종 확정 구매 리스트(7/24 주희님·태민 검토, 노션 맨 아래)**: ①Thor(~962만) ②온디바이스AI카메라=**AMD Kria KV260**($249) ③웹캠 고성능 **e-con e-CAM82_USB**/저비용 **로지텍 C270** ④SBC=**BeagleBone AI-64**($189·TI·미국) ⑤카메라센서 고해상 **RPi HQ IMX477**/저비용 **IMX219 8MP**. ⚠️발주 전 국내정품·세금계산서·SBC 호환 확인.
> - ✅✅ **대표님 컨펌(7/24 저녁)으로 아래 항목 확정 추가/변경** (위 초안에서 갱신): ①**리모트IO 넉넉히 구매**(보류 해제)=**PET-7255 2대 + PET-7015 1대 + PoE 스위치(NEXT-POE8022GDT)**, 출력(DO 10~15)이 입력(DI 8~12)보다 살짝↑→입출력 균형형 PET-7255가 정답, 아날로그=건조기 온도 입력만 PET-7015(히터 연속조절 시에만 AO 추가[미확정]). ②**온디바이스 카메라에 OpenMV N6 추가**(Kria + N6). ③**SBC에 Rubik Pi 3 추가**(BBAI-64 + Rubik Pi 3, ⚠️BBAI-64는 우선 구매하되 산업용 아니어서 검토). 발주 전=로봇 신호 정체(dry/wet·전압·NPN/PNP, 한화 HCR/예승님)+건조 온도조절 방식+최동호 사원 견적. ⚠️노션 최종리스트 표 위 "리모트IO 보류" 인용문은 옛 문구=정리 필요.

> **2026-07-23 조사 (히스토리)** — 대표님 디스코드 지시(①AI카메라·웹캠 더 찾아봐 ②토르 사양 ③네트워크 ④건조대 사업구매 불가)로 태민+주희 심층 조사. ⚠️**결론 다수가 7/24 미팅으로 무효화**(중국칩 배제·reCamera 탈락·Thor 확정)되어 상세는 접음. **살아남은 것**=Thor 가격 $3,499→$5,499 실인상[확인]·건조대 사업구매 불가·PoE 스위치 격상·⭐웹캠 광학 인사이트("4K보다 렌즈"=수동 바리포컬로 화면 꽉 채우기+CPL+고정노출)·⭐우리가 이미 Basler+pypylon 생태계. 전문=`docs/archive_purchase_202607/구매물품_조사_0723.md`(⚠️전제 바뀜 주의).
> ⚠️ **정직성 원칙(태민님)**: 확실하지 않은 건 맞다고 하지 말 것 → 문서에 **[확인]/[미확인]/[실측필요]** 라벨. (조사·보고 전반에 계속 적용)
- ✅ **대표님 7/23 답변**: ①**시스템 서버는 있으나 추론서버 없음→그래서 Thor 살리려는 것**(⚠️"추론서버 있어서"가 아님) ②⭐**Orin NX·Nano 이미 보유** ③녹색 추천행 구매 ④PoE OK.
- ⭐⭐ **작업방식 지시(7/23)**: 대표님 "앞으로 PDF·파일 말고 **노션 페이지**로" → 태민님 "**산출물 만들 때마다 PDF vs 노션 먼저 물어봐줘**". ⚠️**자동으로 HTML/PDF 만들지 말 것** → `memory/feedback_report_doc_format.md`
- ✅✅ **7/24 노션 전면 정비**(위 지시 이행): **Robot Arm Factory 마스터** 6/26→7/24 전면 갱신(본문+Plans/Issues DB, 과거 §9·§10 삭제) + 회의용 페이지 신설(`3a7a7d640dab8120aa0df0241e112d73`). ⚠️Notion MCP=로컬 PDF 직접 첨부 불가(HTML은 content 텍스트로 embed 가능)·한글 자동변환 깨짐→**update 후 fetch 검수 필수** → `memory/project_notion_company_pages.md`

> 🛒🛒 **7/27 발주 리스트 완성** — 노션 `🛒 7/27 최종 발주 장비 리스트업`(`3aaa7d640dab80199ca6ec0bdcd5c5f0`). **오늘 발주 가능 약 1,348만원(VAT포함), Thor가 72%**. ✅**견적서 수령**(굿모닝계측기 2026/07/27-16, 유효 1주일, 납기 2~3주): 확정분 **PET-7255×2+PET-7015 = VAT포함 1,532,300원**(⚠️견적서 총액 2,449,700은 4품목 합계, AO 2종은 참고 문의 답변이지 요청 품목 아님). ⭐⭐**사양서 4종 정독**=ICP DAS 명명규칙(`ET/PET`×`70xx 1포트/72xx 2포트`) + ✅✅**PET-7255 출력=Source(PNP) 확정**(로봇 wet/PNP/24V 일치)·**입력 Dry/Wet·Sink/Source 겸용**이라 세척·건조기 신호 방식 무관 → 현장 실측 리스크 감소. ⚠️PET-7015 **CH3·CH4 AGND 공유**(CH0~2 우선). 🔌**AO는 이번 제외**(필요시 tPET-DA2 24만, PET-7028 59만은 오버스펙. ⚠️**AO만으론 히터 못 돌림**=10V@10mA 제어신호, SSR 별도). 📝**PET-7215 제안했다 철회**(태민님 지적=아날로그 신호 0개·예비 구매에 상위 스펙은 과투자). 📹**웹캠 기종 변경**=e-CAM82 단종→**See3CAM_CU135 $299**(13MP·AR1335 미국칩·M12 교체, 총판 "프린터 OCR에 많이 쓰는 모델", ⚠️렌즈 별매·촬영거리 미정). ⭐**전 품목 국내 구매처 확보**(디지키 원화 실가·C270 정품 23,040원, 🚨N6 국내재고 없음·Rubik Pi 3 총판 없음). → `memory/project_hwaseong_support_program_0716.md` §7/27 오후

> ⭐⭐ **7/27(월) 오후 = 구매물품 재검증 + 리모트IO 2식 확정 + 발주 문의 발송** (대표님 "우선 구매 먼저 확정"). 노션 `🛒 7/24 장비 리서치` 정밀 검증으로 **결함 4건 발견·수정**: ①**5-B IMX219 링크 404**(게다가 BeagleY-AI=다른 보드 문서)→디바이스마트 구매처+BBAI-64 공식문서 분리, ⚠️BBAI-64는 IMX219 기본 비활성=`k3-j721e-sk-rpi-cam-imx219.dtbo` 오버레이+TI `edgeai-gst-plugins` 필요 ②🚨**Kria KV260 카메라 미포함**(보드만, AR1335·microSD·케이블 별매)→**Basic Accessory Pack +$59** 추가 ③🚨**IMX477 렌즈 필수**(없으면 촬영 불가)→실구매 $50→**$75~100** ④§3 Kria 링크가 Luxonis로 오연결→교체. + **부속품 동반구매 총정리 표 신설**. ✅**중국칩 기준=칩(SoC) 원산지 확정**(대표님)→**Rubik Pi 3 유지**(퀄컴 QCS6490, 보드사 Thundercomm 중국합작은 무관). ⭐⭐**리모트IO=PET-7255 2식 확정**: ⚠️기존 근거였던 `IO.csv` 640채널이 **우리 공장이 아님**(컨베이어·리프트 물류라인 청사진, `AJIN_SIMULATION=True` 미사용)→"아날로그 0개"도 함께 무효 → **실물 재산정=입력 10~17/출력 12~18**(1식 8/8은 출력만으로 초과)→**2식 DI16/DO16**, 사업비 마감 후 자비라 넉넉히가 정답. 📮**발주 문의 발송**=예승님(안전신호 dry/wet·E-Stop 물리접점 여부, ⚠️발주 차단 아님)+최동호 사원(견적 **PoE스위치까지 한 견적**=정산·호환 책임 일원화). 🔥**발주 병목=건조기 온도 제어 방식**(우리가 정할 설계 사항, 권고=온오프 확정→PET-7015 1대). → `memory/project_hwaseong_support_program_0716.md` §7/27

> ⭐⭐⭐ **7/27 리모트IO 2식 근거 확정 + 발주 리스트 최종** — **예승님 회신**: 비상정지=티칭펜던트 전용(외부는 프로그램 정지/일시정지까지) / **wet·PNP·24V** / 안전신호는 **로봇 컨트롤러 안전 전용 포트** 권장 / 한솔은 아진엑스텍 보드, **"오리누는 채널 적어 굳이 필요한가"** 의문 제기 / 네트워크=상위PLC↔로봇 같은 대역. → ⭐⭐**한화 매뉴얼 확보·정독으로 정량 검증**(`/home/jtm/docs/robot_manuals/`, 상세 `memory/reference_hcr_manuals_0727.md`): **세대=10L 3세대·12A 2세대**(서로 다름) / **I/O 16/16 PNP + 아날로그 2/2** / ⭐⭐⭐**예승님 의견이 문서상 뒤집힘 = 로봇 I/O 이미 만석**(12A 프린터4대로 `D_GEN_IN_0~7` 8채널 소진·세척/경화 4채널, 10L 그리퍼4+스핀들2) + **안전 I/O 이중화 필수**(에러 221000~224000, 기능당 2채널). **대표님 전·후공정 통합 방침**까지 얹으면 건조기(DI2·DO3~5·AI1)+타워램프(DO4)+세척제어(DO2~4) 자리 없음 → **PET-7255 2식 확정**(근거가 "미래 대비"→**"지금 이미 부족"**). ✅Modbus 맵 확정판(130 명령·131~135 파라미터·150/151·200/206 + 8단계 핸드셰이크, 로봇 IP `192.168.100.241`). 🛒**발주=7/24 수량 그대로 + 부속품 2건 추가**(Kria 액세서리팩 $59 / IMX477 렌즈 $25~50). 🐛한글파일명 NFD/NFC 함정(bash 실패·python 정상).

> ✅ **7/27(월) 지난주 주간보고 Notion 게시 완료** — `📌 주간보고 · 정태민`(`3a6a7d640dab810b87f7cdeb5e781ffe`). 성과 4건=①빈피킹 카메라 촬영 파이프라인 복구 ②RGB 융합·ACE2 캘리브(RMS 0.546px) ③구매물품 조사→미팅→최종 리스트 대표님 보고 ④로봇 티칭 2차 교육. ⚠️**자동생성 페이지에 6월 W24 옛 내용이 남아 있었음**(템플릿 이월)→전면 교체. 🐛**Notion 한글 자동변환 버그가 `old_str`(검색어)까지 깨뜨려 매칭 실패를 유발**하는 신규 양상 규명(해결=깨지는 단어 안 쓰고 주변만 매칭) + 여러 교체 동시 전송 시 **일부 조용히 누락**→매 회 fetch 검수 필수. IRIS 연구노트는 태민님이 직접(웹 Claude). → `memory/feedback_weekly_report_bilingual.md` + `memory/project_notion_company_pages.md`

상세·**전체 참고 링크**: `memory/project_hwaseong_support_program_0716.md` §7/23 + `docs/archive_purchase_202607/구매물품_조사_0723.md`

---

## 🥇🥇 빈피킹 궁극 목표 — Depth+RGB 무조건 연동 + eye-in-hand + 그리퍼 파지 (2026-08-21)

> **태민님**: *"**Depth랑 RGB는 무조건 연동**이야. 두개를 같이해서 **카메라 위치를 로봇 암에 달아서
> 로봇좌표를 통해 로봇 그리퍼가 우리 부품을 집을 수 있게** 하는게 **궁극적인 문제**고,
> **가장 중요한 말**이야."*

**네 조각** = ①**Depth+RGB 융합(무조건)** ②**eye-in-hand**(카메라를 로봇암에)
③**hand-eye**(로봇 좌표로 변환) ④**그리퍼가 실제로 집는다**

### 🚨🚨 내가 반복해서 이 목표를 "범위 밖"으로 밀었다

8/5 회의(*"로봇 제어 전부 제외"*)를 근거로 **hand-eye·파지를 계속 후순위로** 내렸고,
8/20~8/21엔 **RGB 융합까지 *"우선순위 하향"*** 으로 판정했다.

⭐⭐ **판정 근거는 데이터였지만 재는 자를 잘못 골랐다:**
- 내 자 = *"C 23건 중 RGB 후보 4건(0.8%)"* ⇒ **인식 F1 개선 효율**
- 🥇 **태민님 자 = "그리퍼가 집을 수 있는가"** ⇒ **RGB는 효율 문제가 아니라 전제**

⇒ ⭐⭐ **"사업 범위"와 "프로젝트 궁극 목표"는 다른 축이다.**
8/5는 **이번 사업 산출물의 경계**를 정한 것이고, 이것은 **빈피킹이 결국 무엇을 해야 하는가**다.
**둘을 섞어 목표를 깎지 말 것.**

📌 **8/5 이후에도 살아있는 근거** = 대표님 7/20 *"빈피킹 속도 내라"* · 북극성 *"로봇이 빈피킹 잘함"* ·
⭐**기획서 완성물 ②에 *"스핀들·그리퍼 공정 시퀀스 제어"* 가 문자로 있다.**

### ✅ 선행조건 실측 (8/21) — A~C는 이미 끝났다

| 단계 | 내용 | 상태 |
|---|---|---|
| **A** | ACE2 렌즈 + intrinsic | 🟢 7/20 완료 (RMS 0.546px) |
| **B** | Blaze↔ACE2 extrinsic | 🟢 7/28 완료 (산포 5.29mm · `[+44.7,-0.5,-2.1]`mm·1.78°) |
| **C** | RGB-D 오버레이 정합 | 🟢 7/31 완료 (각도 바꿔도 유지 = extrinsic 확증) |
| **D** | **모델에 RGB 입력 경로** | 🔴 **0건** · 🚨**재학습 필수**(아래) · 📅**8/24(월) 촬영 착수** |
| **E** | **hand-eye**(카메라↔플랜지) | 🔴 미착수 — 로봇·IPC 필요 |
| **F** | 그리퍼 실장 + 파지 시험 | 🔴 미착수 — 그리퍼 미장착 |

⇒ ⭐⭐ **하드웨어·캘리브 비용은 이미 지불됐고 남은 것은 D·E·F다.**

### 🚨🚨 D단계 전제가 **두 번** 무너졌다 (8/21 → 8/24) — 둘 다 확인 부족

| 시점 | 내가 믿었던 것 | 실제 |
|---|---|---|
| ~8/20 | *"90장에 RGB가 동봉돼 있어 재택 가능"* | 🔴 **RGB 실측 0건**(npy는 depth 단독 2D · png는 **depth 컬러맵 렌더** 1696×960) |
| ~8/23 | *"데이터만 찍으면 모델에 붙일 수 있다"* | 🔴 **모델이 못 받는다**(아래) |

**[8/24 체크포인트 직접 확인]**
```
T100_csblur_lr1e4_ep80/best.pt
  input_mode = zv
  backbone.stem.0.block.0.weight = (32, 2, 3, 3)   ← 입력 2채널로 물리적으로 박힘
```
모드 표(`depth_vq_detector/depth_preprocess.py:16`) = `z`(1)·`zv`(2)·`xyzv`(4)·`xyznv`(7)
— **전부 depth 파생이고 RGB 모드가 없다.** 2채널 = 정규화 depth + 유효 마스크.

⇒ 🚨 **RGB를 넣으면 첫 conv shape이 바뀌어 기존 체크포인트를 못 읽는다 = 재학습 필수.**
**8/21 화이트리스트처럼 후처리로 이득을 얻는 경로가 아니다.**

⭐⭐ **그래서 촬영을 2회차로 나눈다** — 재학습이 필수면 찍은 것이 **학습셋**이 되고,
그걸로 성능을 재면 8/20에 밟을 뻔한 **train-on-test**를 반복한다.
**세션 A(60장·학습) / 세션 B(30장·시험지)**, 중간에 카메라 재설치로 cross-session 성격 확보.
🚨 **B는 학습에 절대 넣지 않는다** · 🚨**8/18 90장도 재학습 C의 학습 데이터라 시험지로 못 쓴다.**

📌 **교훈 = "데이터가 없다"까지 확인하고 멈췄고 "모델이 받을 수 있나"를 안 봤다.**
같은 질문의 **양쪽 끝**(입력 데이터 / 모델 입력층)을 다 봐야 전제가 선다 → [[never-state-unverified]]

**D단계 실제 작업량** = ①동시 촬영(공장) ②역방향 정렬 함수 신설
(🚨현행 `rgbd_fusion.align_depth_to_ace2()`는 **depth→ACE2 격자** 방향뿐이라 **컬러가 안 나온다**.
필요한 건 반대 = 투영된 uv에서 ACE2 픽셀 샘플링. ⭐`project_points()`가 이미 uv를 주므로 빠진 조각은 작다)
③모드 표에 `zvrgb`(5채널) 추가 + `make_depth_input()`에 rgb 인자
(🚨**RGB는 자체 정규화 필요** — depth는 프레임별 median/p05-p95라 그대로 이어붙이면 스케일이 안 맞는다)
④A100 재학습 ⑤세션 B로 판정

### ⚠️ 정직하게 남겨둘 리스크

🚨 **KAIST 경고가 여전히 유효**하다 — *"RGB 만능 아님. **표면패턴(유형B)에만 결정적**"*.
우리 부품은 **전부 회색 단색 레진**이라 **RGB 이득이 작을 수도 있다.**
⇒ 그런데 그건 **붙여봐야 아는 것**이고 *"안 붙여보고 순위를 내리는 것"* 과는 다르다
⇒ **D단계를 실제로 측정해서 판정한다.**

상세: `memory/project_binpicking_ultimate_goal_0821.md` · ToDo `bin_picking/docs/E2E_TODO.md`

---

## 🥈 빈피킹 북극성 (North Star) — **빈피킹 트랙 안에서의** 최상위 기준

> 🚨 **2026-08-18 층위 교정** — 이 북극성은 **프로젝트 전체의 최상위가 아니다.**
> 태민님 8/18: *"모든 것의 목표의 기준은 대표님 기획서다. 그게 프로젝트의 Goal이야."*
> ⇒ **빈피킹은 Goal 산출물 6개 중 ②「셀B 제어 SW」의 일부**다(위 §🥇 프로젝트 Goal 참조).
> ⭐ **다만 8월 최우선 작업인 것은 맞다** — 인식 F1이 성과 지표이고 ②의 일정이 8~9월이다.

> **2026-05-21 사용자 명시**: "우리의 목적은 학습이 잘 되어서 카메라로 잘 인식해서 로봇이 결국 빈피킹을 잘하는거니까, 성공적인 빈피킹 프로젝트가 되도록 안내해줘. 이 내용은 가장 중요한 내용이지 메모하고 있어."

```
학습이 잘 됨  →  카메라가 잘 인식  →  로봇이 빈피킹을 잘 수행
```

**빈피킹 작업 시작 전 자문**: "이 작업이 카메라 인식 잘 되고 로봇 빈피킹 잘 되는 데 기여하나?"
⭐ **단 그 앞에 한 단계가 더 있다** = *"이 작업이 기획서 산출물 6개·정량목표 5개 중 무엇에 기여하나?"*

**중간 산출물은 다 수단**: mAP 수치, annotation 양, 모델 크기, 코드 라인 수 → 모두 목적이 아닌 수단

상세 (Tier 1/2/3 분류 + 함정 5가지 + 진행률 표): `memory/project_binpicking_north_star_0521.md`

---

## ⭐ 빈피킹 일정 재정렬 (2026-05-22 대표님 통화)

**핵심**: 빈피킹 실 시연 = **가을 (9~10월)** 협력사 페이스. 그동안 우리 = 학습 + 카메라 완성도.

### 단계별 일정 (재정렬)
| 단계 | 목표 | 마일스톤 |
|------|------|--------|
| A. 학습 | v2/v3 완성 + 7클래스 통합 + 도메인 매칭 | 5~7월 |
| B. 카메라 인식 | mAP50 0.95+, 실전 occlusion 검증, 라이브 30fps | 6~8월 |
| C. 좌표 출력 | 6요소 YAML/JSON + 한솔 인터페이스 합의 | 6월 초 |
| D. 로봇 E2E | YOLO → Modbus → HCR-10L | **가을 (9~10월)** |
| E. 실연동 | 공장 빈에서 실 피킹 | **가을 (9~10월)** |

### 우선순위 변경
- **P0 (빈피킹)**: 학습 + 카메라 개발 집중 (실 로봇 시연 약속 X)
- **P1 (전공정)**: 그리퍼 교체 (한솔과 조만간 회의, 별도 트랙)

### 함정 회피 (5/22 신규)
"가을 페이스니까 천천히" 자만 X — 시간 여유 = A/B/C 완성도 끝까지 끌어올리는 시간

상세: `memory/project_binpicking_timeline_realignment_0522.md`

---

## ⭐ 전체 프로젝트 그림 — 전공정/후공정 분리 (2026-05-26)

**시연 순서 (대표님 방향성)**:
1. **전공정 시연 1순위** = 한솔코에버 그리퍼 교체 (빌드플레이트 픽업) → 출력→세척→경화 E2E
2. **후공정 시연 2순위** = 빈피킹 (가을 9~10월, 협력사 페이스)

**우리 6~8월 미션**: 학습 모델 도메인 일반화 — "수치 자랑"이 아니라 "진짜 현장에서 통하는 모델"

| 공정 | 장비 | 우리 책임 |
|------|------|---------|
| 전공정 | 3D 프린터 4대 (Form 4) + 세척기 2대 + 경화기 | 데이터 수집 + 불량/완료 감지 모델 |
| 후공정 | 빈피킹 비전 + HCR-10L 협동로봇 | 카메라 인식 + 6요소 좌표 출력 |

## ⭐ 데이터 수집 카메라 전략 (2026-05-26)

**핵심**: 데이터 수집 = 핸드폰, 운영 = 전용 카메라. 빈피킹 v2가 검증한 패턴 그대로 전공정에도 적용.

| 단계 | 카메라 | 이유 |
|------|--------|------|
| **데이터 수집 (Phase A)** | **핸드폰** | 다각도/다거리/다조명 즉시 다양화, 빈피킹 v2 mAP 0.99 검증, KAIST 팀원 4명 동시 분담 |
| **운영 — 세척기/경화기** | MaixCAM ($40~50/대, 1 TOPS NPU + WiFi 6 + MQTT) | 온디바이스 추론, 24시간 무인 |
| **운영 — 프린터 4대 timelapse** | 외부 USB/IP 카메라 + 라즈베리파이 4세트 (~$300) — 권고 | 1~4시간 timelapse 화질 우위 |
| **운영 — 빈피킹** | Basler Blaze + ace2 (eye-in-hand) | depth + RGB 동시 |
| **학습 finetune (Phase B)** | MaixCAM 시점 100장씩 추가 | 도메인 갭 깸 |

**5/26 사용자 지시**: 전공정 학습 데이터도 **바로** 모아야 함. 후공정/전공정 병행 수집. 5/30 사무실부터 핸드폰으로 세척기/경화기 LED 상태 시작.

상세 (필요 데이터 분량, 시나리오, 메타데이터 스키마, KAIST 연결 액션): `memory/project_data_collection_long_term.md`

---

## 🔁 보고 체계 — 주간보고만 (2026-08-21 확정) ⭐⭐⭐

> 🚨 **태민님 8/21**: *"**일일보고는 작성 안 하고**, 작성한 **주간보고를 각자 공유하면서
> 매주 월요일 2시에 회의**를 해."*

| 항목 | 내용 |
|---|---|
| **일일 업무일지** | 🔴 **작성하지 않는다**(이전 "매일 퇴근 전 5분" 루틴 폐지) |
| **주간보고** | ✅ **유일한 정기 보고물** — 한국어 + 영어 둘 다 |
| **회의** | **매주 월요일 14:00** — 각자 **주간보고를 공유하며** 진행 |

⇒ ⭐⭐ **주간보고가 회의 자료 그 자체다.** 읽는 사람은 **대표님·타 부서**이므로
**우리 내부 용어를 쓰면 뜻이 통하지 않는다**(아래 ③).

### 🚨 작성 방식 5가지 (반드시 이대로)

**① 중요도 순서** — 🚨일자별 나열 금지. 판정 기준 = **기획서 산출물 6개·정량목표 5개 기여도.**

**② 핵심만 + 결과 숫자** — *"무엇을 했다"* 로 끝내지 말 것.
⭐⭐ **안 된 것도 *"안 됐고, 그래서 어떻게 했다"* 까지**(실패가 곧 판단 근거).
🔴 과정 나열·도구명·커밋 해시 제외.

**③ 🚨 내부 용어 금지** — 판정 = **처음 보는 사람이 사전 없이 읽히나.**
`부품 화이트리스트` → **공정에 사용하지 않는 부품을 인식 결과에서 제외** /
`임계값 조정` → **인식 민감도 조정** / `cross-session` → **학습 때와 다른 환경** /
`TTA` → **같은 장면을 여러 배율로 반복 인식하는 기법** / `RGB` → **컬러** /
`F1` → **인식 정확도** / `상주 프로세스` → **상시 대기 방식** /
`E2E 러너` → **한 번의 실행으로 끝까지 처리하는 프로그램**

**④ 🚨 넣지 않을 것** = ***"이제 와서 왜 했냐"* 나올 항목**(빼거나 "버그 수정" 정도로만) ·
**재택/사무실 근무 형태**(박람회처럼 일정 자체가 업무인 것만) ·
**내부 일정 병목·협업사 지원 범위**(리스크는 **업무 자체의 불확실성만**) · 구매·행정 진행

**⑤ ✅ 검증된 포맷** (8/21 W33분, 태민님 *"지금 포맷 좋다"*)
항목 = `**N. [무엇을] — [핵심 결과 숫자]**` + 불릿 2~4개
1. **제목에 숫자 두 개** — 🚨하나만 쓰면 오해된다(정확도 0.5998만 쓰면 *"60%밖에"* 로 읽히니
   **파지 확률 95.5%를 병기**)
2. ⭐⭐ **두 숫자가 다르면 왜 다른지 설명** — **위치 정확도 97.5%** → **이름은 벌림 폭만 결정**
   → **실사용 491→577개** 3단계
3. ⭐ **성과 간 인과 밝히기** — 안 하면 *"많이 했는데 성과 없음"* 으로 읽힌다
4. **한계를 항목 안에 같이** — 숨기면 나중에 더 큰 문제

⭐ **작업량이 성과인 항목**(촬영 90장·라벨 630개)은 **설계 의도**를 함께.
**리스크 3개 정도** + 각 항목에 **대응 계획**.

✅ **자동생성 템플릿 오염 해결(8/21)** — 매주 옛 내용이 남던 원인은
**개인 템플릿에 6월 본문이 박혀 있어서**(자동생성이 복제). ⭐**매주 지운 건 복제 결과였고
원본은 그대로**였다 = 증상만 지웠다 ⇒ 템플릿을 빈 양식으로 교체.

### 🚨🚨 ⑥ 발행 전 자기검산 (이게 없으면 위 5가지가 지켜지지 않는다)

🚨 **8/21 W33분이 좋게 나온 것은 태민님이 네 번 지적해준 결과다** —
*"우리만 아는 단어 쓰지 마"* / *"이제 와서 왜 했냐 나올 항목 빼라"* / *"재택·사무실 빼라"* /
*"재촬영·학습도 넣고 이유와 파지 확률도 넣어라"*.
⇒ ⭐⭐ **내 초안은 네 번 미달이었다.** *"규칙을 읽는 것"과 "적용했는지 확인하는 것"은 다르다*
(오늘 확인 없이 6번 틀린 것과 같은 계열) ⇒ **발행 전에 반드시 대조한다.**

**핵심 5개**(전문 14항목은 memory 파일):
- 🚨 **지표가 하나뿐인 항목** — *"이 숫자만 보면 나쁘게 읽히나?"* 자문 → 보완 지표 병기
- 🚨 **두 숫자가 다른데 이유를 안 쓴 곳** — 다르면 반드시 왜
- 🚨 **"많이 했는데 결과가 작은" 항목** — 다른 성과와의 **인과**를 붙인다
- 🚨 **내부 용어 전수 검색** — 화이트리스트/임계값/cross-session/TTA/RGB/F1/게이트/E2E…
- 🚨 ***"이제 와서 왜"* 나올 항목 · 근무 형태 · 내부 사정** 제거

⭐ **초안을 보여드릴 때 체크리스트 결과를 함께 말한다** — 그러면 매번 지적하지 않아도 된다.

### 📓 IRIS 연구노트 (8/21 확정)

**작성 채널 = 웹 Claude**(회사 주간보고만 Notion MCP 직접). Claude Code는 **자료를 만들어 전달**한다.

**① 파일** = **`M월 N주차 IRIS 연구노트.md`** — 🚨**월 기준 주차**(내부 `W33` = **8월 3주차**)
**② 구성 = 요약본 + 전체본 한 파일에** — 🚨*"요약만 주면 빈약해서"* 태민님이 매번 CLAUDE.local.md를
**복붙**했다 ⇒ **1부 요약본**(중요도 순·수치 포함) + **2부 전체본**(CLAUDE.local.md 해당 주 구간 **그대로**)
⭐ 근거 = *"요약은 숫자를 흘린다"*(8/19 실측) · IRIS는 **경위와 실패 과정이 곧 내용**
**③ 이후** = 태민님이 *"이번주 끝났어. CLAUDE.local.md 주간 단위로 요약해줘"* 라고 하면
→ ①**md 삭제** ②**주간 압축**(직전주=요약블록 / 오래된 것=간결 / **task 바뀐 것=아주 간결**,
근거 = *"어차피 memory에 다 있다"*)
🚨🚨 **절대 조건 = 다음 주 작업이 끊김 없이 이어질 것** — **진행 ToDo·선행조건·다음 주 계획·
회신 대기·미해결 리스크·되풀이 금지 교훈**은 남긴다. 📌**압축 후 수치 토큰 대조**(8/19 규칙)

상세: `memory/feedback_weekly_report_bilingual.md` · `memory/feedback_report_tool_division_0619.md` ·
`CLAUDE.local.md` § 업무관리 워크플로우

---

## ⭐ Notion 마스터 페이지 — Robot Arm Factory (2026-05-26 전면 갱신)

> ⚠️ **2026-06-09 개편 → 2026-08-21 재확정**(위 § 보고 체계 참조 = **일일 폐지·주간만**).
> 아래 Robot Arm Factory 페이지는 프로젝트 개요용으로 유지.

**위치**: `notion.so/orinu/Robot-Arm-Factory-60d973ebb99942c8852017d39d58e6f6`
- 부모: orinu HQ > Physical AI Engineering Hub
- 5/26 MCP 복구 + 일괄 갱신 완료

**구성**:
- 본문 callout 8섹션: 전체 프로젝트 구조 / Hardware / Phase 별 현황 / 후공정 빈피킹 학습 진척 / 데이터 수집 전략 / KAIST / 향후 로드맵 / Operational
- Plans & Roadmap DB: 9 rows (Phase 1~3 완료 + 전공정 그리퍼/시연 + 후공정 학습/카메라/좌표/시연)
- Tasks DB: 20 rows (5/26~28 P0 6건 + 5/29 P0/P1 6건 + 6월~ 백로그 8건)
- Issues & Risks DB: 10 rows (도메인 갭 High + Part5 정체 + ACE2 + IPC-510 + 등)

**명명 규칙 (사용자 5/26 결정)**:
- ✅ Notion = **"한솔코에버"** 실명 OK
- ❌ GitHub/README/외부 출력물 = "협력사" 유지 (보안 원칙 그대로)
- 메모리/CLAUDE.md = 자유

**Notion MCP 연결**:
- VSCode `claude.ai Notion` MCP 활성
- 401 떨어지면 `claude.ai/directory/connectors/...` 에서 "연결 해제" → "연결" 재시도 (VSCode Disable/Enable만으론 안 됨)
- 상세: `memory/project_notion_mcp_setup_pending.md`

---

## ⭐ 빈피킹 AI 프레임워크 결정 (2026-05-22)

**결정**: PyTorch + Ultralytics (TensorFlow 안 씀)

**파이프라인**:
```
[학습]  AICA A100 (PyTorch 2.1 + CUDA 11.8 + Ultralytics 8.4.51)
   ↓ best.pt
[변환]  ONNX (산업 표준, yolo export format=onnx)
   ↓ best.onnx
[배포]  IPC-510 (NVIDIA GPU, ONNXRuntime-GPU)
   ↓ 추론
[출력]  6요소 YAML/JSON (x, y, z, edge, angle, label) — `detect_and_output.py`
   ↓
[통신]  한솔 시스템 → Modbus → HCR-10L
```

**TF 안 쓰는 이유 (대표님 보고 답변)**:
1. YOLOv8/v11 = PyTorch 전용 (한솔 권고와 일치)
2. 산업 표준 = PyTorch → ONNX → 어디서나 배포
3. AICA + 한솔 + 우리 코드 = 모두 PyTorch 진영

**미확정 (다음 단계)**:
- IPC-510 정확한 GPU 모델 확인 (5/29~ 셋업 시)
- v2 학습 결과 분석 (5/23 토 또는 5/26 화)
- ONNX 변환 + 한솔 인터페이스 합의 (6월 초)

상세 (대표님 추가 질문 답변 + 다른 옵션 비교): `memory/project_yolo_framework_decision.md` + `memory/project_ai_deployment_landscape.md`

---

## 대표님 피드백 (핵심 결정사항)

> 시간 순이 아닌 **주제별 결정 흐름**으로 정리. 구식 결정도 "왜 정하고 왜 바뀌었는지" 보존.

### 1. 개발 방향성 (2025.01~2026.02)

**자체 개발 병행 결정 (2025.01.28~30)**
- 한솔 의존도 낮추기 위해 자체 Web API 방식 병행 개발 + 모바일 모니터링 강조
- 목표: 설 전 API 구축 완료 → ✅ Phase 1 완료

**공장 PC 설치 + SaaS 방향 (2026.02.04)**
- 공장 PC 설치 확정 (Linux 의도였으나 실제는 Windows로 진행됨)
- SaaS 플랫폼 구축 예정
- 세척기/경화기 완료 감지 = OpenMV 카메라 (02-06 확정) → **2026.04.14에 MaixCAM으로 전환** (마일스톤 표 참조)

**2/12 데모 후 PreForm 동등 구현 지시**
- 슬라이스 미리보기 / 예열 / 프린트 시간 / 일시정지 + 4대 독립 컨테이너 + 탭 구분
- 결과: ✅ Phase 2 5탭 UI 완성 (2/27 기준)
- 서버: 5090 운영(폐기 예정) + 8085 개발 → 5090 폐기 후 카카오 VM으로 이전 (2/26 결정)

### 2. 한솔코에버 협업 구조 변경

**소스코드 공유 결정 (2026.02.26)**
- 원래 2/24 미팅에서 "소스코드 공유 X, 가이드라인만"으로 결정 → **2/26 변경: 소스코드 공유**
- 이유: 한솔 작업 효율 + Phase 전환(인수인계 후 OpenMV 개발) 일정 단축
- AICA A100: 한솔 3월간 1대 필요 → 근형님 전달 완료

**협업 담당자 변경 흐름**
- ~~김기원 주임 (`justkiwon`) + 이나라 주임~~ — 김기원 PR #3 제출(3/24) 후 `hansol-dev` 브랜치 전환(3/25, `591b95a`)
- **2026-04-03 김기원 주임 퇴사** — 코드 docs 폴더 역할별 요약 보유. 직접 지원 불가, 구조/플로우 문의는 가능
- **2026-04-29 사고**: 공장 PC origin이 `justkiwon/3D_printer_automation` (퇴사자 fork) 가리켜 100커밋 차이 발생 → 원격 복구 진행
- **현재 협업 담당자**: 이예승 사원 (한솔코에버, GitHub: `eseung97@gmail.com`, 연락처: 010-4946-3610)

**한솔 머지 이력** (전체: `memory/project_hansol_merge_issues.md`)
| 회차 | 일자 | 커밋 | 내용 | 비고 |
|------|------|------|------|------|
| 1차 | 4/3 | `9c161dc` | 김기원 주임 코드 (sequence_service / AutomationPage / automation_db.py) | 인코딩 깨짐 복원 |
| 2차 | 4/16 | `e68c2b1` | 자동화 CMD 프린터 할당 콤보박스 + 공용 큐 버그 수정 | App.tsx mojibake 차단 |
| 3차 | 4/23 | `9f97f1e` | 경화기 2→1대 축소 (Cure 2 비활성화) | 인코딩 깨끗 / `runtime.py:121` 후속 패치 미커밋 |
| 4차 | 5/6 | `b9164d9` | 시뮬 토글 + 프린터 별명 매핑 | — |
| 5차 | 5/6 | `9fd365a` | `cell_state.simul_mode` 컬럼 자동 마이그레이션 (4차 누락분 보강) | 3개 서버 배포 완료 |

### 3. 빈피킹 (Phase 5) 방향 결정

**3D+RGB 카메라 조합 결정 (2026.04.03)**
- Blaze-112(ToF) 단독 vs ace2(RGB) 조합 → **조합** 결정
- STL 수집 결과 (4/6): 55개 다운 → 중복 제거 → bbox 분석 → **29종** 확정 (목록 자체는 미확정, 킵고잉)

**카메라 배치 — 4/10 1차 결정 → 4/23 한솔 회의 재합의**
- 4/10 대표님 피드백: "1대 고정(eye-to-hand) + 1대 로봇암(eye-in-hand)" + 시각화 요청 (실패 케이스 이미지 첨부)
- 4/23 한솔 3자 회의 (김주엽 파트장 + 이예승): **eye-in-hand에 Blaze + ace2 2대 동시 마운트**로 합의 (대표님과 일치)
- 5/6 회의에서 카메라 브라켓 확정: 코에버 설계 → 오리누 3D 출력 → 검증 후 철제 가공

**빈피킹 좌표 — 6DoF → 4DoF 단순화 (2026.05.06 한솔 회의)**
- 이전: 6DoF 오일러 ZYX 가정으로 다면 처리하려고 했음
- 변경: **X, Y, Z, Theta (4DoF)** + 다면은 자세 분리(A자세/B자세) + 리그립으로 해결
- 블로커: 한화 로보틱스 별도 라이브러리/패키지 = 한솔 이예승 ASAP 확인. 답 받기 전 좌표 출력 코드 확정 금지

**대표님 5/6 빈피킹 개인 지시 4가지** ⭐ — `memory/project_binpicking_ceo_directive_0506.md`
> 로봇 장착은 추후로 미루더라도 인식 자체를 먼저 확실히

1. **Basler 카메라 로컬 테스트 우선** — IPC-510/로봇 셋업 기다리지 말 것 → 5/8 진행 중 (어댑터 도착 대기)
2. **공장 실물 부품 다각도 촬영 + 학습** — CAD-only FPFH/ICP 한계, 실데이터 학습 필요. 부품 1종당 수백 장
3. **X, Y 각도(뒤집기) 데이터 고민** ⭐
   - 안정 자세 enumeration (CAD bbox + COM)
   - 자세별 그래스프 가능 여부 → grasp_database.yaml 자세별 확장
   - regrasp 시퀀스 정의
   - 학습 라벨링 형식 (부품 ID + 자세 클래스 + 6DoF pose)
   - **코드 짜기 전 명세 초안 → 대표님 align**
4. **예승님 연락** — (a) 빈피킹 좌표 명세 (좌표계/단위/회전/그리퍼/시퀀스) (b) 바텀비전 홀 검출 소스코드

**5/6 한솔 3자 회의 추가 안건** — 상세: `memory/project_meeting_0506_hansol.md`
- 다면 인식 정식 안건화 (리그립 스테이션 vs 시퀀스 분할 A/B자세)
- Formlabs API 무인 운전 불가 — 공식 확인. 출력 종료 후 수동 터치 체크리스트 필수
- 그리퍼 교체 요청 (한솔) — 잔여 레진 제거 어려움
- 3D 프린터 하단 파손 270만원 분담 협의 중 (⚠️ **6/15 추가 발견**: Form 5대 중 3대 광학 판(LCD/LPU 윈도우) 균열 → 출력 불가, 멀쩡한 2대(ShrewdStork+Gecko)만 가용. 대표님/예승님 보고 필요 = 이 파손 이력과 묶일 수 있음 → `memory/project_factory_work_0615.md`)
- 삼성전자 ~2억 규모 하반기 지원사업 참여 준비
- 카카오톡 단톡방 운영 (5/7 입장 완료)

### 4. HCR-10L 로봇 연동 (2026.04.14 교육 1회차)

- **한화 HCR 개발 특성**: 두산/현대와 달리 **펜던트(Rodi) 중심 개발**. 외부 PC는 Modbus 레지스터 간접 제어
- 비전PC가 Modbus 레지스터에 피킹 좌표 쓰기 → 펜던트 프로그램이 읽어서 모션 실행
- **사용자 사용 가능 레지스터: 130~255** (문서상 128~이나 실사용 130번부터)
- **좌표계**: Base(로봇 바닥 기준) vs TCP(그리퍼 끝단 기준)
- **TCP 좌표 Modbus 읽기**: Register 400~405 (1/10mm, 1/10deg, 16bit 정수)
- **TBD**: TCP 오프셋, 작업 영역, 오일러 컨벤션 (그리퍼 장착 + 빈 배치 후 실측)
- 자료: `PLC_Cobot_Modbus_Guide.pdf` (34p, 예제 3건)
- 4/15 Modbus 재설계: FLOAT32(40001~) → **INT16(130~140 비전PC→로봇 / 150~151 로봇→비전PC)** (`a13b5ce`)
- 상세: `memory/reference_hcr_user_education.md`

### 5. 산업용 PC (IPC-510) 카메라 구성 (2026.04.10 / 4/23 입고)

- **카메라 최대 6대 구성**: Bottom Vision 1대 + 빈피킹 2대(Blaze-112+ace2) + 3D프린터/경화기 모니터링 1~2대 + 양손 로봇(추후) 1대
- **스펙 우려**: GPU 5060 + RAM 8GB → 6대 동시 처리 버거울 수 있음
- **대안**: 젯슨 나노로 분산 가능성
- **현재 (5/8)**: 4/23 입고 완료, 셋업 미시작. HCR-10L 빈피킹 전용 (4/29 합의). HCR-12는 공장 PC 잔류
- 상세: `memory/project_robot_pc_assignment.md`

### 6. Phase 4 — OpenMV → MaixCAM 전환 (2026.04.14)

- 원래 OpenMV AE3로 세척기/경화기 완료 감지 (2026.02.06 확정)
- 4/14 대표님 지시로 MaixCAM 전환 — RISC-V + 1 TOPS NPU + 4MP, Cloud 없이 온디바이스 AI
- 보유 장비: MaixCAM 1대 + LicheeRV Nano 2대
- 우선순위: 빈피킹 우선, MaixCAM은 여유 시 PoC
- 역사: `memory/project_openmv_image_capture.md` (3/16 시도 → 4/14 전환)

---

## Phase별 개발 계획 (확정)

| Phase | 항목 | 우선순위 | 상태 (2026-05-26) |
|-------|------|----------|------|
| **Phase 1** | Web API 모니터링 | 🔴 URGENT | ✅ 완료 |
| **Phase 2** | Local API 원격 제어 + 프론트엔드 UI | 🔴 URGENT | ✅ 완료 (5탭 UI + JWT 인증 + 3개 서버 운영) |
| **Phase 3** | HCR 로봇 연동 | 🟡 HIGH | ✅ 한솔 머지 5차 완료. ✅ **7/22 예승님 2차 교육 완료**(로봇 티칭+Modbus TCP, 오전만·오후 반차). 주희님 정리본으로 **펜던트 프로그래밍 UI 조작법 확보**(위치4방식·변수·로직명령·서브프로그램·그리퍼 `D_GEN_OUT_*`/`Reverse` 필수) → [[robot-teaching-0605]] §주희님 정리본. ⏭️ 실 출력+로봇 E2E는 다음 현장 |
| **Phase 4** | 장비 모니터링 (세척+건조 중심) | 🟡 HIGH | ⭐⭐ **7/16 대표님 회의로 범위 재정의**([[ceo-meeting-0716]]): 로봇 자동화=**프린터+세척+건조**(건조 신규), **경화기는 자동화·Vision 대상 완전 제외**(ROI 안 나오면 사람이). 카메라=**OpenMV N6 전환**(AE3 폐기, 대표님 발주). 세척기 **idle/complete=빌드플레이트 유무** 확정. ⭐⭐**모델 방향전환=완료/대기는 OCR 아닌 영상분류**(idle/complete=형상 문제, running만 OCR/카운트다운). **7/16 진행**: MaixCAM 세척기 재촬영·크롭·분류→**분류 PoC 실행 완료**=같은세션 resnet18 F1 0.982(7종 비교 최고)지만 ⚠️**cross-session(다른날 테스트) 0.34~0.70 폭락=배경외우기, 지금 데이터로 실공정 배포 불가**. 데이터 진단=전처리(7/14통짜vs7/16크롭)+양(세션2개뿐) 둘 다 부족. 실공정 지표=cross-session macro-F1+per-class recall. 필요=여러세션 재촬영+crop통일. (태민님 지시 4=모델다돌려최고·데이터진단·최고모델기억·실공정지표) ⭐⭐**7/17 = 최고모델 resnet18 정식 학습·저장(이번주 마무리, best.pt=A100 wash_model_0717) + 분류모델 방향 뒤집힘**: 돌린 8개(수제특징5+딥러닝3) 결과가 알려준 것=①병목은 모델 아닌 데이터(모델 더 돌리기 무의미) ②cross-session만 진실 ③딥러닝이 얕은모델 대비 3%p뿐=문제 단순(상태3개=이진질문2개 뚜껑닫힘/플레이트유무) ④실패는 항상 idle→running(배경 붙잡음) ⑤전처리 섞이면 손해. **결론=분류모델 공정사용 애매→룰베이스+OCR 하이브리드**(running=OCR·카운팅 / idle↔complete=룰베이스 1차·분류 백업 2차, 팀 내 빛반사 룰기반 방향과 일치). **7/14~15 분석**: rapidocr 우승, OpenMV 완료판독 불가·MaixCAM급 필요, 완료판단=타이머(완료 IO 없음)→Vision 용도=검증+카운팅. 세척기 룰기반(빛반사) 가능성. 상세 `memory/project_ceo_meeting_0716.md`+`project_vision_state_detection_0715.md`+`project_hwaseong_support_program_0716.md` |
| **Phase 5** | 3D 빈피킹 비전 시스템 | 🔴 URGENT | ✅ 트랙 2 v2 **5모델 학습 + 분석 완료** (5/22 시작 → 자정 전 종료 → 5/26 분석). **🥇 yolov8n mAP50 0.9939 / 🥈 yolo11s 0.9910 / 🥉 yolov8m 0.9899**. Part2 회복 **v1 0.656 → v2 yolo11s 0.958 (+30%p)** ⭐. Part5 0.909 정체 = v3 보강 필요. 우승 후보 = yolov8n(6.3MB) or yolo11s(19.2MB), 5/27 ONNX + 도메인 갭 후 최종. 좌표 6요소 출력 코드 + PyTorch → ONNX → IPC-510 결정 완료. **🔥 5/22 대표님 통화: 빈피킹 = 가을(9~10월) 협력사 페이스, 우리 = 학습+카메라 완성도** — `project_yolo_v2_training_results_0522.md` + `project_binpicking_timeline_realignment_0522.md` |

### v2 5모델 비교 결과 (5/26 시점) ⭐

| Rank | Model | Params | mAP50 | mAP50-95 | Precision | Recall | best.pt |
|------|-------|--------|-------|----------|-----------|--------|---------|
| 🥇 1 | **yolov8n** | 3.2M | **0.9939** | 0.7458 | 0.991 | 0.978 | 6.3MB |
| 🥈 2 | yolo11s | 9.5M | 0.9910 | 0.7446 | 0.964 | 0.979 | 19.2MB |
| 🥉 3 | yolov8m | 25.9M | 0.9899 | 0.7255 | 0.972 | 0.947 | 52.1MB |
| 4 | yolo11m | 20.1M | 0.9868 | 0.7225 | 0.982 | 0.929 | 40.5MB |
| 5 | yolo11l | 25.3M | 0.9842 | 0.7363 | 0.967 | 0.916 | 51.2MB |

**핵심 발견**: 가장 작은 yolov8n이 1등 — 데이터셋 작아서(946 aug) 큰 모델 과적합 경향. **Part2 Recall +30%p 회복** (5/20 공장 멀티 촬영 효과 입증). **Part5 0.909 정체** = v3 보강 필요. 상세 5×5 클래스별 표 + 다음 액션 → `memory/project_yolo_v2_training_results_0522.md`

### v3 학습 계획 (5/27 수립) ⭐

> 사명: **"외운 모델(0.99)" → "현장에서 통하는 모델"** (도메인 일반화 + Part5 약점 해소). 계획서: `bin_picking/yolo_track/V3_TRAINING_PLAN.md`

v2의 두 약점을 v3가 푼다:
1. **Part5 Recall 0.909 정체** (5모델 전부 동일 = 데이터 문제) → Part5 단일/멀티 보강 50~80장 + 클래스 균형
2. **도메인 갭 미검증** (valid도 train과 같은 환경) → 다른환경 100~150장

⭐ **도메인 갭 hold-out 전략** (가장 중요): 5/29 다른환경 촬영분의 30%(30~40장)를 **학습에 절대 미투입** → v2 두 모델(yolov8n vs yolo11s) inference로 **우승 모델 정직하게 확정** + 도메인갭 개선 측정. Roboflow `20260529_holdout_*` 별도 batch, export 제외 (누수 방지 = 생명).

모델 후보: v2 5개 → **v3는 yolov8n + yolo11s 2개 집중** (데이터 작아 큰모델 과적합 교훈). 7클래스(Part6/7 통합) vs 5클래스는 5/29 촬영량 보고 현장 판단. 성공 기준: Part5 Recall 0.95+ / hold-out mAP50 0.90+ / 우승 1개 확정 + ONNX 변환.

**5/27 재택 작업**: 한솔 6요소 YAML 샘플(`sample_output_6elements.yaml`) + 카톡 확정본(성과공유+방향확인 톤, 미발송) + v3 계획서 작성. **ONNX·도메인갭은 보류** (6000 GPU없음+디스크99% / 평가셋 없음 → 5/29 사무실 IPC-510). — `memory/project_binpicking_overview.md` + `bin_picking/yolo_track/V3_TRAINING_PLAN.md`

---

## 📍 주요 버그 fix 2건 (5/29 JWT / 6/1 Formlabs) + 재발 방지 룰

> 일자별 진척 상세는 메모리. 여기엔 **재발 방지 룰**(아키텍처 차원 영구 규칙)만 보존.

**1. JWT 회귀 버그**(5/29, `db6adcf`) — 5/6 JWT 도입 시 sequence_service 클라이언트 Authorization 누락 → 운영 모드 web-api 401 → CMD 픽업 실패. fix=`web-api/app/core/jwt_middleware.py` loopback 면제 → [[jwt-sequence-service-bug-0529]]
**2. Formlabs status stale 버그**(6/1, `044ddb7`) — Cloud API가 이전 `current_print_run.status=FINISHED` 유지, `ready_to_print`만 갱신 → 우리 웹 stuck. fix=`formlabs_client.py` `FINISHED+ready_to_print=READY`→IDLE → [[formlabs-status-stale-bug-0601]]

### 🔐 재발 방지 룰 (두 사고 공통 = 외부 API/내부 호출자 동기화)
- **web-api 인증/미들웨어/응답 스키마 변경 시** 내부 호출 클라이언트 같이 확인: `sequence_service/app/cell/printer_interface.py`, `factory-pc/file_receiver.py`, 향후 추가분
- **외부 API 응답 단일 필드 의존 금지** — stale 가능성 의심, 다른 필드 cross-check, reference client(PreForm 등) 동작과 비교
- **큰 변경/배포 후 검증 루틴**: ① `tasklist|findstr python.exe` 3개+ ② `curl .../local/health`→ok ③ **시뮬 CMD 1회 끝까지**(PRINT_FINISHED→ROBOT_REQ) ④ 로그 401/500 확인. ⚠️ git pull은 안전, **NSSM restart + 운영 모드 첫 진입**이 위험 → [[deployment-verification-routine]]

### 📋 Notion DB 작성 규율 (6/1 사용자 지시, 영구)
- 시작일자 명시 / Priority P0~P3 / **Status 정확히**(예정=To Do / 진행중=In Progress / 완료=Done). 회사 자산+외부 가시성이라 정확성 핵심 → [[notion-task-status-discipline]]

### ⭐ 대표님 빈피킹 2시나리오 (6/1) + 개발 전략
- **① 정렬형**(occlusion 약함) **먼저** YOLO bbox/seg+4DoF로 E2E → **② 적재형**(occlusion 심함)은 seg+depth+grasp로 단계 확장 → [[binpicking-two-scenarios-0601]]
- 본인 생존 전략(지시 아님): **빈피킹 = 회사 ↔ KAIST 연계 개발** (1인+화/목 부재, 두 트랙 한 작업으로) → [[feedback-binpicking-kaist-linkage]]

### 한솔 머지 6·7차 — ✅ 공장 배포 완료
- 6차 `310087d`(시뮬 동결 fix, 6/4) / 7차 `5288396`(robot.py 실 TCP I/O, 6/8). ⚠️ 예승님 커밋은 **personal/hansol-dev**에 올라옴 → 머지 시 personal 먼저 확인 → [[hansol-merge-issues]]

---

## ⚠️ 대표님 5/28 4대 지시 + 5/29 후속 갱신

상세: `memory/project_ceo_directives_0528.md`

1. **외부 시연 일정** — ✅ **5/29 오후 통화로 6/1 (월) 15:00 그대로 확정**. (5/29 오전 한 차례 "연기, 6/2 또는 6/5" 흐름 있었으나 오후 재확정.) **시연 범위 = 펜던트 시뮬레이션 + 웹에서 출력 거는 것까지** (5/29 오후 대표님 추가 지시). **시연 본질 = 로봇 펜던트 모션 시연** (실제 출력 X, 5/28 예승님 답변 그대로).
2. **5/28 예승님 답변으로 시나리오 정정** — 정태민 "감 안 옴" → 예승님 본인 권장 "1회로 위험" → **추가 교육 진행 결정**. **5/29 오후 예승님 공장 방문 + 펜던트 시연 전 과정 직접 설명 완료** = 추가 교육 사실상 달성. 5/29 저녁 본인 직접 리허설 진행.
3. **한솔코에버 협업 = 유지 방향** — 5/29 흐름(오전 1시간 30분 통화 + 오후 공장 직접 방문 + JWT fix 공동 검증 + 펜던트 시연 설명)으로 협업 실질 심화. 5/29 사용자 지시: 종료 관련 표현 앞으로 항상 제외 (`memory/feedback_hansol_termination_phrase.md`). 5/28 잠정 보류 항목은 협업 유지 전제로 진행 모드.
4. **ACE2 C-mount 렌즈 8mm + 12mm 둘 다 자체 구매** — 대표님 직접 결재. 5/8 김주엽 과장 "현장 보유" → 5/15 미수령 → 5/20 미장착 확정 → 자체 구매 결정. **렌즈 결정 측정 = 불필요 (둘 다 구매)** 하지만 본업 작동거리·KAIST 라이브 데모 일정 안전마진용으로 측정 가치 남음. → ✅ **6/15 8mm(C23-0824-5M)+12mm(C23-1224-5M) 둘 다 입고 완료**. 설명서 스펙·렌즈 선택 분석(8mm 유력)·KAIST 검증=ACE2 결정 → `memory/project_ace2_camera.md` § 6/15

### 5/28 잠정 보류 항목 → 5/29 진행 모드 전환 (협업 유지 전제)

| 항목 | 5/28 잠정 조치 | 5/29 갱신 |
|------|----------|----------|
| 한솔 카톡 6요소 메시지 | 발송 보류 (로컬 보관) | 종료 가설과 무관, 내용 outdated 가능성 별도 판단 |
| 그리퍼 교체 회의 (한솔과) | 회의 보류 | 진행 모드 |
| 6요소 좌표 명세 미확정 5항목 | 자체 결정 전환 옵션 검토 | 한솔과 자연스럽게 합의 |
| 한솔 인계 코드 4파일 | 이미 인수, 활용 OK | 그대로 OK |
| 로봇 교육 | 종료 시 그 전에 1회 더 요청 옵션 | 5/29 예승님 공장 방문 = 사실상 추가 교육 |
| sequence_service 머지 | 종료 시 추후 X | 진행 모드 (다음 안정 재택일 PR) |

## ⭐ ACE2 C-mount 렌즈 — eye-in-hand 확정 + 후보 좁히기 (5/28)

⭐ **eye-in-hand 확정** (5/28 사용자 정보) — 로봇암에 브라켓 달아서 카메라가 움직이며 빈 부품 촬영·인식. 5/6 회의 합의 구조 재확인.

상세 보고서: `bin_picking/archive_cards_202605_08/ACE2_LENS_REPORT_0528.md` (5/29 측정 후 최종 확정)

**카메라 사양 정정** (Basler 공식 페이지 5/28 재확인):
- 모델: ace 2 **a2A2448-23gcBAS** (color, GigE)
- 센서: **Sony IMX547** (기존 코드 IMX392·메모리 IMX264 모두 오류)
- 광학 포맷: **1/1.8"** (6.71×5.61mm, 대각 8.75mm)
- 픽셀 피치: **2.74µm** (기존 코드 3.45µm 가정 오류)
- 해상도: 5MP (2448×2048), C-mount

**호환 시리즈 정리**:
| 시리즈 | 최대 센서 | 우리(1/1.8") 호환 |
|--------|----------|------------------|
| C125 | 1/2" | ❌ 비네팅 우려 |
| **C23** | **2/3"** | ✅ 완전 커버 |

**후보 4종 (5/28 검증 완료, 4개 모두 우리 카메라 호환 ✅)**:
- 일반 12mm **C23-1224-5M** : https://www.baslerweb.com/ko-kr/shop/basler-lens-c23-1224-5m-f12mm/
- 일반 8mm **C23-0824-5M** : https://www.baslerweb.com/ko-kr/shop/basler-lens-c23-0824-5m-f8mm/
- Premium 12mm **C23-1224-5M-P** : https://www.baslerweb.com/en/shop/lens-c23-1224-5m-p-f12mm/
- Premium 8mm **C23-0824-5M-P** : https://www.baslerweb.com/en/shop/lens-c23-0824-5m-p-f8mm/

**Premium(-P) vs 일반 차이**: 메탈 하우징 + 조리개/초점 잠금 나사 + 더 정밀한 광학 (왜곡·비네팅 적음). 인식 정확도 약간 유리하나 더 비쌈. 일반도 충분히 사용 가능.

**5/28 대표님 결정**: **8mm + 12mm 둘 다 구매** (고정 위치 변경 대비). 등급(일반 vs Premium)은 대표님 검토 중. **발주는 대표님이 직접** 진행.

**구매처**: 알트시스템(한국 디스트리뷰터) 또는 Basler 한국 공식(`baslerweb.com/ko-kr/`)

**5/29 현장 측정 — 발주 결정과 별개로 v3 학습/캘리브용 여전히 필요**:
- 빈 외형 (가로·세로·**깊이**) / 빈 작업대 높이 / 부품 적재 상태
- **로봇 그리퍼 TCP ↔ 빈 윗면 거리 (최단/최장)** ⭐ — 카메라 실 작동거리
- 그리퍼-카메라 브라켓 실측 (도착 시)
- 알고 있는 기하: 한솔 코드 검증값 = 카메라 광학 중심이 그리퍼 플랜지 위쪽 **~212mm** (우리 그리퍼 교체 후 재측정)

**코드 정정 (입고 후)** — `bin_picking/src/acquisition/basler_capture.py` `ACE2_5MP_SPEC`:
- `"sensor": "Sony IMX392"` → `"Sony IMX547"`
- `"pixel_pitch_um": 3.45` → `2.74`
- `fx`, `fy` 재계산 (선택된 초점거리 / 2.74µm)

---

## 🎓 KAIST 부트캠프 3단계 (6/2~7/9) — ✅ 종료 · 🏆우수상·수료

> 🚨 **끝난 트랙이다.** 6주 일자별 경위(방향 3회 전환·Visual Hull·합성데이터 8000장·sim2real 여정)는
> **`memory/project_digital_twin_synth_data_research_0609.md`** + `memory/project_kaist_final_presentation_0709.md` +
> `CLAUDE.local.md` W23~W27 주간 요약에 보존. 여기엔 **회사 자산으로 남은 것만** 적는다.

### 🟢 회사에 남은 것

| 항목 | 내용 |
|---|---|
| **모델** | **CADENCE** = 3D 인코더(**PointNet++**, CAD→pointcloud) + 2D 인코더(**VQ-VAE의 VQ**, DepthMap) latent 정합 + DETR계열 detector |
| **성과** | 27종 test100 **F1 0.684**(위치 0.88/종류 0.85) · **depth만 입력** |
| **배포 모델** | `T100_csblur_lr1e4_ep80/best.pt` = ⭐**현행 운영 모델** |
| **레시피** | csblur(엣지블러 light) · **lr 1e-4** · ep80 · batch2 · **320×576** · amp false |
| **코드** | `bin_picking/depth_track/`(회사) · `kaist_backup/`(동결) · 6000 `/data/jtm/a100_backup_0710/`(체크포인트 4개) |

### ⭐⭐⭐ sim2real 교훈 (지금도 판단 근거)

**합성만 = 5% 붕괴 → 합성 aug 천장 0.203 → 실측 fine-tune 0.45 → lr·데이터로 0.818(test40) → 정식 test100 0.684**
⇒ ⭐**지렛대는 실측 소량 fine-tune과 lr이었고**, 합성 증강·해상도·threshold·병합·loss는 **전부 천장**.
🚨**병목 = 학습이 아니라 센서 물리**(축대칭·표면패턴) ⇒ **종류혼동 3유형** =
**A** 축대칭·크기미세차(병합) / **B** 표면패턴(**RGB가 결정적**) / **C** 입체돌기(고해상 depth)
⚠️ **병합은 함부로 하지 말 것** — 4쌍 검증에서 **1쌍만 정당**했고(나머지는 크기 근본차로 depth 구분 가능),
7/9에 병합으로 F1이 **0.684→0.669**로 떨어진 전례가 있다.

### 📌 후속 (태민님 직접)

**논문** = "CADENCE: CAD-Aligned Depth Embeddings with Neural Codebook Encoding"(태민님 **2저자**·5인 공저).
🔄**타겟 학회 AAAI 2027(#45391) → IEEE BigData**(조교님 7/29 연락, 마감 8월 후반) — **현재 태민님 액션 없음**.
특허·PoC는 대기.

## 프로젝트 구조

```
3D_printer_automation/
├── CLAUDE.md                    # 프로젝트 상태 문서 (이 파일)
├── CLAUDE.local.md              # 세션별 작업 이력 (git 제외)
├── README.md
├── .gitignore
│
├── docs/                        # 문서
│   ├── Phase1_WebAPI_개발설계서.docx
│   └── Phase2_LocalAPI_아키텍처설계.md
│
├── web-api/                     # 백엔드 (FastAPI) - Phase 1 + 2 통합
│   ├── .env.example             # 환경변수 템플릿
│   ├── data/local.db            # SQLite 데이터베이스
│   ├── app/
│   │   ├── main.py              # 앱 진입점 (lifespan, CORS, SPA)
│   │   ├── core/
│   │   │   ├── config.py        # 설정 관리 (Web + Local API)
│   │   │   └── auth.py          # OAuth2 인증
│   │   ├── services/            # Phase 1: Web API 서비스
│   │   │   ├── formlabs_client.py     # Formlabs 클라우드 API
│   │   │   ├── polling_service.py     # 상태 폴링 (15초)
│   │   │   └── notification_service.py # 알림 발송
│   │   ├── api/
│   │   │   └── routes.py        # Phase 1: REST API + WebSocket (11 routes)
│   │   ├── local/               # Phase 2: Local API ✅ 완료
│   │   │   ├── routes.py        # /api/v1/local/* 라우터 (32 routes)
│   │   │   ├── schemas.py       # 프리셋/작업 스키마
│   │   │   ├── models.py        # SQLAlchemy 모델
│   │   │   ├── services.py      # 프리셋/작업 서비스
│   │   │   ├── database.py      # SQLite 설정
│   │   │   └── preform_client.py # PreFormServer 클라이언트
│   │   └── schemas/
│   │       └── printer.py       # Pydantic 모델
│   ├── tests/
│   ├── Dockerfile
│   ├── docker-compose.yml
│   └── requirements.txt
│
├── frontend/                    # 프론트엔드 (React + Vite + TS + Tailwind CSS 4) ✅ 완료
│   ├── src/
│   │   ├── App.tsx              # 메인 라우터 (7탭 + 알림벨)
│   │   ├── components/
│   │   │   ├── Dashboard.tsx           # 모니터링 탭: 프린터 4대 그리드 + 타임라인
│   │   │   ├── PrinterCard.tsx         # 프린터 요약 카드
│   │   │   ├── PrinterDetail.tsx       # 프린터 상세 정보 뷰
│   │   │   ├── PrinterInfoModal.tsx    # 프린터 상세 모달 (3탭, 글로벌)
│   │   │   ├── PrinterTimeline.tsx     # 타임라인 간트 차트
│   │   │   ├── PrintPage.tsx           # 프린트 제어 탭
│   │   │   ├── PrinterPrintControl.tsx # 프린터별 독립 제어 컨테이너
│   │   │   ├── QueuePage.tsx           # 대기 중인 작업 탭
│   │   │   ├── HistoryPage.tsx         # 이전 작업 이력 탭
│   │   │   ├── StatisticsPage.tsx      # 통계 탭
│   │   │   ├── AutomationPage.tsx     # 자동화 탭 (한솔코에버)
│   │   │   └── AutomationManualPage.tsx # 자동화 수동제어 탭 (한솔코에버)
│   │   ├── types/
│   │   │   ├── printer.ts       # Phase 1 타입
│   │   │   └── local.ts         # Phase 2 타입
│   │   └── services/
│   │       ├── api.ts           # Phase 1 API
│   │       └── localApi.ts      # Phase 2 API
│   └── package.json
│
├── sequence_service/             # Phase 3: 시퀀스 서비스 (한솔코에버, 4/3 머지)
│   ├── app/cell/                # 시퀀스 런타임, Modbus, 로봇/프린터 제어
│   ├── app/core/config.py       # 시퀀스 서비스 설정
│   ├── app/db/                  # MySQL 모델/세션
│   ├── app/io/                  # Ajin IO (AXL.dll, Windows)
│   └── app/main.py              # 서비스 진입점
│
├── main.py                      # 통합 런처 (web-api + sequence_service, Windows)
│
├── factory-pc/                  # 공장 PC 스크립트
│   └── file_receiver.py         # STL 파일 수신 + 스크린샷 서빙 (포트 8089)
│
├── bin_picking/                  # Phase 5: 3D 빈피킹 비전 시스템
│   ├── src/
│   │   ├── acquisition/         # L1: 카메라 캡처 (depth_to_pointcloud, realsense_capture, basler_capture)
│   │   ├── preprocessing/       # L2: 전처리 (cloud_filter — ROI, 이상치, 다운샘플, RANSAC, 법선)
│   │   ├── segmentation/        # L3: 분할 (dbscan_segmenter)
│   │   ├── recognition/         # L4: 인식+자세 (cad_library, pose_estimator, size_filter)
│   │   ├── grasping/            # L5: 그래스프 계획 (grasp_planner, grasp_database.yaml)
│   │   └── communication/       # L6: 로봇 통신 (modbus_server — HCR-10L INT16)
│   ├── models/
│   │   ├── cad/                 # STL 원본 (46개, 고유 45종)
│   │   ├── reference_clouds/    # pickle 캐시 (포인트+법선+bbox)
│   │   └── fpfh_features/       # pickle 캐시 (FPFH 33D)
│   ├── config/
│   ├── tests/                   # E2E 테스트 (test_e2e_redwood, test_e2e_realsense)
│   └── tutorials/               # Open3D 학습 (01~11)
│
├── OpenMV/                      # Phase 4: OpenMV 카메라 (참고자료 + 스크립트)
├── robot-control/               # Phase 3: 로봇 제어 (미구현)
├── vision/                      # Phase 4: 비전 검사 (미구현)
└── shared/                      # 공유 유틸리티 (미구현)
```

---

## 하드웨어 사양

### Formlabs Form 4 (4대 보유)
| 항목 | 사양 |
|------|------|
| 기술 | mSLA (Masked Stereolithography) |
| 빌드 볼륨 | 200 × 125 × 210 mm (5.25L) |
| XY 해상도 | 50 µm |
| 연결 | Wi-Fi, USB, Ethernet |
| machine_type | `"FORM-4-0"` |

### 협동로봇
| 항목 | HCR-12 (로봇1) | HCR-10L (로봇2) |
|------|----------------|-----------------|
| 용도 | 빌드플레이트 교체, 세척기 투입 | 후가공 탭, 제품 이송 |
| 가반하중 | 12 kg | 10 kg |
| 통신 | Modbus TCP (포트 502) | 동일 |

### 후처리 장비 (⚠️ API 미지원 → 카메라로 완료 감지)
- Form Wash (2대), Form Cure (2대)
- **해결**: ~~OpenMV 카메라~~ → **Sipeed MaixCAM**으로 완료 감지 (4/14 대표님 지시, OpenMV 제외)

### Sipeed MaixCAM (세척기/경화기 완료 감지용, OpenMV 대체)
- **모델**: Sipeed MaixCAM ($33~48) - RISC-V SG2002 + **1 TOPS NPU**, WiFi 6, 2.3" IPS 터치, 4MP
- **접근법**: find_blobs() LED 감지 (100+fps, NPU 불필요) → MQTT → 서버, 온디바이스 AI
- **통신**: WiFi 6 → MQTT (paho-mqtt 내장) + Modbus TCP/RTU 내장 + Flask HTTP 서버
- **모델 학습**: MaixHub (무료, 사진→어노테이션→학습→QR배포)
- **자동 실행**: 전원 ON 시 autostart 지원
- **참고**: https://wiki.sipeed.com/maixcam
- ~~기존 OpenMV AE3는 프로젝트에서 제외 (4/14 대표님 지시)~~

---

## Formlabs API 비교

| 구분 | Web API | Local API |
|------|---------|-----------|
| 버전 | 0.8.1 (Beta) | 0.9.11 |
| 기반 | 클라우드 (api.formlabs.com) | 로컬 PC (PreFormServer) |
| 인증 | OAuth 2.0 | 없음 (로컬 실행) |
| Rate Limit | IP 100 req/sec, 사용자 1500 req/hr | 없음 |
| **프린터 모니터링** | ✅ 가능 | ⚠️ 제한적 |
| **작업 전송** | ❌ 불가 | ✅ 가능 |

> **핵심**: Web API는 읽기 전용! 원격 프린팅은 Local API 필수

### Formlabs API 사용 현황 (2026-02-26)

| 구분 | 전체 | 사용 중 | 미사용 | 사용률 |
|------|------|--------|--------|--------|
| Web API | 19개 | 6개 | 13개 | 32% |
| Local API | 35개 | 17개 | 18개 | 49% |
| **합계** | **54개** | **23개** | **31개** | **43%** |

#### 현재 사용 중인 Web API (6개) — 모니터링 전용
| # | API | 용도 |
|---|-----|------|
| 1 | `POST /o/token/` | OAuth2 토큰 발급 (자동 갱신) |
| 2 | `GET /printers/` | 프린터 4대 상태 조회 (15초 폴링) |
| 3 | `GET /printers/{serial}/` | 특정 프린터 상세 조회 |
| 4 | `GET /prints/` | 전체 프린트 이력 |
| 5 | `GET /printers/{serial}/prints/` | 프린터별 프린트 이력 |
| 6 | `GET /events/` | 프린터 이벤트 (완료/에러) |

#### 현재 사용 중인 Local API (17개) — 프린트 제어
| # | API | 용도 |
|---|-----|------|
| 1 | `GET /` | PreFormServer 연결 상태 확인 |
| 2 | `POST /discover-devices/` | 네트워크 프린터 검색 |
| 3 | `POST /scene/` | Scene 생성 |
| 4 | `DELETE /scene/{id}/` | Scene 삭제 |
| 5 | `GET /scene/{id}/` | Scene 정보 조회 |
| 6 | `POST /scene/{id}/import-model/` | STL 파일 로드 |
| 7 | `POST /scene/{id}/auto-orient/` | 자동 방향 설정 |
| 8 | `POST /scene/{id}/auto-support/` | 자동 서포트 생성 |
| 9 | `POST /scene/{id}/auto-layout/` | 자동 배치 |
| 10 | `POST /scene/{id}/print/` | 프린터로 작업 전송 |
| 11 | `GET /scene/{id}/print-validation/` | 프린트 전 유효성 검사 |
| 12 | `POST /scene/{id}/models/{id}/duplicate/` | 모델 복제 (대량 배치) |
| 13 | `GET /list-materials/` | 사용 가능 재료 목록 |
| 14 | `POST /scene/{id}/hollow-model/` | 내부 비우기 (레진 절약) |
| 15 | `POST /scene/{id}/save-screenshot/` | 미리보기 스크린샷 |
| 16 | `POST /scene/{id}/estimate-print-time/` | 정밀 시간 예측 |
| 17 | `POST /scene/{id}/interferences/` | 모델 간 간섭 검사 |

#### 미사용 API 중 활용 가치 높은 것 (미구현)
| API | 분류 | 기능 |
|-----|------|------|
| `GET /tanks/` | Web | 레진 탱크 이력 |
| `GET /cartridges/` | Web | 카트리지 소모 이력 |
| `POST /scene/{id}/label-part/` | Local | 모델에 라벨 각인 |
| `POST /load-form/` | Local | .form 파일 로드 |
| `POST /save-form/` | Local | Scene → .form 저장 |

#### API로 할 수 없는 것 (한계)
| 기능 | 상태 | 우리 대안 |
|------|------|----------|
| 프린트 일시정지/재개/취소 (원격) | **미지원** | 터치스크린 안내 표시 |
| Webhook (실시간 이벤트 푸시) | **미지원** | 15초 폴링 |
| Form Wash/Cure 제어 | **API 없음** | OpenMV 카메라 완료 감지 |
| 프린터 설정 변경 (원격) | **미지원** | 터치스크린 |

---

## Phase 1: Web API 모니터링 ✅ 완료

### API 엔드포인트
| Method | Endpoint | 설명 |
|--------|----------|------|
| GET | `/api/v1/dashboard` | 4대 프린터 상태 요약 |
| GET | `/api/v1/printers` | 프린터 목록 |
| GET | `/api/v1/printers/{serial}` | 특정 프린터 상태 |
| GET | `/api/v1/prints` | 프린트 이력 (날짜/상태/프린터 필터) |
| GET | `/api/v1/statistics` | 통계 데이터 |
| WS | `/api/v1/ws` | 실시간 업데이트 |

### 확인된 프린터 (4대)
| 이름 | 시리얼 | IP | 비고 |
|------|--------|-----|------|
| CapableGecko | Form4-CapableGecko | 192.168.219.46 | Grey V5 |
| HeavenlyTuna | Form4-HeavenlyTuna | 192.168.219.48 | Clear V5 |
| CorrectPelican | Form4-CorrectPelican | 192.168.219.43 | Flexible 80A V1.1 |
| ShrewdStork | Form4-ShrewdStork | 192.168.219.45 | ✅ 운용 중 (4/3 헤드커버 수리 완료) |

---

## 프론트엔드 UI 구조 (2026-02-27 최신)

### 5탭 네비게이션 + 알림벨
| 탭 | 컴포넌트 | 기능 |
|----|----------|------|
| **모니터링** | Dashboard.tsx | 프린터 4대 그리드 카드, 상태 필터(토글), 타임라인 간트 차트 |
| **프린트 제어** | PrintPage.tsx | 프린터별 독립 컨테이너 (PrinterPrintControl) |
| **대기 중인 작업** | QueuePage.tsx | 드래그앤드롭 순서 변경, 예약 시간 |
| **이전 작업 내용** | HistoryPage.tsx | 로컬+클라우드 이력, 필터, CSV, 메모 |
| **통계** | StatisticsPage.tsx | 재료 도넛차트, 일별 바차트, 프린터별 가동률 |
| **🔔 알림벨** | App.tsx | 미읽음 뱃지, 드롭다운, 30초 폴링 |

### 프린터 상세 모달 (PrinterInfoModal) — PreForm 앱 수준 3탭
- **트리거**: 프린터 이름(파란 링크) 또는 ℹ️ 아이콘 클릭 → 슬라이드-오버
- **Details / Settings / Services** 3탭

### 데이터 흐름
```
REST 초기 로드 → State → WebSocket 실시간 구독 (15초 폴링 폴백)
Phase 1: api.ts (Formlabs Cloud)  →  Dashboard, HistoryPage, StatisticsPage
Phase 2: localApi.ts (Local API)  →  PrintPage, QueuePage, HistoryPage, Notifications
```

---

## Phase 2: Local API 원격 제어 ✅ 완료

### API 엔드포인트 (32 routes)
| Method | Endpoint | 설명 |
|--------|----------|------|
| GET | `/api/v1/local/health` | Local API 상태 확인 |
| POST | `/api/v1/local/printers/discover` | 프린터 검색 |
| POST/GET/PUT/DELETE | `/api/v1/local/presets[/{id}]` | 프리셋 CRUD |
| POST | `/api/v1/local/presets/{id}/print` | 프리셋으로 프린트 |
| POST/GET/DELETE | `/api/v1/local/upload`, `/files[/{filename}]` | 파일 관리 |
| POST/GET | `/api/v1/local/print[/{id}]` | 프린트 작업 |
| POST/DELETE | `/api/v1/local/scene/prepare`, `/{id}/print`, `/{id}` | Scene 관리 |
| GET/POST | `/api/v1/local/scene/{id}/validate`, `/models`, `/models/{id}/duplicate` | 유효성/복제 |
| GET | `/api/v1/local/materials` | 재료 목록 |
| GET/POST | `/api/v1/local/scene/{id}/screenshot[/{filename}]` | 스크린샷 |
| POST | `/api/v1/local/scene/{id}/estimate-time`, `/interferences` | 시간/간섭 |
| GET/POST/PUT/DELETE | `/api/v1/local/notes[/{print_guid}][/{note_id}]` | 메모 CRUD |
| GET/POST | `/api/v1/local/notifications[/mark-read]` | 알림 |

### TODO (미완료)
- [ ] 실제 프린터 프린트 전송 테스트 (레진 탱크 장착 필요)

### 운영 인프라 (현재 = 2026-05-08)

| 서버 | URL | 인증 | 역할 | 상태 |
|------|-----|------|------|------|
| **공장 PC** | `https://factory.flickdone.com/` (Cloudflare Tunnel) | JWT | 운영 (실제 프린터 제어 + sequence_service + frontend) | ✅ NSSM OrinuMain 자동 시작 |
| **카카오 VM** | `http://61.109.239.142:8085/` | JWT | 운영 (모니터링 / SaaS) | ✅ systemd 자동 시작 |
| **6000 서버** | `http://106.244.6.242:8085/` | JWT | 개발 + 모니터링 병행 (개발 환경 본진) | ✅ systemd 자동 시작 |

**로그인 (모든 서버 공통)**: `admin` / `orinu2026!` (분기별 회전, 다음 2026-09-01)

**진화 흐름**:
- ~~5090 VM 운영~~ (2/12) → ~~카카오 VM 외부 접속 + Basic Auth~~ (4/16) → 카카오 + 공장 PC Cloudflare Tunnel + JWT (현재)
- ~~3/26 6000 서버 WireGuard Method B 설치~~ → ~~4/24 Cloudflare Tunnel 도입~~ → 현재 VPN은 백업 경로로만 유지
- 도메인 변경: ~~`lab.flickdone.com`~~ (3/18) → **`factory.flickdone.com`** (4/21 확정)

### 6000 서버 VPN 정보 (백업 경로, 현재 운영 의존도 낮음)

| 항목 | 값 |
|------|-----|
| VPN IP | 10.145.113.8/24 |
| 인터페이스 | wg0 |
| conf 파일 | `/etc/wireguard/wg0.conf` (키 정보, 커밋 금지) |
| Peer Endpoint | 106.244.6.242:56461 (= orinu.iptime.org) |
| AllowedIPs | 10.145.113.0/24 (192.168.100.0/24는 3/18 제거 — 로봇 충돌 해결) |
| 자동 시작 | `sudo systemctl enable --now wg-quick@wg0` |

> Method B 채택 (서버에 WG 직접 설치). Method A(공유기 라우팅)는 실패.

### 공장 PC 정보 (운영 본진)

| 항목 | 값 |
|------|-----|
| Windows 사용자 | `devfl` |
| 외부 접속 | `https://factory.flickdone.com/` (Cloudflare Tunnel `orinu-factory`) |
| VPN IP | 10.145.113.3 (백업) |
| AnyDesk | 원격 접속용 |
| 배포 경로 | `D:\3D_printer_automation_0305\3D_printer_automation` |
| Python | 3.14.3 / Node v24 |
| 배포 방법 | `deploy.bat` (관리자 cmd, 4/30 도입) |
| PreFormServer | `C:\PreFormServer\PreFormServer.exe -p 44388` (v3.55.0.606, 시작 프로그램 등록) |
| PreForm 앱 | `C:\Program Files\Formlabs\PreForm\3.57.0.622\PreForm.exe` (별도) |
| file_receiver | 포트 8089 → `C:\STL_Files` (시작 프로그램) |
| MariaDB | 11.3 (port 3306, 자동 시작) |
| NSSM 경로 | `C:\nssm\nssm-2.24\win64\nssm.exe` (PATH 미등록, 전체 경로 사용) |

> ⚠️ PreFormServer는 `-p 44388` 옵션 필수. 옵션 없이 실행하면 바로 종료됨.
> 자동 시작 체계 / 재부팅 후 체크 / NSSM 명령은 "마지막 업데이트 → 운영 인프라 핵심 정보" 섹션 참조.

---

## Phase 3: HCR 로봇 연동 ✅ 한솔 머지 7차까지 공장 PC 배포·검증 완료

- **프로토콜**: Modbus TCP (포트 502), pymodbus 3.x
- **로봇**:
  - HCR-12 (빌드플레이트 교체, 세척기 투입) — 공장 PC 연결 (현재)
  - HCR-10L (후가공, 제품 이송, 빈피킹) — IPC-510 이전 예정 (4/29 합의)
- **한솔코에버 협업**: `hansol-dev` 브랜치 → 머지 5차까지 완료 (`9c161dc` / `e68c2b1` / `9f97f1e` / `b9164d9` / `9fd365a`)
- **3/27 한솔 자체 최종 시연 완료**
- **다음주 예승님 방문**: 실 출력 + 로봇 E2E 테스트 (시뮬 토글로 SIMUL_MODE=false)
- **HCR-10L Modbus 레지스터** (4/15 INT16 재설계, `a13b5ce`):
  - 비전PC → 로봇: 130~140
  - 로봇 → 비전PC: 150~151
  - 로봇 내장: 400~405(TCP 좌표) / 600(상태) / 700~702(명령)
  - 인코딩: INT16 (1/10mm, 1/10deg, 16bit 정수)

---

## Phase 4: 장비 모니터링 (MaixCAM, ~~OpenMV~~ 대체) ⬜ 빈피킹 후순위

> 4/14 대표님 지시로 OpenMV → MaixCAM 전환. 이전 OpenMV 설계는 `memory/project_openmv_image_capture.md`에 역사로 보존.

### 용도
- **세척기/경화기 완료 감지** — Form Wash/Cure는 API 미지원이라 카메라 기반 감지 필요 (2026.02.06 확정)
- **부품 식별 + 불량 검출** — Phase 5 빈피킹 카메라(Basler)와 별도, 추후 검토

### 카메라 배치 (4대 예정)
| 위치 | 감지 내용 |
|------|----------|
| 세척기 1, 2번 전면 | 세척 중/완료 |
| 경화기 1, 2번 전면 | 경화 중/완료 |

### 기술 스택 (2026.04.14 이후)
- **Sipeed MaixCAM**: RISC-V SG2002 + 1 TOPS NPU + 4MP 터치 스크린
- **온디바이스 AI**: Cloud 없이 현장 독립 동작 (find_blobs() / YOLO)
- **통신**: WiFi 6 → MQTT (paho-mqtt 내장) + Modbus TCP/RTU 내장
- **학습**: MaixHub (무료 사진→학습→QR 배포)
- **보유 장비**: MaixCAM 1대 + LicheeRV Nano 2대 (4/6 수령)

> 통신 아키텍처는 OpenMV 시기와 동일: `카메라 → MQTT → Mosquitto → FastAPI → HCR 로봇(Modbus)`. MQTT E2E는 3/12 검증 완료.

상세: `memory/project_maixcam_monitoring.md`

---

## Phase 5: 3D 빈피킹 비전 시스템 🔄 합성데이터(디지털 트윈) v1~v3 8000장 구축 (6/12)

> **문서**: ORINU-DEV-2026-002 (구본경 대표, 2026-03-18)
> **개발 환경**: Mac (Intel) + venv binpick (Python 3.12 + Open3D 0.19.0). 6000 서버는 Open3D 불가 (AVX2 미지원)
> **파이프라인**: L1 영상취득 → L2 전처리 → L3 DBSCAN분할 → L4 FPFH+RANSAC+ICP → L5 그래스프 → L6 Modbus
> **🎯 6/2 마감**: KAIST 3단계 부트캠프 회사 데이터 프로젝트 시작 — 그 전까지 학습 데이터셋 v1 (~1,200~2,400장) 확보

### 현재 진행 상태 (2026-05-20)

| 단계 | 상태 |
|------|------|
| W0~W2 학습 (논문 3편 + 튜토리얼 11개 + 실전 코드 3개, 6,149줄) | ✅ 완료 (3/23) |
| W3+ L1~L6 파이프라인 + 그래스프 DB 29종 + E2E 시나리오 | ✅ 완료 (4/6~10) |
| Modbus INT16 재설계 + Colored ICP + Basler 듀얼 캡처 모듈 | ✅ 완료 (4/15) |
| RealSense D435 라이브 + Full Pipeline PASS | ✅ 완료 (4/13~14) |
| 카메라 입고 (Blaze-112 + ace2) | ✅ 4/23 입고 / 5/8 사무실 운반 |
| **Mac Blaze 풀 작동** (pypylon 단독, IPC-510 대기 불필요) | ✅ 5/12 (commit 7e28df9 외 5건) |
| **사전 디벨롭 코드** (test_basler_live + pose_enumerator + auto_label) | ✅ 5/11 (~1,900줄) |
| **학습 데이터 라벨 신뢰도 인프라** (스키마 확장 + 대칭 그룹 + 검증 매뉴얼 + SOP 보강) | ✅ 5/13 (commit f9ec525 + dc8d0bf) |
| **5/15 본 캡처 인프라** (intrinsics sanity + capture wrapper + viewer 가드 + runbook + smoke test + .gitignore + 1pager v2.4) | ✅ 5/14 (commit 5개) |
| **5/15 공장 방문 + 한글 fix + Phase 2 E2E + ACE2 케이블 인수 + 부트캠프 주제 확정** | ✅ 5/15 |
| **5/18 트랙 1 P5 보류 → 트랙 2 (Roboflow + YOLOv8n) 전환 + AICA A100 부활 + 한솔 코드 인계** | ✅ 5/18 — YOLOv8n mAP50 0.988 (단일환경, 누수 의심) |
| **5/19 Roboflow 178장 + 협력사 통합 메일 + 5/20 작업 계획** | ✅ 5/19 |
| **5/20 ACE2 셋업 진단 — 어댑터/통신/코드 OK, C-mount 렌즈 미장착** (commit `09bdb33` live_viewer_ace2.py) | ✅ 5/20 — 한솔 보유 렌즈 인수 대기 |
| **5/20 한솔 좌표 명세 답변 수신 — YOLOv11s/m 권고 + 6요소 (x,y,z,edge,angle,label) 명세** | ✅ 5/20 — 우리 v2 코드 변경 결정 |
| **5/20 v2 5모델 비교 학습 인프라 완성** (yolov8n/m + yolo11s/m/l, commit `9b68c14`) + 공장 촬영 전환 가이드 (`docs/archive_track1_202605/factory_capture_20260520.md` commit `ee332b9`) | ✅ 5/20 오후 |
| **5/20 저녁 공장 촬영 완료 — 핸드폰 200장+** (한화 협동로봇 + 흰 작업대 후보 1순위, 미확정) — **2개 조합 C(5,2)=10쌍 전수 + 3개 조합 C(5,3)=10조합 전수** + 5종 단일 + 빈/박스 | ✅ 5/20 저녁 — 5/21 Mac 업로드 예정 |
| 5/21 Mac 업로드 → Roboflow batch `20260520_factory_*` → annotation → manual split v2 → AICA 5모델 비교 학습 | ⏳ 5/21~ |
| 5종 풀 데이터셋 (~1,200~2,400장) | ⏳ 5/22~6/1 (사무실 가용 5일) |
| ACE2 RGB 인수 (한솔 C-mount 렌즈) + RGB+Depth 통합 출력 | ⏳ 5/22~ |
| eye-in-hand 캘리브레이션 (2세트) + Colored ICP 실연동 | ⏳ 6월 이후 (그리퍼 장착 후) |
| X/Y 각도 데이터 명세 (대표님 5/6 지시 3번) | ✅ **1pager + pose_validation_protocol + stable_poses.yaml 검증 매뉴얼 완성** (align 대기) |
| **한솔 좌표 명세 (대표님 5/18 피드백)** | ✅ 5/20 — 6요소 명세 수신 + v2 모델 변경 결정 |

### 핵심 성과 수치

**W3+ 실제 STL 29종 기반 E2E** (4/6~10, Mac):
| 지표 | 결과 | 목표 | 판정 |
|------|------|------|------|
| 인식률 (easy, 5종) | **100%** (5/5) | 85% | ✅ |
| 인식률 (crowded, 10종) | **90%** (9/10) | 80% | ✅ |
| 인식률 (hard, 5종) | **60%** (3/5) | 85% | ⚠️ FPFH 한계 → Colored ICP 도입 |
| RMSE | **1.0~1.5mm** | 3mm | ✅ |
| 매칭 시간 (OBB SizeFilter) | **0.4~0.6초** | 2.0초 | ✅ 3~5배 여유 |
| L1~L6 파이프라인 | **전체 구현 완료** | — | ✅ |
| L5 그래스프 DB | **29종 완성** | 29종 | ✅ |
| 4/22 데모 리허설 synthetic | **9.7s → 1.5s** (8배 단축) | — | ✅ |

### 레진별 파라미터 (참고)

| 레진 | voxel | Robust kernel | 비고 |
|------|-------|--------------|------|
| Grey/White | 2mm | Tukey 1mm | 표준 |
| Clear | 3~4mm | SOR + 멀티스케일 | 반투명 → ToF 노이즈 큼 |
| Flexible | 2mm | Huber 1.5mm | 변형 허용 |

### 코드 위치 (구현 완료)

`bin_picking/` 하위:
- `tutorials/01~11` (4,247줄, ✅ 전체 PASS)
- `src/recognition/` — cad_library / size_filter / pose_estimator
- `src/preprocessing/cloud_filter.py` — L2 전처리 (레진별 프리셋)
- `src/segmentation/dbscan_segmenter.py` — L3
- `src/grasping/` — grasp_planner + `grasp_database.yaml` (29종)
- `src/communication/modbus_server.py` — L6 (pymodbus 3.x, INT16)
- `src/acquisition/` — depth_to_pointcloud / realsense_capture / **basler_capture** / hand_eye_calibration
- `src/main_pipeline.py` — L1~L6 통합
- `tests/test_e2e_redwood.py` + `tests/test_e2e_cad_matching.py`

상세 파일 목록 / 줄 수 / 역할: `memory/project_binpicking_overview.md` + `memory/project_binpicking_e2e_history.md`

### 남은 개발 작업

| 작업 | 블로커 | 예상 시점 |
|------|--------|----------|
| Mac 라이브 검증 (Blaze 부팅 OK / pylon 설치 / depth 스트림) | 어댑터 도착 (5/9 토요일) | 5/10~5/12 |
| 코드 수정 (BLAZE_112 fx/fy 460→416/188, ACE2 모델명 a2A2448) | 라이브 검증 | 5/10~ |
| 실물 SLA 부품 ACCEPT 검증 | 라이브 + 부품 | 5월 중 |
| X/Y 각도 데이터 명세 초안 ⭐ | 없음 (지금 가능) | 다음주 align 전 |
| 실물 부품 다각도 촬영 데이터셋 | 라이브 + 명세 align | 5월 중~후반 |
| eye-in-hand 캘리브레이션 (2세트) | 카메라 + 로봇 | 카메라 입고 후 |
| Colored ICP 실연동 | RGB 데이터 | 라이브 + ace2 |
| multi-view 재촬영 파이프라인 | 카메라 + 로봇 | 6~7월 |

---

## 기술적 제약사항 및 대안

| 제약 | 문제 | 우리 대안 |
|------|------|----------|
| Web API 읽기 전용 | 프린트 전송 불가 | Local API 병행 |
| Web API 예열/충전 미반영 | IDLE로 표시됨 | Local API 연동 시 해결 |
| ~~공장 WiFi VPN 문제~~ | ~~VPN 라우팅 깨짐~~ | ✅ 해결 (3/26): 6000 서버에 WireGuard 클라이언트 설치 (Method B) |
| .form 파일 미지원 | STL만 지원 | `POST /load-form/` 구현 필요 |
| Form Wash/Cure API 없음 | 장비 제어 불가 | OpenMV 카메라 |
| Webhook 없음 | 실시간 푸시 불가 | 15초 폴링 |

---

## 16단계 공정 흐름

| # | 공정 | 담당 |
|---|------|------|
| ① | STL 파일 업로드 | 사용자 (웹/앱) |
| ② | 프린터로 작업 전송 | 백엔드 (Local API) |
| ③ | 3D 프린팅 | Form 4 (4대) |
| ④ | 프린팅 완료 감지 | 백엔드 (Web API 폴링) |
| ⑤~⑥ | 빌드플레이트 픽업 → 세척기 투입 | HCR-12 |
| ⑦ | 세척 완료 감지 | OpenMV #1, #2 |
| ⑧ | 경화기 투입 | HCR-12 |
| ⑨ | 경화 완료 감지 | OpenMV #3, #4 |
| ⑩~⑫ | 픽업 → 서포트 제거 → 후가공 | HCR-10L |
| ⑬ | YOLO 비전 검사 | Intel RealSense |
| ⑭~⑮ | 양품/불량 분류 → 적재 | HCR-10L |
| ⑯ | 완료 보고 | 백엔드 (알림) |

---

## 기술 스택

| 분류 | 기술 |
|------|------|
| **백엔드** | Python 3.11+, FastAPI, httpx, pydantic-settings, SQLite + SQLAlchemy |
| **프론트엔드** | React 18 + TypeScript, Vite, Tailwind CSS 4, WebSocket |
| **인프라** | Docker, WireGuard VPN |
| **Phase 3~4** | pymodbus, Ultralytics YOLO, OpenMV AE3, Edge Impulse, Mosquitto MQTT |

---

## 완성 아키텍처 설계

**설계 문서**: `.claude/plans/staged-rolling-kite.md`

### 구현 순서
1. **인프라 기반**: PostgreSQL 마이그레이션, Docker Compose 확장, 이벤트 버스, React Router
2. **Phase 3**: Modbus 클라이언트 → 로봇 API → 로봇 UI
3. **Phase 4**: MQTT 클라이언트 → OpenMV 스크립트 → 카메라/비전 API → UI
4. **통합**: FSM 엔진 → 공정 관리 UI → SaaS tenant_id → 통합 테스트

---

## 참고 링크

### Formlabs
- Web API: https://support.formlabs.com/s/article/Formlabs-Web-API
- Local API: https://formlabs-dashboard-api-resources.s3.amazonaws.com/formlabs-local-api-latest.html
- Python: https://github.com/Formlabs/formlabs-api-python

### 기타
- 한화로보틱스: robot_inquiry@hanwha.com
- YOLO: https://github.com/ultralytics/ultralytics
- Intel RealSense: https://github.com/IntelRealSense/librealsense

---

## 환경 변수 (.env)

```bash
# Formlabs Web API
FORMLABS_CLIENT_ID=your_client_id
FORMLABS_CLIENT_SECRET=your_client_secret

# PreFormServer (공장 PC VPN)
PREFORM_SERVER_HOST=10.145.113.3
PREFORM_SERVER_PORT=44388

# 공장 PC 파일 수신
FILE_RECEIVER_HOST=10.145.113.3
FILE_RECEIVER_PORT=8089

# 폴링
POLLING_INTERVAL_SECONDS=15

# 사용자 로그인 (JWT) — 셋 다 비면 인증 OFF (로컬 개발용)
AUTH_USERNAME=admin
AUTH_PASSWORD_HASH=  # bcrypt 해시 (평문 X). 생성: python -c "import bcrypt; print(bcrypt.hashpw(b'pw', bcrypt.gensalt(rounds=12)).decode())"
JWT_SECRET=          # 서버별 랜덤 32바이트. 생성: python -c "import secrets; print(secrets.token_urlsafe(32))"
JWT_EXPIRE_DAYS=7
JWT_ABSOLUTE_MAX_DAYS=30
```

---

## 사용자 인증 (JWT 로그인)

5/6부터 JWT 기반 로그인 시스템 운영. **공통 1개 계정 + 7일 sliding refresh + 30일 절대 최대**.

| 항목 | 값 |
|------|-----|
| 사용자명 | `admin` |
| 비번 | macOS 키체인 "안전한 메모" `orinu Web Login 2026-05` 참조 |
| 토큰 만료 | 7일 (사용 시 자동 연장) |
| 절대 최대 | 30일 (이후 강제 재로그인) |

### 코드 구조
- `web-api/app/core/user_auth.py` — bcrypt + JWT 핵심 로직
- `web-api/app/core/jwt_middleware.py` — ASGI 미들웨어, `X-New-Token` 헤더로 sliding refresh
- `web-api/app/api/auth_routes.py` — `POST /api/v1/auth/login`, `GET /api/v1/auth/me`, `POST /api/v1/auth/logout`
- `frontend/src/components/LoginPage.tsx` — 다크 테마 로그인 페이지
- `frontend/src/services/auth.ts` — 토큰 관리 + `authFetch` (모든 API 호출 통과)

### 로컬 개발
`.env`에 `AUTH_USERNAME` 또는 `AUTH_PASSWORD_HASH` 또는 `JWT_SECRET` 셋 중 하나라도 비면 **인증 자동 OFF**. 로컬 새로고침 자유.

### 회전 정책
- 분기별 (3/6/9/12월 1일)
- 즉시 회전 트리거: 직원 퇴사 / 노트북 분실 / 누출 의심
- 3개 서버 동기화 필수
- 상세: `memory/project_web_auth_security.md`

---

## 자동 배포 워크플로우 (5/6~)

태민님이 코드 변경 + git push 후:

```
태민님: "배포해줘"
저: scripts/deploy_servers.sh 실행
   → 6000 서버 (로컬) + 카카오 VM (SSH) 동시 배포
   → 부팅 검증 + 외부 접속 검증 (HTTP 401 expected)
태민님: 공장 PC AnyDesk → 관리자 cmd
   → cd /d D:\3D_printer_automation_0305\3D_printer_automation
   → deploy.bat
```

**옵션** (`scripts/deploy_servers.sh`):
- `--skip-deps` — 의존성 변경 없을 때
- `--skip-build` — 백엔드만 변경 시
- `--6000-only` / `--kakao-only` — 한 서버만

---

## 한솔코에버 협업 타임라인

- ✅ HW 설계변경 및 구축 (02-25~03-18): 바렐→스핀들, 재원텍+코에버
- ✅ SW 개발 (02-26~03-19): API 분석, 로봇/비전/3D프린팅 연동, 시퀀스 개발 — 김기원(퇴사), 이나라, 이예승
- ✅ 데모 시연 (03-20): 경기ITP-코에버 3자 최종 확인 완료
- ✅ 한솔 최종 시연 (03-27): 한솔 자체 진행
- ✅ 머지 1차 (04-03, `9c161dc`): 김기원 주임 코드 통합 (sequence_service / AutomationPage / automation_db.py)
- ✅ 머지 2차 (04-16, `e68c2b1`): 이예승 사원 — 자동화 CMD 프린터 할당
- ✅ 머지 3차 (04-23, `9f97f1e`): 이예승 사원 — 경화기 2→1대 축소 (Cure 2 비활성화)
- ✅ 머지 4차 (05-06, `b9164d9`): 이예승 사원 — 시뮬 토글 + 프린터 별명 매핑
- ✅ 머지 5차 (05-06, `9fd365a`): 이예승 사원 — `cell_state.simul_mode` 컬럼 자동 마이그레이션 (4차 누락분)
- ⏳ 다음주 예승님 방문: 실 출력 + 로봇 E2E 테스트 (시뮬 토글 OFF)

상세: `memory/project_hansol_merge_issues.md`, `memory/project_meeting_0423_hansol.md`, `memory/project_meeting_0506_hansol.md`

---

## GitHub 협업 구조 (한솔코에버)

| 항목 | 내용 |
|------|------|
| 리포 | `m2222n/3D_printer_automation` (Private) |
| main 보호 | Require PR + Restrict deletions + Block force pushes |
| 오리누 작업 | `main` 브랜치 |
| 한솔 작업 | `hansol-dev` 브랜치 |
| 한솔 권한 | Write (Collaborator: ~~`justkiwon`~~ 퇴사, `eseung97` 이예승) |
| 리모트 | `origin` = orinu-ai, `personal` = m2222n |

---

## 마지막 업데이트

> 일자별 작업 이력은 **`CLAUDE.local.md`** 참조. 이 섹션은 **프로젝트 마일스톤 + 핵심 의사결정 + 현재 진행**만 보관.

### 마지막 업데이트 일자: 2026-07-10 (KAIST 부트캠프 종료(우수상) 후 정리 — sim2real 여정 완결 + 대표님 SaaS 고도화 지시 착수 + 프린터 어댑터 리팩터링·배포)

> ⚠️ 아래 마일스톤 표는 5/20까지 상세, 이후는 요약. 6/13~7/10 상세 일자별은 `CLAUDE.local.md`(주간 요약) + `memory/project_digital_twin_synth_data_research_0609.md`(§6/13~§7/8) + `memory/project_kaist_final_presentation_0709.md` 참조.

### 마일스톤 (시간 순)

| 시기 | 마일스톤 | 결과 / 메모리 참조 |
|------|---------|-------------------|
| 2026-01 | Phase 1 Web API 모니터링 착수 | ✅ 완료 |
| 2026-02 | Phase 2 Local API + 프론트 UI / 5090 VM 운영 | ✅ 완료 (5090은 폐기, 카카오로 전환) |
| 2026-02-12 | 대표님 데모 성공 → PreForm 동등 구현 지시 | ✅ 5탭 UI + 알림벨 완료 |
| 2026-02-26 | 소스코드 공유 결정 + Phase 전환 | 🔄 한솔 협업 시작 |
| 2026-03 (W11~W13) | Phase 4 (OpenMV→MaixCAM) + 한솔 PR + WireGuard | 🔄 MaixCAM 후순위 |
| 2026-03-27 | 한솔코에버 최종 시연 | ✅ 한솔 자체 진행 |
| 2026-04-03 | 한솔 머지 1차 (`9c161dc`) — 김기원 코드 통합 | ✅ — `memory/project_hansol_merge_issues.md` |
| 2026-04-06~10 | 빈피킹 W3+ 파이프라인 완성 (L1~L6 + 그래스프 DB 29종) | ✅ — `memory/project_binpicking_overview.md` + `memory/project_binpicking_e2e_history.md` |
| 2026-04-13~14 | RealSense D435 라이브 연동 + Full Pipeline PASS | ✅ — `memory/project_realsense_d435.md` |
| 2026-04-14 | HCR-10L 로봇 교육 1회차 (펜던트 + Modbus TCP) | ✅ — `memory/reference_hcr_user_education.md` |
| 2026-04-15 | Modbus INT16 재설계 + Colored ICP + Basler 듀얼 캡처 모듈 | ✅ — 카메라 입고 전 SW 마무리 |
| 2026-04-16 | 한솔 머지 2차 (`e68c2b1`) + 카카오 VM 외부 접속 + Basic Auth | ✅ — `memory/project_kakao_vm_migration.md` |
| 2026-04-21 | 도메인 확정 `factory.flickdone.com` | ✅ |
| 2026-04-22 | 데모 리허설 피드백 반영 (synthetic 9.7s→1.5s, 크래시 방어) | ✅ 커밋 6건 |
| 2026-04-23 | Basler 입고 + IPC-510 입고 + 한솔 3자 회의 + 머지 3차 (`9f97f1e`) | ✅ — `memory/project_meeting_0423_hansol.md` |
| 2026-04-24 | Cloudflare Tunnel 구축 + NSSM OrinuMain 등록 + DB(MariaDB) 재조사 + Formlabs Secret Rotate | ✅ — `memory/project_cloudflare_tunnel.md` |
| 2026-04-29 | 🔥 공장 PC 원격 복구 (origin URL 정정 + aiomqtt 누락) | ✅ — `memory/project_factory_pc_remote_recovery_0429.md` |
| 2026-04-29 | KAIST 부트캠프 합격 (4/30~7/9) | ✅ — `memory/project_kaist_bootcamp.md` |
| 2026-04-30 | `deploy.bat` 도입 + smoke test + 재부팅 자동복구 검증 | ✅ — `memory/project_deploy_bat.md` |
| 2026-05-06 | JWT 로그인 도입 + 한솔 머지 4·5차 + 공장 PC 응답 영구 해결 (12시간, 11커밋) | ✅ — `memory/project_web_auth_security.md` |
| 2026-05-06 | 한솔 3자 회의 (4DoF / 다면 인식 / 한화 패키지) + 대표님 빈피킹 개인 지시 4가지 | ✅ — `memory/project_meeting_0506_hansol.md` + `memory/project_binpicking_ceo_directive_0506.md` |
| **2026-05 (W19~W21)** | ⭐**Basler 실물 셋업 → Mac 단독 워크플로 확립 → 트랙2 전환** — 카메라 개봉·어댑터 검증(ipTIME U1G-C)·**5/12 Mac Blaze 풀 작동**·라이브 뷰어 / 5/15 공장(**한글 파일명 fix**·**Phase 2 E2E 풀패스**·ACE2 케이블 인수·⭐**출력 실패 진짜 원인=레진 탱크 잔여물**) / 5/18 **Roboflow v1 + AICA A100 부활 + YOLOv8n mAP50 0.988**(⚠️같은 환경) + 한솔 빈피킹 코드 인계 / 5/20 **ACE2 렌즈 미장착 진단** + ⭐**한솔 좌표 6요소 명세 수신**(x,y,z,edge,angle,label) | ✅ — `memory/project_basler_setup_history.md` · `project_roboflow_dataset.md` · `project_hansol_coord_spec_0520.md` · `project_phase2_e2e_complete_0515.md` · `reference_aica_a100.md` |
| 2026-05-22~29 (W21~22) | v2 5모델 YOLO 학습(🥇yolov8n mAP50 0.9939) + 일정 재정렬(빈피킹=가을) + JWT 회귀버그 fix + 대표님 5/28 4대 지시 | ✅ — `memory/project_yolo_v2_training_results_0522.md` |
| 2026-06-01~05 (W23) | 삼성 시연 성공 + Formlabs fix + KAIST 첫 미팅(방향 전환) | ✅ — `memory/project_kaist_meetings_timeline.md` |
| 2026-06-08~12 (W24) | KAIST 1차 발표 + Visual Hull baseline + 디지털트윈 합성데이터 v1~v3(8000장) | ✅ — `memory/project_digital_twin_synth_data_research_0609.md` |
| 2026-06-15~19 (W25) | 부품 27종 확정 + 모델 아키텍처 확정(PointNet++/VQ-VAE) + ⭐대표님 6/18 SaaS 지시 | ✅ — `memory/project_saas_platform_directive_0618.md` |
| 2026-06-22~26 (W26) | 조교 코드 통합·A100 학습 1사이클(합성 class 0.804) + 2차 발표 + 실증 100장 촬영 | ✅ — `memory/project_digital_twin_synth_data_research_0609.md` §6/23~26 |
| 2026-06-29~07-08 (W27) | ⭐⭐ sim2real 여정: 실측 5% 붕괴 → real fine-tune 대전환 → test100 F1 0.684 → 학습 종료(병목=센서 물리) | ✅ — `memory/project_digital_twin_synth_data_research_0609.md` §7 |
| **2026-07-09** | 🏆 **KAIST 부트캠프 3단계 최종발표 — 우수상 수상·수료** | ✅ — `memory/project_kaist_final_presentation_0709.md` |
| **2026-07-10** | 대표님 7/7 SaaS 고도화 지시 착수(현황정리+ERP/MES/ATS 기능정의) + 프린터 어댑터 리팩터링·3서버 배포 + KAIST 코드 3곳 백업 | ✅ — `memory/project_ceo_saas_directive_0707.md` + `memory/project_printer_adapter_refactor_0710.md` |

### 핵심 의사결정 (이유 + 결과 보존)

#### 운영 서버 — 5090 폐기 → 카카오 VM 이전 (2026-02-26)
- **배경**: 5090 VM 한계 + VPN-로봇 네트워크 충돌
- **결정**: 운영을 카카오 클라우드로 이전 (대표님 지시)
- **결과**: 4/16 카카오 VM 외부 접속 정상, Basic Auth 적용. 4/24 Cloudflare Tunnel 도입으로 공장 PC가 외부 접속 단일 진입점이 됨

#### 프린터 제어 경로 — VPN(Method B) → Cloudflare Tunnel
- **배경**: WireGuard VPN으로 6000↔공장 연결 → 도메인 확정 + 상용화 대비 + VPN 의존도 감소 필요
- **결정**: 4/21 도메인 `factory.flickdone.com` 확정 → 4/24 Cloudflare Tunnel `orinu-factory` 구축 → 공장 PC NSSM OrinuMain 자동 시작 등록
- **결과**: 외부 HTTPS 200 운영, 재부팅/정전 자동 복구. VPN은 개발/백업용으로 유지 (6000 서버 `10.145.113.8`)
- 상세: `memory/project_cloudflare_tunnel.md`, `memory/reference_factory_pc_deployment_guide.md`

#### 인증 방식 — Basic Auth → JWT 로그인 (2026-05-06)
- **배경**: 4/24 Cloudflare Tunnel 활성화 후 공장 PC `.env`에 BASIC_AUTH_* 변수 누락 사고 (외부 인증 없는 상태로 운영)
- **결정**: JWT 토큰 + React 로그인 페이지 + 7일 sliding refresh + 30일 절대 최대
- **결과**: 3개 서버(6000/카카오/공장) 동기화. `admin` / `orinu2026!`. 분기별 회전 (다음 2026-09-01)
- 상세: `memory/project_web_auth_security.md`

#### 공장 PC 응답 일관성 — `.env` 가짜 알림 자격증명 영구 fix (2026-05-06)
- **증상**: `factory.flickdone.com` 1회차 빠르고 이후 timeout, Cloudflare 524
- **진짜 원인**: `.env`의 가짜 알림 자격증명(SMTP/Slack/FCM 6개)이 매 폴링마다 5초 timeout 누적 → web-api 응답 막힘
- **결정**: `.env` 6개 비우기 → 코드가 `not_configured`로 즉시 skip, `.env.example`도 빈 값 + 사고 이력 주석
- **결과**: 외부 10번 연속 401+0.2초

#### 공장 PC 배포 — 수동 9단계 → `deploy.bat` 1줄 (2026-04-30)
- **배경**: 4/29 원격 복구 시 수동 9단계 절차 + 인코딩 깨진 머지가 라이브 직행할 위험
- **결정**: `deploy.bat` 5단계(git pull / pip / npm build / nssm restart / health check) 각 errorlevel abort. GitHub Actions runner는 라이브 직행 위험으로 기각
- **결과**: 4/30 smoke test 완료 (vite build + NSSM 재시작 + HTTP 200). 5/6에 좀비 정리 로직 추가
- 상세: `memory/project_deploy_bat.md`

#### DB 아키텍처 — MariaDB 11.3 (공장 PC) + SQLite (web-api)
- **배경**: 4/24 오전 "MySQL 미설치" 잘못 판단 → 오후 재조사 시 MariaDB 11.3이 자동시작 중이고 `automation` DB가 sequence_service 로그 적재 중
- **현재**: 공장 PC = MariaDB 11.3(port 3306, 자동화 로그) + SQLite(web-api 프리셋/알림). 6000/카카오 = SQLite 모니터링 전용
- **올바른 확인 방법**: `netstat :3306` → `tasklist` PID → `mysqld.exe` 확인
- **옵션 2c (원격 DB 접근)**: Cloudflare Tunnel TCP ingress + 카카오 VM cloudflared 프록시 + Access Service Token. 5월 후반 구현 예정. 실시간 제어는 절대 터널 경유 금지
- 상세: `memory/project_sequence_service_deployment.md`

#### Phase 4 — OpenMV → MaixCAM 전환 (2026-04-14)
- **배경**: OpenMV AE3로 세척기/경화기 완료 감지 검토 중 → MaixCAM이 성능 우위 (RISC-V + 1 TOPS NPU, 4MP)
- **결정**: 대표님 지시로 OpenMV 제외, MaixCAM으로 전환. Cloud 없이 온디바이스 AI
- **현재**: 빈피킹(Phase 5) 우선, MaixCAM은 여유 시 PoC. 보유 장비 = MaixCAM 1대 + LicheeRV Nano 2대
- 상세: `memory/project_maixcam_monitoring.md`, `memory/project_openmv_image_capture.md` (역사)

#### 빈피킹 카메라 배치 — eye-in-hand 2대 동시 마운트 (2026-04-23 한솔 회의)
- **배경**: 4/10 대표님 피드백 = "1대 고정 + 1대 로봇암(eye-in-hand)" 검토
- **결정**: 4/23 한솔 회의에서 **eye-in-hand + Blaze-112(ToF) + ace2(RGB) 2대 동시 마운트** 합의 (한솔 김주엽 파트장 + 대표님 일치)
- **브라켓**: 코에버 설계 → 오리누 3D 출력 → 검증 후 철제 가공 (5/6 회의 재확인)

#### 빈피킹 좌표 — 6DoF Euler ZYX → 4DoF X,Y,Z,Theta (2026-05-06 한솔 회의)
- **배경**: 빈 안 부품의 다면 처리를 좌표 차원으로 풀려고 하니 복잡
- **결정**: 좌표는 4DoF(X,Y,Z,Theta)로 단순화. 다면 처리는 **자세 분리(A자세/B자세) + 리그립**으로 해결
- **블로커**: 한화 로보틱스 별도 라이브러리/패키지 = 한솔 이예승 ASAP 확인 (답 받기 전 좌표 출력 코드 확정 금지)

#### 한솔 협업 담당자 변경
- **2/24~3/24**: 김기원 주임(`justkiwon`) — PR #3 제출 후 `hansol-dev` 브랜치 전환
- **4/3 퇴사 확인**: 김기원 주임 한솔코에버 퇴사. 직접 지원 불가, 구조/플로우 문의는 가능. **공장 PC origin이 퇴사자 fork 가리켜 4/29 원격 복구 사고로 이어짐**
- **현재**: 이예승 사원 (한솔코에버, GitHub: `eseung97@gmail.com`) — 4/16 머지 2차, 4/23 머지 3차, 5/6 머지 4·5차

---

### 📦 5/8~5/20 Basler 셋업 이력 — ✅ 종료 (상세는 memory)

> 🚨 **끝난 일이다.** 어댑터 8단계 검증 절차·부품 식별·5/12 Mac 풀 작동 경위는
> **`memory/project_basler_setup_history.md`** + `memory/reference_basler_blaze_112.md` +
> `memory/project_ace2_camera.md`에 보존. `CLAUDE.local.md` W19~W21 요약에도 있다.

**남은 결론만** = 카메라 **Blaze-112**(ToF·848×480·**24VDC 고정**·PoE 아님) + **ace2 a2A2448-23gcBAS**
(Sony **IMX547**·**2.74µm**·1/1.8"·RJ45 직결) · 어댑터 **ipTIME U1G-C**(USB 3.0, ⚠️AMCA017은 100Mbps로 사양 미달) ·
Mac 단독 워크플로 확립(pypylon 단독, pylon Viewer는 macOS Blaze 미지원) ·
🚨**GigE throughput 10Mbps 튜닝 필수**(macOS+USB 어댑터가 66Mbps를 못 따라감) ·
카메라 대역 **192.168.20/24 영구 분리**(Wi-Fi 충돌)

### 운영 인프라 핵심 정보 (재확인용)

#### AICA A100 GPU (비전 학습용, 5/18 부활)

| 항목 | 값 |
|------|---|
| 호스트 | `114.110.133.5:2222` (`root` / `vt2603!`) |
| GPU | NVIDIA A100-SXM4-80GB × 1 |
| 환경 | PyTorch 2.1.0 + CUDA 12.8, conda `/opt/conda` (Python 3.10) |
| 작업 디렉토리 | `/workspace` (49TB NFS, 컨테이너 재시작 시 보존) |
| 6000 인증 | ssh ed25519 key 등록 (무비번 접속) |
| 학습 함정 | `/dev/shm 64MB` → `workers=0 cache=ram` 필수 / `python` PATH 미설정 → `/opt/conda/bin/python` |
| 5/18 첫 학습 | YOLOv8n parts-5class-v1: 150 epochs / **10분 22초** / **mAP50 0.988** |

상세: `memory/reference_aica_a100.md` — 접속 / ssh key 4단계 등록 / 환경 구축 / 학습 운영 / 함정 7가지 / 컨테이너 재생성 시 절차

#### 공장 PC 자동 시작 체계 (4/24 완성)

> 공장 PC 재부팅/정전 시 전원 ON만으로 운영 시스템 자동 복구.

| # | 이름 | 종류 | 포트 | 역할 |
|---|------|------|------|------|
| 1 | **cloudflared** | Windows 서비스 | - | Cloudflare Tunnel (`factory.flickdone.com`) |
| 2 | **OrinuMain** ⭐ | Windows 서비스 (NSSM) | 8085 | `python main.py` (web-api + sequence_service + frontend) |
| 3 | **PreFormServer** | 시작 프로그램 | 44388 | Formlabs Local API |
| +a | file_receiver.py | 시작 프로그램 | 8089 | STL 파일 수신 |
| +a | AnyDesk | Windows 서비스 | - | 원격 접속 |

**재부팅 후 체크 (관리자 cmd)**:
```cmd
sc query cloudflared
sc query OrinuMain
netstat -ano | findstr LISTENING | findstr "8085 44388 8089"
curl http://127.0.0.1:8085/
```

**NSSM 운영 명령** (전체 경로 `C:\nssm\nssm-2.24\win64\nssm.exe` 사용, PATH 미등록):
```cmd
# 재시작
C:\nssm\nssm-2.24\win64\nssm.exe restart OrinuMain

# 완전 정리 후 재시작 (좀비 발생 시)
C:\nssm\nssm-2.24\win64\nssm.exe stop OrinuMain
taskkill /F /IM python.exe
timeout /t 5
C:\nssm\nssm-2.24\win64\nssm.exe start OrinuMain
timeout /t 20  # 초기화 15~20초 필수
```

**참고**: `memory/project_cloudflare_tunnel.md`, `memory/reference_factory_pc_deployment_guide.md`, `memory/project_deploy_bat.md`

#### DB 아키텍처 (4/24 확정)

| 서버 | DB | 용도 |
|------|-----|------|
| 공장 PC | **MariaDB 11.3** (port 3306, AUTO_START) | sequence_service `automation` DB |
| 공장 PC | SQLite (`web-api/data/local.db`) | web-api 프리셋/알림/업로드 |
| 6000 / 카카오 VM | SQLite | web-api 모니터링 전용 |

**MariaDB 위치**: `C:\Program Files\MariaDB 11.3\` / 설정 `data\my.ini` / Windows 서비스 이름 `MariaDB`. 5월 후반 옵션 2c(원격 조회) 구현 시 `bind-address=127.0.0.1` + `remote_readonly` 계정 분리 예정.

**🔒 절대 금지**:
- MariaDB 3306을 공유기 포트포워딩으로 인터넷 직접 노출
- Cloudflare Tunnel TCP ingress + Access 미적용 방치
- sequence_service가 터널 경유 DB로 변경 (인터넷 장애 시 로봇 정지)

#### Formlabs Credentials Rotate 주의 (4/24 학습)

- "Rotate Client Secret" 시 **Client ID도 같이 바뀜** → 두 줄 모두 교체
- 반영 누락 시 해당 서버 Formlabs 401 → 폴링 전체 멈춤
- 스크린샷/문서 공유 시 `.env` 모자이크 필수

#### 한솔 협업 — 미해결/대기

- ⚠️ `runtime.py:121` 4/24 후속 패치 (`{1: None, 2: None}` → `{1: None}`) 미커밋 상태
- ⏳ 4/30 미해결 의문: `git fetch` 인증창 안 뜬 이유 (PAT 캐시 추정, 5월 말 PAT 만료 시 재검증) — `memory/project_deploy_bat.md`
- ⏳ "렌즈 2개" 의미 한솔 추가 확인 (5/8 단톡)
- ⏳ 한화 로보틱스 별도 라이브러리/패키지 확인 (5/6 회의 액션, 한솔 이예승 ASAP)
- ⏳ 빈피킹 카메라 브라켓 설계 (5/6 회의 액션, 한솔 이예승 ASAP)
