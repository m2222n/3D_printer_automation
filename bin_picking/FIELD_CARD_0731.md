# 🏭 7/31(금) 공장 실행 카드

> 현장에서 문서 뒤질 시간 없게 한 장으로. **위에서부터 순서대로.**
> 🥇촬영이 1순위 — 나머지 다 못 해도 이것만은 할 것(cross-session 검증의 유일한 전제).

---

## 🎒 챙길 것 (출발 전)

- [ ] **맥북** + 전원
- [ ] **카메라 2대**(Blaze, ACE2) + **랜 어댑터** + 랜케이블
- [ ] **모니터**(사무실 것 — 확장용이라 빼도 맥 본체로 작업 가능) + **HDMI 케이블**
- [ ] ⚠️ **젠더**(IPC-510이 구형이라 **VGA 가능성**. HDMI만 가면 못 씀)
- [ ] **USB 키보드·마우스**(IPC-510용)
- [ ] 줄자(빈↔카메라 거리 확인용)

---

# 🥇 1순위 — 새 세션 촬영 (1~2h)

## 왜: F1 0.818은 학습에 쓴 세션으로 낸 값이라 **"처음 보는 환경에서 되는가"는 아직 답이 없다.** 리포 데이터로는 원리상 검증 불가 → 새 세션 촬영이 유일한 길.

## 1-0. 🔴 맥으로 스크립트 받기 (5분) — **이걸 빼먹으면 촬영이 시작조차 안 된다**

맥 터미널에서:
```bash
ls ~/3D_printer_automation          # ① 리포가 있나 확인
```

**있으면** (권장 — 오버레이 도구·캘리브 json까지 다 받아짐):
```bash
cd ~/3D_printer_automation && git pull
cp bin_picking/depth_track/scripts/blaze_capture_crosssession.py ~/Desktop/
```
⚠️ `git pull`에서 **충돌·"local changes" 오류**가 나면 **덮어쓰지 말 것**(맥에 커밋 안 한
변경이 있을 수 있음). 그때는 아래 scp 방법으로 우회.

**없으면** (촬영만 가능 — 촬영 스크립트는 리포 import 0건이라 파일 1개로 돌아감):
```bash
scp <6000>:~/3D_printer_automation/bin_picking/depth_track/scripts/blaze_capture_crosssession.py ~/Desktop/
```
🔴 이 경우 `setup_camera_net.py`가 없으므로 카메라가 안 열리면 §문제 대응 A의 수동 절차를 쓸 것.

### ⭐ 받았는지 반드시 확인
```bash
cd ~/Desktop && grep -c "raw_is_mm" blaze_capture_crosssession.py
```
🔴 **결과가 0이면 옛 파일이다**(7/30 단위 버그 fix 미포함). 그 상태로 찍으면 화면이 계속
`NOT OK`로 떠서 시간을 다 쓴다. 0이 아니어야 진행.

> ⚠️ 맥의 Claude Code에는 **PATH 캐싱 버그**가 있어 명령이 안 먹을 수 있다 →
> **맥 터미널에서 직접 실행**할 것.

## 1-1. 카메라 네트워크 (1분)
```bash
sudo python bin_picking/tests/setup_camera_net.py --apply
```
> 어댑터를 뺐다 꽂으면 macOS가 en 번호를 재부여해 수동 IP가 무력화된다.
> 이 한 줄이 빈/APIPA 포트를 찾아 필요 대역을 채운다.

**안 열리면** → 아래 §문제 대응 A

## 1-2. 촬영 실행
```bash
cd ~/Desktop
sudo python blaze_capture_crosssession.py --scale 2
```
> ⚠️ 스크립트를 맥으로 먼저 복사해야 함(6000 → 맥).
> 실행 시 **c1 조건 메모를 터미널에 입력**받는다(예: "형광등만, 높이 55cm, 무광 빈").
> OpenCV 창이 떠 있어도 **터미널에 포커스를 옮겨 타이핑**할 것 — 멈춘 게 아니다.

## 1-3. 🔴 화면에서 반드시 확인할 것

```
saved=0/30  next=shot_001  COND=c1
BAND(400-600mm in crop)=12k (7%)  [OK]     ← OK가 떠야 저장 의미 있음
DIST med=487mm                              ← 400~600 안이어야 함
(all_valid=28% fps=8.5)  white box = eval crop region
```

| 표시 | 의미 | 대응 |
|---|---|---|
| `[OK]` | 부품이 평가 영역에 충분히 있음 | 저장(`s`) |
| `[..]` | 부족 | 부품을 **흰 사각형 안**으로, 거리 조절 |
| `DIST <<< TOO FAR/NEAR!` | 대역 밖 | **카메라 높이 조절**(0.5~0.8m) |
| 🚨 `DIST med=3000` 근처인데 실제 50cm | **단위 문제** | 아래 §문제 대응 C |

🔴 **부품은 400~600mm + 화면 중앙 2/3(흰 사각형) 안.** 평가가 그 대역만 남기므로
   벗어나면 **검출 0건**이 된다.

## 1-4. 키
| 키 | 동작 |
|---|---|
| `s` | 저장 |
| **스페이스** | **조건 그룹 변경**(c1→c2). 터미널에 새 조건 메모 입력 |
| `u` | 직전 취소 |
| `q` | 종료 |

## 1-5. ⭐ 조건 3종 (각 7~10장, 총 20~30장)

**부품은 27종 그대로.** 바꾸는 것은 **조건**이다.

| 조건 | 바꿀 것 | 예시 메모 |
|---|---|---|
| **c1** | 조명 | "형광등만, 창측 블라인드 없음" |
| **c2** | 카메라 높이·각도 | "높이 70cm, 15도 기울임" |
| **c3** | 배경·빈 | "무광 검정 빈, 다른 바닥" |

각 조건에서 **부품 위치·자세는 여러 번 바꿔가며** 찍는다(빈에 흩뿌리기).

## 1-6. 🔴 절대 바꾸지 말 것 = 센서 설정
- `OperatingMode=ShortRange` — **스크립트가 자동 확인하고 안 맞으면 종료시킨다**
- 필터 On, 848×480 raw 저장

> 이유: 센서 설정을 바꾸면 성능 저하가 **"일반화 실패"인지 "설정 불일치"인지 구분 불가**.
> 7/6에 LongRange로 찍혀 일부 그룹 품질이 떨어진 사고가 있었다.

## 1-7. 종료 후 — 전송하고 그 자리에서 결과 보기
```bash
# ① 맥 → 6000 전송 (capture_meta.json도 같이 가야 함)
scp -r ~/Desktop/blaze_crosssession_0731 <6000>:/data/jtm/

# ② 6000에서 검출 확인 (라벨 없이 5분, 20~30장)
bash bin_picking/depth_track/scripts/eval_crosssession.sh \
     /data/jtm/blaze_crosssession_0731 nolabel
```
> 추론은 맥에서 불가(GPU 없음) → 6000. **라벨 없이도 "찾긴 찾나"는 그 자리에서 알 수 있다.**
> ✅ 이 경로는 7/29 실측 5장으로 검증됨(장당 8.40건 = 기준 8.01의 105%).
>
> **판정 기준**: 장당 검출이 기준(8.01건)의 50% 이상이면 🟢, 0건 장면이 30% 넘으면 🔴.
> ⚠️ 🟢이어도 **"뭔가 찾았다"일 뿐** — 맞게 찾았는지(F1)는 labelme 라벨링 후.
> 🔴이면 **그날 안에 재촬영 가능**하므로 꼭 돌려볼 것.

---

# 🥈 2순위 — IPC-510 조사 (30분, 세팅 아님)

## 왜: 4/23 입고 후 3개월간 **스펙조차 미확인**. 뭘 깔지 정하려면 스펙을 알아야 한다.

🚫 **아무것도 설치·삭제하지 말 것.** OS는 예승님 회신(윈도우 vs 리눅스) 후.

### 사진 4장 + 확인 1건
- [ ] 🔴 **① 뒷면 포트 전체** ← **최우선**. **랜포트 개수**로 구조가 갈림
      (카메라 2대 + 로봇 = 3대역 필요. 1개뿐이면 스위치 구성 필요)
- [ ] ② 측면·상단 **스티커**(모델명·시리얼)
- [ ] ③ **부팅 화면**(빈 깡통 맞나, BIOS만 뜨나)
- [ ] ④ (OS 있으면) 시스템 정보(CPU·RAM·GPU)
- [ ] ⑤ **인터넷 되나** (안 되면 pip·apt 전부 막힘)

---

# 🥉 3순위 — 로봇 위치 확인 (10분)

- [ ] **HCR-10L·HCR-12가 각각 어느 PC에 물려 있나**
      (우리 기록은 **4/29 기준** = 둘 다 공장 PC, 10L만 IPC-510 이전 예정)
- [ ] 아진 IO 보드가 공장 PC에 물려 있나(있으면 sequence_service 이전 불가)

> 예승님 회신이 오면 생략 가능.

---

# 4️⃣ 4순위 — 오버레이 정합 검증 (1h, 시간 남으면)

## 왜: 두 카메라 정렬값(7/28: x=44.7mm·회전 1.78°·산포 5.29mm)이 **맞는지 확인하는 유일한 방법.**
산포는 "일관성"만 보고(일관되게 틀릴 수도), 자 측정은 x축만 가능하다.
⚠️ **RGB 융합의 전제이지만 지금 빈피킹 돌리는 데는 필수 아님** → 촬영 우선.

```bash
sudo python bin_picking/tests/verify_rgbd_overlay.py --dilate 0
```
- `a`키 = 오버레이 on/off 비교
- 필요 조건: **무광 빈 · top-down 지그 · 0.5~0.8m · 850nm 직사광 회피**
- 판정: 커버리지가 수십 %로 오르고 **부품 위에 색이 제대로 앉는지**
- ⚠️ 재택 실패 원인 = Blaze 108° 초광각이라 커버리지 0.4~3.4%(커튼을 재고 있었음)

---

# 5️⃣ 기회 되면
- [ ] 💡 **불량품 자연 발생분** 있으면 챙기기(출력 실패·미성형·휨). 의도 제작은 안 함
- [ ] 빈↔카메라(TCP) 작동거리 실측
- [ ] ⏸️ hand-eye 캘리브 = 로봇 전원·펜던트 필요, 별도 시간 확보 후

---

# 🔧 문제 대응

## A. 카메라가 안 열림 (`Failed to download XML` / discover 실패)
⭐ **에러에 찍힌 IP를 먼저 읽어라** — 7/29에 이 한 줄이 정답을 가리켰다.

1. **셸 환경변수 확인**(가장 흔한 원인)
   ```bash
   env | grep BASLER
   ```
   옛 IP가 있으면 `unset BASLER_ACE2_IP BASLER_BLAZE_IP`
2. **랜포트 IP 확인** — `ifconfig | grep -A3 en` → `169.254.x`(APIPA)면 소멸된 것
   → `sudo python bin_picking/tests/setup_camera_net.py --apply`
3. **실제 물린 포트 확인**
   ```bash
   arp -an | grep -i <MAC>     # → "on en8" 식으로 찍힘
   ```
   ⭐ 규칙 = **카메라 IP 대역 = 그 카메라가 꽂힌 랜포트 대역**.
   어긋나면 **열거(발견)는 되는데 열기만 timeout**(열거=브로드캐스트, 열기=유니캐스트)
4. **케이블 반대로 꽂았나** 확인(7/28 실제 원인이 이것이었음)
5. IP Configurator로 Static 확인(F5 Refresh)

## B. ShortRange 오류로 스크립트가 종료됨
의도된 동작이다(경고 아니라 종료). pylon Viewer에서 `OperatingMode`를 ShortRange로
바꾸고 재시도. **이걸 무시하고 찍으면 그 데이터는 해석 불가**가 된다.

## C. 🚨 DIST가 3000mm 근처로 뜨는데 실제로는 50cm 촬영
**단위 스케일 문제.** 저장 npy는 `raw × 10/65535`가 mm인데, 라이브 값이 이미 mm일 수 있다
(현장 확인 미완). 이때만:
```bash
sudo python blaze_capture_crosssession.py --scale 2 --raw-is-mm
```
> 저장 원본은 어느 쪽이든 raw 그대로라 **데이터는 안전**하다. 판정 표시만 달라진다.

## D. 촬영 중 스크립트가 죽음
`capture_meta.json`과 npy는 **매 장 즉시 저장**되므로 유실 없다.
다시 실행하면 **마지막 번호 다음부터 이어찍기**된다(조건 번호도 이어짐).

---

# ✅ 최소 성공 기준
- 🔴 **0순위: 맥으로 스크립트 받기**(§1-0) — 이게 안 되면 나머지가 전부 불가
- 🥇 **새 세션 20장 이상 + `capture_meta.json`** → 6000 전송 완료
- 🥈 IPC-510 뒷면 포트 사진 1장(랜포트 개수)
- 나머지는 보너스

> 🔴 촬영을 못 하면 그날의 다른 성과로 대체 불가하다.
> cross-session 결과가 8월 이후 방향(로봇 진행 vs 데이터 보강)을 결정한다.
