# 🥇 빈피킹 ToDo — 궁극 목표 기준 (2026-08-21 재작성)

> 🚨 **이 파일은 고정 파일명이다 — `E2E_TODO_MMDD.md` 같은 새 파일을 만들지 말고 이 파일을 덮어쓴다.**
> 근거 = 매주 새 파일이 생기면 ①어느 것이 현행인지 grep으로 갈리지 않고 ②CLAUDE.md 인용이 깨진다.
> 주차별 이력은 `CLAUDE.local.md` 주간 요약에 이미 남는다. (2026-08-21 규칙 확정)

## 🥇🥇🥇 궁극 목표 (태민님 8/21 *"가장 중요한 말"*)

> *"**Depth랑 RGB는 무조건 연동**이야. 두개를 같이해서 **카메라 위치를 로봇 암에 달아서
> 로봇좌표를 통해 로봇 그리퍼가 우리 부품을 집을 수 있게** 하는게 **궁극적인 문제**."*

**네 조각** = ①Depth+RGB 융합(무조건) ②eye-in-hand(카메라를 로봇암에) ③hand-eye(로봇 좌표) ④**그리퍼가 집는다**

🚨🚨 **내가 반복해서 이걸 "범위 밖"으로 밀었다.** 8/5(로봇 제어 제외)를 근거로
hand-eye·파지를 후순위로 내렸고, 8/20엔 RGB까지 *"우선순위 하향"* 으로 판정했다.
⭐⭐ **재는 자를 잘못 골랐다** — 내 자는 *"인식 F1 개선 효율"*(RGB 몫 0.8%)이었고
🥇 **태민님 자는 "집을 수 있는가"** 다. **RGB는 효율 문제가 아니라 전제다.**
📌 **"사업 범위"와 "궁극 목표"는 다른 축** — 섞어서 목표를 깎지 말 것.

---

## ✅ 선행조건 실측 (8/21) — A~C는 이미 끝났다

| 단계 | 내용 | 상태 |
|---|---|---|
| **A** | ACE2 렌즈 + intrinsic | 🟢 7/20 완료 (RMS 0.546px) |
| **B** | Blaze↔ACE2 extrinsic | 🟢 7/28 완료 (산포 5.29mm) |
| **C** | RGB-D 오버레이 정합 | 🟢 7/31 완료 (각도 바꿔도 유지) |
| **D** | **모델에 RGB 입력** | 🔴 **0건** (`mentoring_new`에 `rgb` grep 0) |
| **E** | **hand-eye**(카메라↔플랜지) | 🔴 미착수 (로봇 필요) |
| **F** | 그리퍼 실장 + 파지 시험 | 🔴 미착수 (그리퍼 미장착) |

⇒ ⭐⭐ **하드웨어·캘리브 비용은 이미 지불됐다. 남은 것은 D·E·F.**

---

## 📋 ToDo (순서 = 두 축을 함께 만족시키는 순)

### ✅ T1. E2E 러너 — 지금 되는 것을 한 명령으로 **완료 (8/21)**
**왜 먼저인가** = 사업 산출물(모듈→웹)에 직결되고 **1일이면 되고**,
D·E·F 전부 *"이 사슬 위에 얹는 것"* 이라 **먼저 있어야 얹을 자리가 생긴다.**
- ✅ `bin_picking/src/run_binpick_e2e.py` 신설 — 촬영결과→추론→6요소→게이트→웹
- ✅ **검증된 플래그를 코드에 못박음**(8/14에 `--real_uint16_max_depth_m` 누락으로
  부품 이름이 바뀌는 것을 겪었다 — 사람이 매번 치면 또 틀린다)
- ✅ **A100 실행 2/2 성공** — `shot_001` 검출 7건[09_guide_paper_r·07_guide_paper_l·13_x2·brkt_switch]
  장면 `in_distribution`(3.13%) · 웹 ✅ / `shot_002` 7건 · 웹 ✅
- 🚨🚨 **알려진 한계 = KTR 3초 초과.** A100 8/21 실측 6.7~7.5초 / **6000 8/25 실측 4.27초**
  ~~📌 **해법 = 상주 프로세스**~~
  🚨🚨 **[2026-08-25 정정] 상주 프로세스는 해법이 아니다 — 실측으로 확인했다.**
  모델 1회 로드 배치 러너(`src/infer_batch_runner.py`)를 만들어 재보니
  **4.27 → 3.44초(18%)** 에서 멈췄다(기대값 2.4초는 틀렸다).
  cProfile 로 3.20초를 갈라보니 **모델 forward 가 1.92초(60%)** ·
  postprocess 0.57초 · 나머지 0.7초 ⇒ ⭐ **forward 가 벽이다.**
  🥇 **본해법 = GPU** — IPC 에 **RTX 5060 이 있는데 torch 가 `+cpu` 빌드**라 안 쓴다.
  ⚠️ **[미확인]** GPU 에서 몇 초가 되는지는 못 재봤다(6000 에 GPU 없음).
  🚨 **[확인필요]** KTR *"응답속도 3초"* 의 판정 범위 = **추론만인가 촬영·전송·표시까지인가**
  📌 교훈 = **원인 진단(재로드)은 맞았지만 그 크기를 재보지 않아 해법 효과를 과대평가했다.**
- 🐛 클래스명을 `BinPickingReporter`로 **지어 썼다**(실제 `WebReporter`) — 추측 금지 8번째.
  ⭐다만 AttributeError로 **크게 실패해 즉시 잡혔다**
- ⏭️ **남은 것 = IPC 이식**(카메라·로봇이 붙는 자리 · T3~T5의 실행 무대)

### ✅ T2. web-api 재시작 — 사슬 마지막 칸 **완료 (8/21)**
7/10부터 42일 돈 프로세스(PID 257740)를 교체. **8/13 라우터가 처음 올라왔다.**
- ✅ **binpick 경로 5개 등록**(openapi 확인) — `reports`(POST) · `health`/`scenes`/`scenes/latest`/`scenes/{pk}`(GET)
- ✅ **실왕복 = HTTP 201**(A100 E2E 산출물 `shot_001_c1.six.json`을 6000 라우터로)
- ✅ **조회로 끝까지 확인** = 검출 7건 · 부품 이름 4개 · 게이트 `in_distribution`(3.13%) ·
  `trusted: true` · 지연 7452ms · `binpick_scenes 1 / binpick_detections 7`
- ✅ **운영 데이터 보존** — presets 5 · print_jobs 7 · vision 4/5 전부 동일(백업도 떠둠)
  ⭐ `notification_events` 282→290은 **기동 시 프린터 4대 OFFLINE 기록**(6000은 프린터 접근 불가).
  7/31·7/16·7/10에도 같은 패턴이 있어 **정상 동작**임을 날짜별 집계로 확인했다.
- 🐛 **로그에 `address already in use`** = nohup이 두 번 떠서 하나가 죽은 것.
  ⚠️ **살아있는 프로세스는 1개**(PID 1035233)이고 포트도 정상 점유. 결과에는 영향 없다.
- 🐛 **내 실수 1건** = 조회 응답에서 게이트를 **최상위 키로 추측해 읽어** `None`으로 보고
  *"설계원칙 3이 깨졌다"* 고 오판했다. 실제로는 **`scene_gate` 중첩 객체에 정상 저장**돼 있었다.
  ⇒ 🚨 **또 키 이름을 추측했다**([[never-state-unverified]])

🚨 **재기동 명령(다음에도 이걸 쓴다)**
```bash
cd ~/3D_printer_automation/web-api
kill <PID>   # ss -tlnp | grep 8085 으로 확인
nohup ./venv/bin/uvicorn app.main:app --host 0.0.0.0 --port 8085 > /tmp/webapi.log 2>&1 &
```
⚠️ **6000은 개발·데모 서버다.** 공장 PC는 NSSM `OrinuMain`이라 절차가 다르다([[deploy-bat]]).

### ⬜ T3. 🥇 **RGB 융합 D단계** — 📅 **8/26(화) 재택**

> 🚨🚨 **[8/21 정정] 내가 *"90장에 RGB가 이미 동봉돼 있다"* 고 쓴 것은 틀렸다.**
> 전수 확인 결과: `.npy` = **(480,848) uint16 2D = depth 단독** ·
> `.png` = **depth를 컬러맵으로 렌더한 라벨링용**(`make_label_png.py` 산출물, 1696×960) ·
> **ACE2 RGB 파일은 0건** · `/data/jtm` 어디에도 **RGB 실측 데이터가 없다.**
> ⇒ **오늘 6번째 실수와 같은 형태**(확인 없이 "있다"고 단정) → [[never-state-unverified]]

**⭐ 그래서 T3는 "모델에 RGB를 붙이는 일"이 아니라 "RGB 데이터를 만드는 일"부터다.**

#### 🚨 진짜 선행조건 = RGB 실측 촬영 (공장 필요)

| | 필요한 것 | 어디서 |
|---|---|---|
| **D-0** | **Blaze + ACE2 동시 촬영** — depth와 RGB를 **같은 장면에서** | 🏭 **공장** |
| D-1 | RGB를 depth 좌표계로 정렬(extrinsic 적용) | 🏠 재택 |
| D-2 | 모델 입력에 RGB 채널 추가 + A100 학습 | 🏠 재택 |
| D-3 | depth-only와 **같은 자로 비교** | 🏠 재택 |

⚠️ **D-0이 없으면 D-1~3을 못 한다** ⇒ 🚨**8/26(화) 재택에 D단계 학습은 불가능하다.**

#### ⭐ 그래도 8/26 재택에 할 수 있는 것 (D-0 없이)

1. **동시 촬영 스크립트 작성** — 기존 `blaze_capture_crosssession.py`는 **Blaze 단독**이다.
   ACE2를 함께 트리거해 **쌍으로 저장**하는 코드가 필요하고, 이건 **카메라 없이도 짤 수 있다**
   (7/28에 두 카메라 동시 연결까지는 뚫어놨다 · extrinsic도 있다)
2. **RGB 정렬 파이프라인**(D-1) — `rgbd_fusion.py`·`extrinsic_io.py`가 이미 있다.
   ⇒ **7/31 오버레이 검증 코드를 "학습 입력 생성"으로 확장**
3. **모델 입력 경로 설계**(D-2 준비) — `mentoring_new`에 RGB 채널을 어떻게 넣을지.
   🚨 조교 모델은 **depth 1채널 전제**라 입력층·전처리를 봐야 한다
4. ⭐ **RGB 촬영 현장 카드** — 8/18에 배경재 판정이 통했던 이유가 **현장 카드**였다

#### 🚨 판정 기준 (미리 못박는다)

지금 **F1 0.5998 · 치명 17건**이 **줄어드는가**. KAIST 경고(*"회색 단색이면 RGB 이득 작다"*)가
맞을 수도 있고 **그건 붙여봐야 안다**. ⚠️**train-on-test 금지** — 90장은 재학습 C의 학습 데이터.

### ⬜ T4. hand-eye 캘리브레이션 (E) — 로봇 필요
카메라로 보드를 찍으며 로봇을 움직여 **카메라↔플랜지 변환**을 구한다.
- 🟢 준비됨 = `hand_eye_calibration.py`(8/3에 eye-in-hand 단일로 정정) · ChArUco 보드 자체 제작
- 🚨 **선행** = IPC 세팅(8/5 지적: PC 없으면 성립 안 함) · 브래킷 장착(5분)
- ⚠️ **레이아웃 B안이면 기준이 바뀐다**(대차 좌표계) → 8/19 회의 결과 확인 필요

### ⬜ T5. 그리퍼 실장 + 파지 시험 (F) — 최종 판정
**계산이 아니라 실물로 집어본다.** 지금 파지율 95.5%는 **치수 DB 대조 계산값**이다.
- 🚨 미결 = **조우 실물 두께**(제조사 비공개) · **TCP payload 1.5 vs 2.19kg**
  ⇒ 예승님 메시지에 이미 포함(발송 대기)

### ⬜ T6. 운영 절차서 — 공장에서 사람이 무엇을 누르는가
전원→서비스 확인→러너 실행→결과 확인→실패 시 대처. 1장.

---

## 🚨 경영진 확인이 필요한 것

**T4·T5는 8/5 회의(*"로봇 제어는 이번 사업 전부 제외"*) 범위 밖**이다.
⇒ **"궁극 목표로 진행한다"는 확인**이 필요하다.
📌 다만 **완성물 ②에 *"스핀들·그리퍼 공정 시퀀스 제어"* 가 문자로 있고**,
경영진 8/18 방침 = **트리거 방식**(로봇 동작은 펜던트가 실행, 우리는 좌표·신호를 준다)
⇒ ⭐ **"우리가 좌표를 만들어 트리거를 보내는 것"까지는 산출물 안에 있다고 읽힌다.**

## 🚫 지금 하지 않는 것 (실측으로 천장 확인됨)

추가 촬영(+0.03/90장) · 합성 증강(0.203) · HP 튜닝(16조합 소진) ·
**TTA**(8/21 실측 −0.070·8배 느림)
