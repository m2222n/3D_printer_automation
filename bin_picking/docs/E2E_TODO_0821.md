# 🥇 빈피킹 ToDo — 궁극 목표 기준 (2026-08-21 재작성)

## 🥇🥇🥇 궁극 목표 (태민님 8/21 *"가장 중요한 말"*)

> *"**Depth랑 RGB는 무조건 연동**이야. 두개를 같이해서 **카메라 위치를 로봇 암에 달아서
> 로봇좌표를 통해 로봇 그리퍼가 우리 부품을 집을 수 있게** 하는게 **궁극적인 문제**."*

**네 조각** = ①Depth+RGB 융합(무조건) ②eye-in-hand(카메라를 로봇암에) ③hand-eye(로봇 좌표) ④**그리퍼가 집는다**

🚨🚨 **내가 반복해서 이걸 "범위 밖"으로 밀었다.** 8/5(로봇 제어 제외)를 근거로
hand-eye·파지를 후순위로 내렸고, 8/20엔 RGB까지 *"우선순위 하향"* 으로 판정했다.
⭐⭐ **재는 자를 잘못 골랐다** — 내 자는 *"인식 F1 개선 효율"*(RGB 몫 0.8%)이었고
🥇 **태민님 자는 "집을 수 있는가"** 다. **RGB는 효율 문제가 아니라 전제다.**
📌 **"사업 범위"와 "궁극 목표"는 다른 축** — 섞어서 목표를 깎지 말 것.

---

## ✅ 선행조건 실측 (8/21) — A~C는 이미 끝났다

| 단계 | 내용 | 상태 |
|---|---|---|
| **A** | ACE2 렌즈 + intrinsic | 🟢 7/20 완료 (RMS 0.546px) |
| **B** | Blaze↔ACE2 extrinsic | 🟢 7/28 완료 (산포 5.29mm) |
| **C** | RGB-D 오버레이 정합 | 🟢 7/31 완료 (각도 바꿔도 유지) |
| **D** | **모델에 RGB 입력** | 🔴 **0건** (`mentoring_new`에 `rgb` grep 0) |
| **E** | **hand-eye**(카메라↔플랜지) | 🔴 미착수 (로봇 필요) |
| **F** | 그리퍼 실장 + 파지 시험 | 🔴 미착수 (그리퍼 미장착) |

⇒ ⭐⭐ **하드웨어·캘리브 비용은 이미 지불됐다. 남은 것은 D·E·F.**

---

## 📋 ToDo (순서 = 두 축을 함께 만족시키는 순)

### 🔄 T1. E2E 러너 — 지금 되는 것을 한 명령으로 (진행 중)
**왜 먼저인가** = 사업 산출물(모듈→웹)에 직결되고 **1일이면 되고**,
D·E·F 전부 *"이 사슬 위에 얹는 것"* 이라 **먼저 있어야 얹을 자리가 생긴다.**
- ✅ `bin_picking/src/run_binpick_e2e.py` 신설 — 촬영결과→추론→6요소→게이트→웹
- ✅ **검증된 플래그를 코드에 못박음**(8/14에 `--real_uint16_max_depth_m` 누락으로
  부품 이름이 바뀌는 것을 겪었다 — 사람이 매번 치면 또 틀린다)
- ⏭️ A100에서 실왕복 검증 → IPC 이식

### ⬜ T2. web-api 재시작 — 사슬 마지막 칸
🔴 **태민님 승인 필요**(3개월 돈 프로세스). 지금 `/binpick/reports`가 **404**다.

### ⬜ T3. 🥇 **RGB 융합 D단계** — 궁극 목표의 유일한 "재택 가능" 조각
**⭐ 여기가 궁극 목표의 실제 착수점이다.**
- 입력 = **8/18 90장에 RGB가 이미 동봉**돼 있다(새 촬영 불필요)
- 작업 = ①RGB를 모델 입력에 붙이는 경로 신설 ②A100 학습 ③**depth-only와 같은 자로 비교**
- 🚨 **정직한 판정 기준** = 지금 F1 0.5998 · 치명 17건이 **줄어드는가**.
  KAIST 경고(*"회색 단색이면 RGB 이득 작다"*)가 맞을 수도 있고, **그건 붙여봐야 안다.**
- ⚠️ **train-on-test 금지** — 90장은 재학습 C의 학습 데이터다. 새 split 필요.

### ⬜ T4. hand-eye 캘리브레이션 (E) — 로봇 필요
카메라로 보드를 찍으며 로봇을 움직여 **카메라↔플랜지 변환**을 구한다.
- 🟢 준비됨 = `hand_eye_calibration.py`(8/3에 eye-in-hand 단일로 정정) · ChArUco 보드 자체 제작
- 🚨 **선행** = IPC 세팅(8/5 지적: PC 없으면 성립 안 함) · 브래킷 장착(5분)
- ⚠️ **레이아웃 B안이면 기준이 바뀐다**(대차 좌표계) → 8/19 회의 결과 확인 필요

### ⬜ T5. 그리퍼 실장 + 파지 시험 (F) — 최종 판정
**계산이 아니라 실물로 집어본다.** 지금 파지율 95.5%는 **치수 DB 대조 계산값**이다.
- 🚨 미결 = **조우 실물 두께**(제조사 비공개) · **TCP payload 1.5 vs 2.19kg**
  ⇒ 예승님 메시지에 이미 포함(발송 대기)

### ⬜ T6. 운영 절차서 — 공장에서 사람이 무엇을 누르는가
전원→서비스 확인→러너 실행→결과 확인→실패 시 대처. 1장.

---

## 🚨 대표님 확인이 필요한 것

**T4·T5는 8/5 회의(*"로봇 제어는 이번 사업 전부 제외"*) 범위 밖**이다.
⇒ **"궁극 목표로 진행한다"는 확인**이 필요하다.
📌 다만 **완성물 ②에 *"스핀들·그리퍼 공정 시퀀스 제어"* 가 문자로 있고**,
대표님 8/18 = **트리거 방식**(로봇 동작은 펜던트가 실행, 우리는 좌표·신호를 준다)
⇒ ⭐ **"우리가 좌표를 만들어 트리거를 보내는 것"까지는 산출물 안에 있다고 읽힌다.**

## 🚫 지금 하지 않는 것 (실측으로 천장 확인됨)

추가 촬영(+0.03/90장) · 합성 증강(0.203) · HP 튜닝(16조합 소진) ·
**TTA**(8/21 실측 −0.070·8배 느림)
